# Civic Commons — Master Implementation Plan

**Version:** 1.0  
**Status:** Active  
**Last Updated:** 2026-08-05  
**Role:** Single Source of Truth for All Development Work

---

## Overview

This document is the authoritative implementation roadmap for building The Civic Commons platform from scratch. All development work must follow this plan sequentially. No task may be skipped, and no phase may be bypassed.

**Critical Directive:** Before writing any code, consult this plan. Execute tasks in order. Check off completed items immediately. Do not proceed to UI development until the local data, queuing, and cryptographic layers are fully tested and verified.

---

## Phase 1: Environment & CI/CD Setup

### Objective
Establish the foundational development environment, infrastructure-as-code, and continuous integration pipeline to support secure, compliant development across all phases.

### 1.1 Repository & Development Environment Setup
- [x] Initialize Git repository with main branch protection rules
- [x] Create `.gitignore` with exclusions for: `.env`, `*.key`, `*.pem`, `build/`, `.dart_tool/`, `node_modules/`, secrets
- [x] Set up pre-commit hooks: `husky` for linting, format checking, and secret scanning (using custom script)
- [x] Configure `commitlint` with conventional commit format enforcement
- [x] Create directory structure following the technical stack specification:
  ```
  /client (Flutter)
  /services (Go microservices)
  /infrastructure (Terraform, Helm)
  /docs (API specs, architecture diagrams)
  /scripts (deployment, utilities)
  ```
- [x] VERIFY: Run secret scanning script to confirm secret scanning is operational
- [x] VERIFY: Test pre-commit hook with a dummy commit containing a fake API key to confirm rejection

### 1.2 Infrastructure as Code (Terraform)
- [x] Set up Terraform workspace for Hetzner Cloud (eu-central region)
- [x] Define VPC networking with private subnet for database cluster
- [x] Create Kubernetes cluster (1.29+) with node pool configuration
- [x] Define Cloudflare CDN and DDoS protection resources
- [x] Create MinIO distributed mode storage resources (4 nodes, 2 drives each)
- [x] Set up HashiCorp Vault instance for secrets management
- [x] VERIFY: Run `terraform plan` and confirm no destructive changes to existing infrastructure
- [x] VERIFY: Validate Terraform configuration with `tflint` and `checkov` for security compliance

### 1.3 Kubernetes Infrastructure (Helm & ArgoCD)
- [x] Install Helm 3 and configure Helm repositories
- [x] Create Helm charts for: PostgreSQL, Redis, MinIO, NATS, Meilisearch
- [x] Set up ArgoCD for GitOps deployment
- [x] Configure ArgoCD application manifests for all infrastructure components
- [x] Define secrets management strategy: External Secrets Operator syncing from HashiCorp Vault
- [x] VERIFY: Deploy test namespace and confirm ArgoCD syncs successfully
- [x] VERIFY: Test secret injection by creating a test secret in Vault and confirming it appears in Kubernetes

### 1.4 CI/CD Pipeline (GitHub Actions / GitLab CI)
- [x] Create CI pipeline for Flutter client: lint, analyze, unit tests, build APK/IPA
- [x] Create CI pipeline for Go services: gofmt, go vet, unit tests, race detector, security scan (`gosec`)
- [x] Create infrastructure validation pipeline: Terraform plan, `tflint`, `checkov`
- [x] Set up automated dependency scanning: `dependabot` or Renovate
- [x] Configure artifact signing for production builds
- [x] VERIFY: Trigger CI pipeline on a test commit and confirm all jobs pass
- [x] VERIFY: Test security scanner by intentionally adding a vulnerable dependency and confirm pipeline failure

### 1.5 Observability Stack (LGTM)
- [x] Deploy Prometheus with service discovery for Kubernetes
- [x] Deploy Grafana with pre-configured dashboards for: system health, API latency, error rates
- [x] Deploy Loki for log aggregation with PII-scrubbing configuration
- [x] Deploy Tempo for distributed tracing
- [x] Configure log scrubbing rules: redact phone numbers, emails, blind_hash_id patterns
- [x] VERIFY: Ingest test logs containing fake PII and confirm Loki stores only scrubbed versions
- [x] VERIFY: Generate test trace data and confirm it appears in Tempo with proper span correlation

### 1.6 Security Baseline
- [x] Configure Falco for runtime security monitoring
- [x] Set up network policies in Kubernetes (deny-all, allow-specific)
- [x] Configure pod security standards (restricted) for all workloads
- [x] Set up automated backup for PostgreSQL WAL to MinIO
- [x] Configure SSL/TLS certificates with cert-manager for all ingress
- [x] VERIFY: Run a pod with privileged container and confirm Falco detects and alerts
- [x] VERIFY: Test network policy by attempting unauthorized inter-pod communication and confirm denial

---

## Phase 2: Local Cryptography & Zero-Knowledge Layer

### Objective
Implement the client-side cryptographic foundation that ensures zero-knowledge architecture: all encryption happens on-device, keys never leave the secure enclave, and servers only ever handle ciphertext.

### 2.1 Flutter Project Initialization
- [x] Create Flutter 3.x project with minimum SDK constraint matching tech stack
- [x] Configure `pubspec.yaml` with pre-approved dependencies only (libsignal, sqflite_sqlcipher, etc.)
- [x] Set up Flutter linter rules in `analysis_options.yaml`
- [x] Configure code coverage for unit tests
- [x] VERIFY: Run `flutter pub get` and confirm no dependency conflicts
- [x] SECURITY CHECKPOINT: Confirm no cloud-based AI or analytics SDKs are in dependencies

### 2.2 Cryptography Service Foundation
- [x] Create `lib/crypto/crypto_service.dart` with abstract interface for encryption/decryption
- [x] Implement Argon2id key derivation for PIN-to-database-key (memory=64MB, iterations=3, parallelism=4)
- [x] Implement Ed25519 key pair generation for identity keys
- [x] Implement Curve25519 key pair generation for Signal Protocol prekeys
- [x] Create key storage wrapper using `flutter_secure_storage` with hardware-backed keystore
- [x] VERIFY: Write unit tests for Argon2id derivation with known test vectors
- [x] VERIFY: Write unit tests for key generation and confirm keys are not exportable from secure storage
- [x] SECURITY CHECKPOINT: Confirm no private keys are ever written to SQLite or logged

### 2.3 Signal Protocol Implementation
- [x] Implement X3DH key agreement protocol using `libsignal_protocol_dart`
- [x] Implement Double Ratchet session encryption
- [x] Create prekey management system: signed prekey rotation (7 days), one-time prekey batch (100 keys)
- [x] Implement session state storage in SQLCipher database
- [x] Create public key bundle API structure for sharing with other users
- [x] VERIFY: Write unit tests for X3DH handshake with known test vectors
- [x] VERIFY: Write unit tests for Double Ratchet forward secrecy (confirm message keys are discarded)
- [x] SECURITY CHECKPOINT: Confirm message content is never decrypted server-side in tests

### 2.4 Identity Hashing (Phone Number to Blind Hash)
- [x] Implement Argon2id hashing for phone numbers with salt fetched from secure backend
- [x] Create phone number validation (E164 format)
- [x] Implement salt rotation logic (quarterly) with fallback support
- [x] Create blind hash ID generation and storage
- [x] VERIFY: Write unit tests for phone hashing with known salt and confirm output matches expected hash
- [x] VERIFY: Write unit tests for salt rotation and confirm old hashes can still be validated
- [x] SECURITY CHECKPOINT: Confirm raw phone numbers are never persisted to disk or logged

### 2.5 Duress PIN Implementation
- [x] Implement dual PIN registration flow (real PIN + duress PIN)
- [x] Create separate database key derivation paths for each PIN
- [x] Implement decoy database (`vault_decoy.db`) initialization
- [x] Create database selection logic based on which PIN successfully decrypts
- [x] VERIFY: Write unit tests for duress PIN unlocking decoy database
- [x] VERIFY: Write unit tests for real PIN unlocking real database
- [x] SECURITY CHECKPOINT: Confirm app never stores which PIN is real vs duress

### 2.6 Hardware Security Integration
- [x] Implement `FLAG_SECURE` wrapper for Android using platform channel
- [x] Create secure screen wrapper widget for Vault and War Room screens
- [x] Implement root/jailbreak detection (local only, no telemetry)
- [x] Create graceful degradation flow for rooted devices (warning, not block)
- [x] VERIFY: Test on Android device and confirm screenshot is blocked when FLAG_SECURE is active
- [x] VERIFY: Test on rooted device and confirm warning appears without sending telemetry
- [x] SECURITY CHECKPOINT: Confirm no device fingerprinting data is sent to server

### 2.7 Zero-Plaintext Logging System
- [x] Create custom logging wrapper that redacts PII patterns
- [x] Implement hash-only logging for sensitive operations (log hash IDs, not raw data)
- [x] Add boolean success/fail logging for cryptographic operations
- [x] Configure logging levels for development vs production
- [x] VERIFY: Write unit tests that attempt to log PII and confirm redaction occurs
- [x] VERIFY: Write unit tests for hash-only logging and confirm reversibility is impossible
- [x] SECURITY CHECKPOINT: Confirm no `print()` or `debugPrint()` outputs raw payload data

---

## Phase 3: Offline-First SQLite/Queue Repository

### Objective
Build the local data layer with SQLCipher encryption and a queue-based sync system that ensures the app works offline and syncs resiliently when connectivity returns.

### 3.1 SQLCipher Database Schema Design
- [x] Define Drift/SQLCipher schema for core entities:
  - `users` (blind_hash_id, username, device_pubkey)
  - `conversations` (id, participant_hash, encrypted_session_state)
  - `messages` (id, conversation_id, ciphertext, delivered, expires_at)
  - `connection_requests` (id, requester_hash, recipient_hash, status)
  - `sync_queue` (id, operation_type, payload, status, retry_count)
- [x] Create database migration system
- [x] Implement database key derivation from user PIN (Argon2id)
- [x] VERIFY: Write unit tests for schema creation and migration
- [x] VERIFY: Write unit tests for database encryption/decryption with wrong key failure
- [x] SECURITY CHECKPOINT: Confirm all sensitive columns are encrypted at rest

### 3.2 Repository Pattern Implementation
- [x] Create abstract `BaseRepository` class with standard CRUD operations
- [x] Implement `ConversationRepository` for Vault data
- [x] Implement `MessageRepository` with local-first read/write
- [x] Implement `SyncQueueRepository` for pending operations
- [x] Create repository interface that returns local data immediately, then syncs
- [x] VERIFY: Write unit tests for repository CRUD operations
- [x] VERIFY: Write unit tests for repository returning cached data before sync
- [x] SECURITY CHECKPOINT: Confirm repositories never make direct HTTP calls

### 3.3 Sync Queue Implementation
- [x] Create `SyncQueueItem` model with fields: operation_type, payload, status, retry_count, created_at
- [x] Implement queue status enum: `pending`, `in_progress`, `success`, `failed`
- [x] Create queue insertion logic for all mutation operations (POST, PUT, DELETE)
- [x] Implement retry logic with exponential backoff (1s, 2s, 4s ... 5 min max)
- [x] VERIFY: Write unit tests for queue insertion and status transitions
- [x] VERIFY: Write unit tests for exponential backoff calculation
- [x] SECURITY CHECKPOINT: Confirm queued payloads are encrypted before storage

### 3.4 WorkManager Background Sync
- [x] Configure `workmanager` plugin for background task execution
- [x] Create background sync worker that processes pending queue items
- [x] Implement network state detection using `connectivity_plus`
- [x] Create sync trigger on network reconnection
- [x] Implement chunking logic for batch operations (max 10 items per batch)
- [x] VERIFY: Write integration tests for background sync execution
- [x] VERIFY: Write unit tests for network state detection
- [x] SECURITY CHECKPOINT: Confirm background sync respects offline-first architecture

### 3.5 Local State Management (BLoC/Cubit)
- [x] Create BLoC for conversation state (streams from local database)
- [x] Create BLoC for message state (streams from local database)
- [x] Create BLoC for sync status (LIVE, CACHED, QUEUED, OFFLINE)
- [x] Implement state persistence using Hive for non-sensitive data
- [x] VERIFY: Write unit tests for BLoC state transitions
- [x] VERIFY: Write unit tests for database stream emissions to BLoC
- [x] SECURITY CHECKPOINT: Confirm BLoC never exposes raw decrypted data in logs

### 3.6 Hive Local Storage for Non-Sensitive Data
- [x] Create Hive boxes for: ledger_drafts, academy_progress, karma_cache
- [x] Implement Hive initialization with encryption for sensitive boxes
- [x] Create cache invalidation logic for karma scores (5 min TTL)
- [x] VERIFY: Write unit tests for Hive CRUD operations
- [x] VERIFY: Write unit tests for cache invalidation TTL logic
- [x] SECURITY CHECKPOINT: Confirm no PII or sensitive data in unencrypted Hive boxes

---

## Phase 4: API Gateway & Backend Services Foundation

### Objective
Build the API Gateway layer and core backend services in Go, implementing the service boundary architecture defined in the technical stack.

### 4.1 Go Project Structure & Dependencies
- [x] Create Go 1.22+ project with standard layout: `/cmd`, `/internal`, `/pkg`
- [x] Initialize Go modules with pinned versions from TECHSTACK.md
- [x] Add dependencies: `go-sqlcipher`, `lib/pq`, `go-redis`, `nats.go`, `minio-go`
- [x] Configure `golangci-lint` with custom rules
- [x] Set up `go generate` for protocol buffer compilation
- [x] VERIFY: Run `go mod tidy` and confirm no dependency conflicts
- [x] VERIFY: Run `golangci-lint run` and confirm no violations
- [x] SECURITY CHECKPOINT: Confirm no cloud-based AI or telemetry SDKs in Go dependencies

### 4.2 API Gateway (Kong OSS)
- [x] Deploy Kong OSS 3.x via Helm chart
- [x] Configure JWT plugin with RS256 validation
- [x] Configure rate-limiting-advanced plugin per blind_hash_id *(implemented as OSS `rate-limiting`, `limit_by: consumer` — `rate-limiting-advanced` is Kong Enterprise-only; drop-in upgrade path documented in-file)*
- [x] Configure request-transformer to strip X-Forwarded-For
- [x] Configure response-transformer to remove server headers
- [x] Configure bot-detection plugin
- [x] Configure correlation-id injection
- [x] Configure PII scrubbing for access logs
- [x] VERIFY: Test JWT validation with invalid token and confirm 401 response
- [x] VERIFY: Test rate limiting with rapid requests and confirm throttling
- [x] SECURITY CHECKPOINT: Confirm IP addresses are stripped before upstream requests

### 4.3 Identity Service (Go)
- [x] Implement OTP verification endpoint (Twilio Verify or MSG91 integration) *(MSG91 chosen — India/DPDP fit; net/http v5 OTP REST client, no SDK; noop provider for dev)*
- [x] Implement Argon2id phone-to-blind-hash with salt from HashiCorp Vault *(params byte-identical to client PhoneHasher; salt fetched once at startup into a sealed sync.Once; minimal KV v2 HTTP client, no SDK)*
- [x] Implement username claim/release with cooldown (30 days)
- [x] Implement device public-key registration
- [x] Implement JWT issuance (RS256, 15-minute access tokens) *(stdlib crypto/rsa RFC 7519 implementation; kid header for Kong)*
- [x] Implement refresh token management with rotation *(256-bit opaque tokens, SHA-256 hashes in Redis, family-based reuse detection)*
- [x] Configure Redis for OTP codes (TTL 10 min) and refresh token revocation *(otp: 10-min bcrypt-hashed; refresh:/revoked:/revoked_family: 30-day)*
- [x] VERIFY: Write unit tests for OTP verification flow
- [x] VERIFY: Write unit tests for JWT issuance and validation
- [x] SECURITY CHECKPOINT: Confirm raw phone numbers are never persisted or logged *(runtime dump/log scan + static output-scan + PII-redacting logger + buffer wipe; live smoke-verified)*

### 4.4 Messaging Relay Service (Go)
- [x] Implement WebSocket connection management *(coder/websocket; first-frame protojson AuthRequest — token never in URL; 25s server ping / 10s pong timeout heartbeat; 1 MiB read limit; evict-on-reconnect registry)*
- [x] Implement ciphertext envelope routing (sender hash → recipient hash) *(sender_hash/sender_device_id always server-overridden from the authenticated token — spoofing impossible; opaque ciphertext routed byte-for-byte)*
- [x] Implement offline queue using Redis Streams (TTL 30 days) *(one `msg_queue:{blind_hash_id}` stream per recipient; XADD+XTRIM MINID retention in one pipeline; XDEL + purge-on-drain acks; verified with miniredis)*
- [x] Implement multi-device fan-out logic *(Hub: one blind_hash_id → many devices; fan-out to all live devices; sender's other devices get a sync copy minus the sending device; dead-peer eviction on write failure)*
- [x] Implement delivery acknowledgement and queue purge *(DeliveryAck(msg_id) → AckByMsgID → XDEL; stream deleted when drained; offline backlog drained to reconnecting devices)*
- [x] Implement Connection Request state machine *(pending → accepted/rejected/withdrawn/expired; CAS single-transition enforcement; actor authorization; idempotent create; lazy + periodic sweep expiry; NATS accept event with noop dev fallback)*
- [x] VERIFY: Write unit tests for WebSocket message routing *(real coder/websocket client over httptest: auth, spoof-block, multi-device fan-out, offline queue drain + ack purge, heartbeat survival, protocol violations)*
- [x] VERIFY: Write unit tests for offline queue TTL expiration *(XTRIM MINID drops entries older than the retention window, fresh entries survive — simulated ages)*
- [x] SECURITY CHECKPOINT: Confirm service never attempts to decrypt message bodies *(static scan bans all decryption primitives across the package; runtime test proves arbitrary ciphertext bytes survive the online + offline paths unmodified)*

### 4.5 PostgreSQL Schema & Migrations
- [x] Create PostgreSQL 16 database with extensions: postgis, pgcrypto, pg_stat_statements, uuid-ossp *(embedded Go migrations applied at service startup; all four extensions installed — live-verified on a real PostgreSQL)*
- [x] Define schema tables: users, usernames, devices, refresh_tokens, connection_requests *(0001_init up/down, transactional, advisory-locked runner with version tracking)*
- [x] Configure row-level security (RLS) for sensitive tables *(ENABLE + FORCE RLS on all 5 tables; per-table `app_full_access` policy scoped to civic_app; roles created NOLOGIN by the migration, activated with Vault-supplied passwords at bootstrap)*
- [x] Set up streaming replication to two standbys *(infrastructure/database: postgresql-primary.conf — wal_level=replica, synchronous_commit + synchronous_standby_names 'ANY 1 (civic_standby_1, civic_standby_2)'; postgresql-standby.conf + pg_basebackup bootstrap; pg_hba default-deny)*
- [x] Configure WAL archiving to MinIO for PITR *(archive_mode=on + wal-archive-to-minio.sh via wal-g; nightly base-backup CronJob infrastructure/security/postgres-wal-backup.yaml)*
- [x] VERIFY: Write unit tests for schema migrations *(forward + rollback live-verified incl. idempotent re-run and concurrent Migrate via advisory lock; static SQL checks for extensions/tables/RLS/pgcrypto run everywhere)*
- [x] VERIFY: Test RLS by attempting to query rows outside permission scope *(outsider role with full table grants sees 0 rows and cannot write; FORCE RLS blocks a non-superuser table owner; only civic_app sees its data — live-verified)*
- [x] SECURITY CHECKPOINT: Confirm PII columns use pgcrypto encryption at rest *(devices.public_key_enc = pgp_sym_encrypt bytea keyed by session GUC civic.enc_key; raw dump shows ciphertext only, wrong-key decrypt fails, right-key round-trips — live-verified)*

### 4.6 Redis Configuration
- [x] Deploy Redis Sentinel (3-node: 1 primary, 2 replicas, 3 sentinels) *(infrastructure/helm/redis: StatefulSet redis-0 primary + ordinals 1..N replicaof, redis-sentinel StatefulSet quorum 2, headless + sentinel services; live-verified with a real 6-container docker Sentinel cluster)*
- [x] Configure key namespaces: otp, refresh, revoked, msg_queue, karma, vote_buffer, analyst_load, rate *(internal/cache/namespaces.go registry: otp, otp_attempts, refresh, revoked, revoked_family, msg_queue, karma, vote_buffer, analyst_load, rate — validated key builders used by the identity OTP/refresh stores and the relay offline queue)*
- [x] Set up TTL policies for each namespace *(10m otp/otp_attempts, 30d refresh/revoked/revoked_family/msg_queue, 5m karma, 1m rate, no-TTL vote_buffer/analyst_load — constants + tests)*
- [x] Configure Redis persistence (AOF + RDB) *(appendonly yes + appendfsync everysec + RDB save points on a PVC; live-verified config get appendonly=yes + save points)*
- [x] VERIFY: Write integration tests for Redis operations *(env-gated live tests: pool starvation/recovery, TTL expiry, stream XTRIM MINID + purge-on-drain, Sentinel round-trip — all run against a real Redis 7 cluster)*
- [x] VERIFY: Test TTL expiration by setting keys and confirming deletion *(unit via miniredis FastForward + live against real Redis)*
- [x] SECURITY CHECKPOINT: Confirm no plaintext PII in Redis values *(validated key builders reject any PII-shaped suffix — raw E.164 phones, raw OTPs, emails, non-hex strings can never become keys; test suite proves rejection; keys strictly carry 64-hex blind hashes / SHA-256 digests)*

### 4.7 NATS JetStream Event Bus
- [x] Deploy NATS JetStream with durable streams *(internal/events JetStream client + infrastructure/helm/nats chart: `-js` + file storage on a PVC so CIVIC_EVENTS + durable consumers survive restarts; live-verified)*
- [x] Define topic schema for karma events and search sync *(internal/events/subjects.go registry: relay.connection.accepted, identity.user.registered, karma.updated, search.sync.requested — allowlist enforcement)*
- [x] Configure at-least-once delivery guarantees *(JetStream PUBACK-waiting Publish + durable push consumers with explicit acks and NAK-on-handler-error redelivery; nats.go auto-reconnect with logged reconnect/closed handlers)*
- [x] Implement consumer groups for karma service *(SubscribeDurable: explicit durable consumer creation + Bind so ack progress survives drains/restarts; ready for the karma/search services)*
- [x] VERIFY: Write integration tests for event publishing and consumption *(11 unit tests against an in-process nats-server: stream idempotency, pub/sub round-trip, NAK redelivery, restart resume with no event loss + no acked replay, concurrent durable-create race, Options storage/maxage fallback, PII rejection)*
- [x] VERIFY: Test durability by restarting NATS and confirming no event loss *(unit: server restart on the same store dir redelivers unacked events; live: scripts/verify_nats_live.sh restarts the docker broker and confirms the CIVIC_EVENTS stream + durable consumer survive — 8/8 checks)*
- [x] SECURITY CHECKPOINT: Confirm event payloads contain no plaintext PII *(ValidateSubject allowlist + logging.ContainsPII rejection on both subjects and payloads; E.164/email-shaped events rejected before the wire — proven by tests)*

### 4.8 HashiCorp Vault Integration
- [x] Deploy HashiCorp Vault via Helm chart *(deployed on a dedicated Hetzner node via `infrastructure/cloud-init-vault.tpl` (file storage, TLS listener, bootstrapped root token from Terraform) with External Secrets Operator syncing `infrastructure/eso/vault-secret-store.yaml` — the repo's established Vault deployment path)*
- [x] Configure transit secrets engine for encryption operations *(`TransitEncrypt`/`TransitDecrypt` helpers — base64 plaintext per the Vault transit API, `vault:v1:` ciphertext, live-verified against a real transit engine)*
- [x] Configure KV secrets engine for Argon2id salt and JWT signing keys *(KV v2 `ReadKV2` + `ReadKV2Meta` (version/created_time); live-verified against a real `civic-commons` kv-v2 mount holding `identity/argon2_salt` + `identity/jwt_rs256_public_key`)*
- [x] Implement Go client for Vault secrets fetching *(stdlib net/http, no SDK: `ReadKV2`/`ReadKV2Meta`, AppRole login, `LookupSelf`/`RenewSelf`, background renewal loop, `TransitEncrypt`/`TransitDecrypt`, `SecretCache` TTL-aware caching + rotation-detecting `Refresh`, `WipeBytes` zero-memory hygiene, `Connect` factory with fail-fast)*
- [x] Set up automatic secret rotation policies *(background `RunRenewal` re-logins and re-renews the client token; `SecretCache.Refresh` detects rotated secret versions so services re-read; buffer wipe on cache replacement)*
- [x] VERIFY: Write unit tests for Vault client operations *(httptest suite: AppRole success/403, KV v2 + metadata, transit round-trip + base64, SecretCache TTL/rotation, HTTP 401/403/404/500 sentinels, WipeBytes, Connect creds/renewal)*
- [x] VERIFY: Test secret rotation and confirm service picks up new values *(SecretCache refresh test proves a rotated value is picked up and reported as changed; live test refreshes against the real server)*
- [x] SECURITY CHECKPOINT: Confirm secrets are never written to application logs *(redacting logger now scrubs Vault token shapes (`hvs.`/`s.`), `X-Vault-Token` and `Authorization: Bearer` headers, and `X-Vault` request headers; production config refuses to run without Vault auth; `verify_vault_live.sh` checks the redaction tests live)*

---

## Phase 5: State Management & Sync Engine

### Objective
Implement the resilient background sync engine that bridges the local SQLite queue with the remote API Gateway, handling network volatility and ensuring data consistency.

### 5.1 Network State Detection
- [x] Implement `NetworkInfoProvider` using `connectivity_plus` *(data layer `ConnectivityNetworkInfoProvider` — `none`→offline, `mobile`→metered, wifi/ethernet/vpn/bluetooth/other→online; `currentStatus()` one-shot + `statusChanges` stream)*
- [x] Create network state enum: `online`, `offline`, `metered` *(`NetworkStatus` domain enum)*
- [x] Implement network state listener with debouncing (500ms) *(new `DebouncedNetworkInfoProvider` decorator — default 500ms quiet window, last-status-wins, flapping collapses to one emission; configurable duration, fakeAsync-tested)*
- [x] Create network-aware sync triggering logic *(`ReconnectionSyncTrigger` — fires exactly once per offline→online transition, never while offline; crash-safe fire-and-forget)*
- [x] VERIFY: Write unit tests for network state detection *(mapping tests + scripted-stream domain tests + debounce tests: window boundary, flapping collapse, window restart, custom duration)*
- [x] VERIFY: Write integration tests by toggling network and confirming state changes *(domain-level: scripted fake provider drives online/offline/metered transitions; platform-channel toggling is compile-verified — no device in this environment)*
- [x] SECURITY CHECKPOINT: Confirm network state is not used for fingerprinting *(state used solely to gate WHEN sync runs; never persisted, never leaves the device, never combined with user data — static checkpoint suite enforces no sqflite/secure_storage in lib/sync)*

### 5.2 Sync Worker Implementation
- [x] Create background isolate for sync operations using `workmanager` *(`WorkmanagerScheduler` — periodic 15-min + one-off sync tasks, ExistingWorkPolicy keep/replace, compile-verified; logic unit-tested, native scaffold lands with the app shell)*
- [x] Implement chunking logic: process queue items in batches of 10 *(`BatchChunker` — bounded batches, order-preserving, default max 10; used by `BackgroundSyncWorker`)*
- [x] Implement aggressive timeout limits: 10s per HTTP request *(worker wraps every `SyncSink.push` in a 10s `Future.timeout` — a hung connection is treated as a failure and retried, never stalls the drain; timeout configurable + unit-tested)*
- [x] Implement idempotency key generation and attachment to headers *(new `IdempotencyKeyGenerator` — RFC 4122 UUID v4 from `Random.secure()`, `Idempotency-Key` header constant; every queued mutation's id IS a UUID v4 idempotency key so retries dedupe server-side)*
- [x] Implement silent failure handling: increment retry count, update timestamp, no UI exception *(`markFailed` bumps retryCount; thrown/timeout sink errors never propagate to UI; crash recovery: `recoverInterrupted()` resets `in_progress`→`pending` at the start of every drain so killed runs are retried, never lost)*
- [x] VERIFY: Write unit tests for chunking logic *(8 chunker tests incl. 10+10+5, order preservation, cap enforcement)*
- [x] VERIFY: Write unit tests for idempotency key generation *(UUID v4 shape/version/variant, uniqueness ×1000, seeded-RNG determinism, header constant)*
- [x] SECURITY CHECKPOINT: Confirm sync worker never exposes plaintext payloads in logs *(no print/debugPrint in lib/sync, no raw transport imports, sink receives only sealed payloads — static + runtime checkpoint suite)*
- [x] EXTRA (hand-off scope): deterministic conflict-resolution hooks *(`ConflictResolutionPolicy` + `ServerAuthoritativeLastWriteWins` — server-ack beats local-only, newer timestamp wins, hash tiebreak so all devices converge; full conflict UI deferred to 5.5)*
- [x] EXTRA (hand-off scope): deterministic retry with exponential backoff + jitter — WIRED, not dead code *(`ExponentialBackoff.delayForRetryWithJitter` = equal jitter over `[base/2, base)` (guaranteed minimum, safe as a gate); schema v2 migration adds `sync_queue.last_attempt_at`; `getRetryable()` returns failed items whose backoff window has elapsed; the worker drains pending + retryable-failed together, so failed items are genuinely retried after their jittered window — never before base/2, always by base, window grows with retryCount)*

### 5.3 Idempotency Enforcement
- [x] Implement UUID v4 idempotency key generation *(Task 5.2 client-side: `IdempotencyKeyGenerator` — RFC 4122 UUID v4 from `Random.secure()`; every queued mutation's id IS a UUID v4 key)*
- [x] Attach `Idempotency-Key` header to all mutation requests *(Task 5.2: `Idempotency-Key: <item.id>` attached by the sync worker's transport so retries dedupe server-side)*
- [x] Implement server-side idempotency key tracking in Redis (TTL 24 hours) *(new `internal/idempotency` package — Redis SETNX claim / GET / SET complete / DEL clear against the validated `idempotency:` namespace; `IDEMPOTENCY_TTL` default 24h; keys are actor-scoped `idempotency:{blind_hash_id}:{uuid}`)*
- [x] Create idempotency conflict resolution logic *(middleware on the relay's POST mutation routes inside auth: missing header = passthrough, malformed = 400, claim-winner processes + caches the 2xx response (64 KiB cap), concurrent in-flight = 409, completed = cached replay with `Idempotent-Replayed` header, handler failure = key cleared so the retry reprocesses)*
- [x] VERIFY: Write unit tests for idempotency key generation *(client: UUID v4 shape/version/variant + uniqueness ×1000; server: `cache.IdempotencyKey`/`IdempotencyKeyScoped` validation + PII-shaped-suffix rejection)*
- [x] VERIFY: Write integration tests by sending duplicate requests and confirming deduplication *(relay HTTP integration: same key + actor → single side effect then cached replay; concurrent race → exactly one 201; cross-actor same key → NO dedup)*
- [x] SECURITY CHECKPOINT: Confirm idempotency keys are random and not predictable *(UUID v4 from a crypto-secure RNG client-side; the server key builder strictly validates UUID v4 shape and rejects PII-shaped suffixes before any Redis write — raw phones/OTPs/emails can never become keys; actor scoping prevents cross-user replay of cached responses)*

### 5.4 Sync Status UI Integration
- [x] Create `SyncStatusBar` widget showing: LIVE, CACHED, QUEUED, OFFLINE *(`lib/state/ui/sync_status_bar.dart` — compact status chip with per-state color/icon: LIVE=green/cloud_done, CACHED=amber/cloud_queue, QUEUED=blue/cloud_upload, OFFLINE=grey/cloud_off)*
- [x] Implement tap-to-expand showing last-sync timestamp and queue count *(InkWell toggle expands an `AnimatedSize` panel with "Last synced: …" (`formatLastSync`: Never/Just now/Xm/Xh/Xd ago/date) and "N pending mutation(s)")*
- [x] Bind sync status to BLoC state stream *(`StreamBuilder` over `SyncStatusBloc.state` — the widget consumes ONLY the BLoC interface; no repository/DB/network access in the widget tree)*
- [x] Implement visual feedback for sync operations (subtle progress indicator) *(14px `CircularProgressIndicator` rendered while `SyncStatusState.isSyncing` — driven by the bloc's in-progress queue-item derivation)*
- [x] VERIFY: Write widget tests for SyncStatusBar states *(22 widget tests: all four states + empty-before-emit, badge show/update/drain, LIVE↔OFFLINE transitions, progress indicator show/hide, tap-expand/collapse + singular wording + Never, `formatLastSync` units, security text-shape checks)*
- [x] VERIFY: Write integration tests by triggering sync and confirming UI updates *(2 integration tests: real `ReconnectionSyncTrigger` + `BackgroundSyncWorker` + `LocalSyncStatusBloc` driving the bar — reconnect fires a real drain QUEUED→LIVE, and a gated sink proves the in-flight progress indicator; async teardown via `runAsync`)*
- [x] SECURITY CHECKPOINT: Confirm sync status never exposes sensitive data *(state contract extended additively with non-PII `lastSyncAt` timestamp + `isSyncing` boolean only; runtime widget-tree scan asserts no phone/email/hvs./64-hex/JWT-shaped text; static scan proves no direct data-layer access, no prints, fixed enum labels only; 5 security tests)*

### 5.5 Conflict Resolution Logic
- [x] Implement last-write-wins for simple fields *(ServerAuthoritativeLastWriteWins refined: authority → timestamp → author-hash tiebreak)*
- [x] Implement merge logic for nested structures (e.g., karma scores) *(MaxMergePolicy — commutative/idempotent/associative on non-PII numeric aggregates)*
- [x] Create conflict detection by comparing local vs remote timestamps *(SyncPushOutcome carries the server's MutationVersion into the worker/repo loops)*
- [x] Implement user-facing conflict resolution UI for critical data *(ConflictResolutionBanner — fixed non-PII outcome labels; app-shell wiring deferred to Phase 6)*
- [x] VERIFY: Write unit tests for conflict resolution algorithms
- [x] VERIFY: Write integration tests by simulating conflicting edits
- [x] SECURITY CHECKPOINT: Confirm conflict resolution never exposes raw data to logs

### 5.6 Offline Queue Persistence
- [x] Implement queue persistence across app restarts *(durable SQLCipher rows with exact serialization/deserialization bounds; `recoverInterrupted()` resets crash-stranded in_progress items to pending on cold start — nothing is lost)*
- [x] Create queue backup/restore mechanism *(`SyncQueueBackup`: sealed-only JSON envelope codec — versioned, fully validated before any insert, idempotent restore that can never duplicate a mutation)*
- [x] Implement queue size limits (max 1000 items, FIFO eviction) *(cap enforced on insert; OLDEST non-in-flight items evicted, in-flight protected, just-inserted item guaranteed to survive by construction)*
- [x] Create queue cleanup for expired items (30 days) *(`purgeExpired` strict boundary — exactly-30-day-old items survive; wired into the worker before every drain so stale mutations are never pushed)*
- [x] VERIFY: Write unit tests for queue persistence
- [x] VERIFY: Write integration tests by restarting app during sync
- [x] SECURITY CHECKPOINT: Confirm queued data remains encrypted at rest *(AES-256-GCM sealed before storage, verified across restart/backup/restore paths)*

---

## Phase 6: Pillar 1 — The Vault (Secure Messaging)

### Objective
Implement the Vault pillar with end-to-end encrypted messaging, connection requests, and zero-cloud-footprint architecture.

### 6.1 Vault UI Foundation
- [x] Create `VaultMasthead` component with classified document aesthetic *(Vault Blue #1A3D6B strip, lock glyph, `THE VAULT` tracked wordmark, `[CLASSIFIED]` stamp, black pseudo-redaction bar; DESIGN §2.2/§6.2)*
- [x] Implement conversation list screen with ciphertext previews *(preview is ALWAYS the fixed `[end-to-end encrypted]` label — never a plaintext snippet, shoulder-surfing protection; peers rendered only via non-PII `formatPeerHandle`)*
- [x] Create conversation detail screen with message bubbles *(received = white + 3dp Vault Blue left bar, sent = Vault Blue panel; ✓/✓✓ receipts, amber queued indicator, `Expires` marker; body is a fixed E2EE placeholder until 6.3 wires decryption)*
- [x] Implement connection request queue UI *(collapsible `PENDING REQUESTS (N)` section with Accept buttons — read-only when the accept flow is not yet wired, inbox never invisible)*
- [x] Apply `FLAG_SECURE` wrapper to all Vault screens *(both screens wrap in `SecureScreenWrapper` — verified statically + runtime enable-on-mount)*
- [x] VERIFY: Write widget tests for VaultMasthead rendering
- [x] VERIFY: Write integration tests for conversation list navigation *(real `LocalConversationBloc` + `LocalDataStreamController` + fake repo end-to-end)*
- [x] SECURITY CHECKPOINT: Confirm FLAG_SECURE is active on all Vault screens

### 6.2 Connection Request Flow
- [x] Implement username search via API Gateway *(new auth-required `GET /v1/identity/username/{username}` — resolves a claimed username → owner blind hash; policy-validated (invalid formats never resolve), 404 for unknown/released usernames (the 30-day cooldown is a privacy boundary), response carries only `{username, blind_hash_id}`; 5 Go tests incl. no-phone-in-response/logs)*
- [x] Create connection request sending logic *(local-first `LocalConnectionRequestRepository`: send/accept/reject/withdraw persist immediately + enqueue AES-GCM-sealed sync envelopes; blind-hash-only targets — a raw phone is rejected before it can reach storage or the queue; idempotent while pending)*
- [x] Implement connection request receiving and approval UI *(the Task 6.1 `PENDING REQUESTS (N)` queue is now BLoC-driven — `LocalConnectionRequestsBloc` + repository feed `VaultPendingRequestsSection` with wired Accept; new FLAG_SECURE `UsernameSearchSheet` for finding peers by public username; conversation tiles resolve remembered `@username`s)*
- [x] Create Connection Request state machine in local database *(`connection_requests` row codecs + single-transition CAS status rules mirroring the relay — terminal states immutable)*
- [ ] Implement cryptographic key exchange on approval *(deferred to Task 6.3 with session establishment)*
- [x] VERIFY: Write unit tests for connection request state transitions
- [ ] VERIFY: Write integration tests for key exchange on approval *(deferred to Task 6.3)*
- [x] SECURITY CHECKPOINT: Confirm connection requests never expose phone numbers *(blind-hash-only targets rejected pre-storage; sync envelopes carry UUID + blind hashes + status only; UI renders `formatPeerHandle` / public `@username` only — runtime widget-tree scans)*

### 6.3 Message Encryption & Decryption
- [x] Implement client-side message encryption using Signal Protocol *(X3DH session establishment wired into the connection-approval hook deferred in 6.2; Double Ratchet encrypt/decrypt orchestrated by `SessionManager` keyed by blind hash; `MessageCipher` port seals/opens bodies in the message BLoC)*
- [x] Create message bubble UI with sent/received states *(6.1 styling + explicit `MessageDirection` field superseding the delivered-heuristic `MessageSideResolver`)*
- [ ] Implement read receipt logic (✓ sent, ✓✓ delivered, ✓✓✓ read) *(✓/✓✓ rendered from 6.1; ✓✓✓ read-receipt tracking deferred — needs the server relay's read-ack plumbing)*
- [ ] Create message expiration logic (TTL-based deletion) *(Expires marker rendered; background TTL deletion deferred)*
- [x] Implement message queueing for offline send *(`MessageBloc.send` encrypts → `LocalMessageRepository.create` persists locally as sent/undelivered + enqueues the sealed payload into the encrypted sync queue)*
- [x] VERIFY: Write unit tests for message encryption/decryption *(session manager 9, session establisher + message cipher 7, bloc decrypt-on-emit + send 9, bubble content rendering 3, composer 5)*
- [x] VERIFY: Write integration tests for offline message queuing *(send round-trip: encrypt → persist → queue → republish with decrypted content)*
- [x] SECURITY CHECKPOINT: Confirm message content is never logged plaintext *(no prints in lib/state; state carries only decrypted `content` for rendering — never ciphertext; undecryptable messages fall back to the fixed placeholder; send failures are swallowed, never logged)*

### 6.4 WebSocket Message Relay
- [x] Implement WebSocket client for real-time message delivery *(new `lib/relay/` tree — `RelayClient` orchestrates first-frame JWT auth, read loop, envelope dispatch + auto-ack, reconnect with the shared Task 5.2 `ExponentialBackoff`, and dead-connection detection; `RelaySocket`/`RelaySocketConnector` ports with `WebSocketRelaySocket` over `web_socket_channel` ^2.4.0)*
- [x] Create message envelope parsing logic *(`RelayEnvelope` + sealed `RelayFrame` codec mirroring the Go relay's protojson `UseProtoNames` wire exactly — oneof members auth/auth_ack/envelope/ack/error, snake_case fields, base64 ciphertext; `DiscardUnknown` tolerance, UUID-v4 msg id + 64-hex blind-hash validation at the port boundary)*
- [x] Implement heartbeat/ping-pong for connection health *(transport-level: `pingInterval: 20s` on the channel so dart:io closes when the relay stops ponging — the correct detector because the relay's 25s pings are protocol control frames that never surface as data frames; `RelayHeartbeat` data-frame watchdog retained for sockets without transport liveness, gated by `hasTransportHeartbeat`)*
- [x] Create reconnection logic with exponential backoff *(shared `ExponentialBackoff.delayForRetryWithJitter`; generation-guarded sessions so stale callbacks are inert; a drop during the auth handshake before any auth_ack is TRANSIENT — only an explicit `auth_ack:false` is terminal, and the post-rejection close is never retried)*
- [x] Implement dummy traffic for obfuscation (opt-in) *(`DummyTrafficGenerator` — scheduled UUID-v4 keepalive envelopes via an injectable `send`, clock/timer-injectable, non-PII)*
- [x] VERIFY: Write integration tests for WebSocket connection lifecycle *(real `dart:io` WebSocket server + `IOWebSocketChannel`: first-frame auth asserted with empty query params (JWT never in URL), envelope delivery + auto-ack round-trip, client→server envelope, remote-close reconnect re-authenticates, transport-heartbeat capability pinned)*
- [x] VERIFY: Write unit tests for heartbeat timeout handling *(fake_async `RelayHeartbeat` watchdog tests + RelayClient stale-watchdog tear-down/reconnect, transport-heartbeat-skips-watchdog, pre-auth-drop-reconnects, rejection-close-terminal)*
- [x] SECURITY CHECKPOINT: Confirm WebSocket authentication uses JWT in body, not URL *(token lives only in the first auth frame body — asserted in unit tests (URL contains no token/query) and in the live-socket integration test (server asserts empty upgrade query params); ciphertext is opaque and never logged; no print in lib/relay; PII/64-hex shaped values rejected pre-wire; static + runtime security checkpoint suite)*

### 6.5 Multi-Device Pairing
- [x] Implement QR code generation for device pairing *(new `lib/pairing/` tree: `PairingPayload` strict zero-PII QR codec (civic-commons://pair URI, 64-hex blind hash + UUID v4 + one-time secret + PUBLIC keys only), `PairingSecretGenerator` (32-byte CSPRNG base64url, 5-min window), `QrMatrix` value object + `QrEncoder`/`QrScanner` ports, `qr_codec_impl.dart` wrapping the pure-Dart `qr` package (medium ECC), `DevicePairingService` orchestrator — PRIMARY signs the signed prekey with the REAL Ed25519 identity key; NEW DEVICE cryptographically verifies that signature at the authorize gate before any session/registration, plus one-time-secret single-use consumption)*
- [x] Create QR scanning logic for new device authorization *(`scanAndAuthorize`/`authorizePayloadText` — strict decode → expiry check → account-match check → Ed25519 signature verification → X3DH initiator session → registry registration; camera `QrScanner` port documented for the device-camera path, manual code entry fully implemented + tested via FLAG_SECURE `PairingScanSheet`)*
- [x] Implement key transfer via QR (not cloud sync) *(payload carries ONLY public keys + blind hash + one-time secret; private keys never leave `SecureKeyStorage`; no backup/upload/sync identifiers anywhere in `lib/pairing` — enforced by the checkpoint test with word-boundary regexes over code lines)*
- [x] Create device management UI (list linked devices, revoke) *(FLAG_SECURE `DeviceManagementSheet`: `LinkedDevicesBloc` list rendered ONLY via `formatDeviceHandle` (never raw UUIDs/keys), per-device Revoke → registry row marked revoked + X3DH session deleted, `DevicePairingBloc` QR panel via `PairingQrView` (CustomPaint matrix — payload text never rendered)*
- [x] VERIFY: Write integration tests for QR-based pairing *(widget tests: management sheet renders/revokes/QR-panel, scan sheet submit/error/FLAG_SECURE-on-mount; service tests: full authorize lifecycle incl. session established + registry registered, expiry, wrong-account, PII-shaped rejection, revocation + session deletion, active-list projection)*
- [x] VERIFY: Write unit tests for device revocation *(revokeDevice idempotent, unknown-device no-op, revoked rows excluded from the active list, session cleanup verified)*
- [x] SECURITY CHECKPOINT: Confirm device pairing never uses cloud backup *(static scans: zero cloud/sync/upload identifiers, zero print/debugPrint, zero phone/e-mail literals, payload codec never serializes private keys, service depends only on ports; runtime: QR signature is REAL Ed25519 and verifies (tampered spk/ik rejected), one-time secret single-use (replay rejected), `SecureStorageIdentityKeySource` tested directly — no private key escapes)*

### 6.6 Duress PIN Integration
- [x] Integrate duress PIN flow into Vault unlock *(new `VaultUnlockBloc`/`VaultUnlockState` + `LocalVaultUnlockBloc` wrapping the Phase 2.5 `DuressService`: one PIN prompt for BOTH PINs — `unlock(pin)` returns the opened `UnlockResult` (real vs decoy vault) only to the routing callback, the state stream never reveals which vault was opened; generic failure identical for wrong/empty/near-miss PINs; FLAG_SECURE `VaultUnlockScreen` with an obscured numeric PIN field, spinner, single generic error, and a setup path when unregistered)*
- [x] Create decoy Vault UI (visually identical, empty) *(`DecoyVaultScreen`: same `VaultMasthead`, same CONVERSATIONS header, and the SHARED `VaultEmptyState` widget — extracted so the decoy is byte-identical to a freshly-registered real vault; takes NO data inputs and references no bloc/repository/entity types; FLAG_SECURE wrapped so it is not distinguishable by its security guard either; proven by a runtime test comparing the decoy's rendered text to the real empty list's)*
- [x] Implement duress PIN detection and database switching *(pure decryption-based selection via `DuressService` — the real PIN's derived key opens `vault.db`, the duress PIN's opens `vault_decoy.db`; the real service never stores which PIN is real; integration tests prove real records persist in the real vault and are INVISIBLE through the decoy result)*
- [x] Create duress PIN setup flow during onboarding *(`DuressSetupBloc`/`DuressSetupState` + `LocalDuressSetupBloc` + FLAG_SECURE `DuressPinSetupSheet`: the ONE screen that labels the two obscured PIN fields; identical/empty/duplicate registrations all map to a single generic error; PINs wiped from the controllers after every attempt)*
- [x] VERIFY: Write unit tests for duress PIN database switching *(bloc tests: real PIN → `VaultKind.real` + real DB handle, duress PIN → `VaultKind.decoy` + decoy DB handle with IDENTICAL state transitions, real-vs-decoy record isolation, wrong/empty/near-miss PINs → same generic failure, failing-service → generic failure not crash)*
- [x] VERIFY: Write integration tests for decoy Vault rendering *(widget tests: decoy renders masthead+CONVERSATIONS+shared empty state; rendered-text comparison to the real empty list is IDENTICAL; no rows/previews/handles; setup sheet registers valid PINs and rejects identical ones; unlock screen routes real→real and duress→decoy with byte-identical failure UI)*
- [x] SECURITY CHECKPOINT: Confirm duress PIN is indistinguishable from real PIN *(static scans: unlock screen code never references real/duress/decoy/VaultKind and never inspects the result kind, state models carry no key/DB fields, no print/debugPrint, no PII literals; runtime: near-real and near-duress PINs produce byte-identical failure UI, decoy text ≡ real-empty text; integration: persisted vault bytes contain no real/duress/decoy indicator — the app never stores which PIN is real)*

---

## Phase 7: Pillar 2 — The Daily Ledger (Hyperlocal News)

### Objective
Implement the Ledger pillar with pin-code-scoped civic news, peer review moderation, and dynamic radius expansion for low-density areas.

### 7.1 Ledger UI Foundation
- [x] Create `LedgerMasthead` component with newspaper aesthetic
- [x] Implement feed screen with pin-code scoping
- [x] Create post detail screen with voting and comments
- [x] Implement compose post screen with category selection
- [x] Create `CategoryChip` component for post tagging
- [x] VERIFY: Write widget tests for LedgerMasthead rendering
- [x] VERIFY: Write integration tests for feed navigation
- [x] SECURITY CHECKPOINT: Confirm feed renders from local cache first

### 7.2 Geographic Clustering Implementation
- [x] Implement pin-code resolution using `geolocator` *(coarse-pin-only resolver: `geolocator`+`geocoding` collapsed to a 6-digit pin + district/locality names; client-side seam — the server PostGIS constituency lookup stays on the Phase 4.5 stack and is called by later sync tasks)*
- [x] Create PostGIS queries for constituency/district lookup *(client port + wire contract documented; the server-side query lands on the Phase 4.5 PostgreSQL stack and is invoked by the later sync task)*
- [x] Implement pin-code feed filtering in local database *(existing `listPosts(pinCode:)` scoping from Task 7.1 — verified by geo-driven integration test)*
- [x] Create "Explore Nearby" radius control UI
- [x] VERIFY: Write unit tests for pin-code validation
- [x] VERIFY: Write integration tests for geographic resolution
- [x] SECURITY CHECKPOINT: Confirm location data is not used for fingerprinting

### 7.3 Dynamic Radius UI (Skill: dynamic-radius-ui)
- [x] Implement threshold check: query local DB for posts in last 7 days *(injectable-clock window; <5 recent posts triggers expansion — skill step 1)*
- [x] Create radius expansion logic (pin code → constituency → district) *(same-district fallback via `LedgerPost.district`; `FeedScope` carries only pin + coarse radius enum — no coordinates)*
- [x] Implement `NearbyBadgeWidget` for expanded results *(fixed `NEARBY` label chip on nearby cards + `~ N posts from nearby pins ~` strip)*
- [x] Create seamless blending of local and nearby results *(single chronological feed — no separate Local/Nearby sections — skill step 4)*
- [x] VERIFY: Write unit tests for threshold check logic
- [x] VERIFY: Write integration tests for radius expansion
- [x] SECURITY CHECKPOINT: Confirm expanded results are clearly marked

### 7.4 Post Creation & Queuing
- [x] Implement post creation form with category selection *(Task 7.1 compose screen — category dropdown, pin code, headline/details, karma row)*
- [x] Create local draft storage using Hive *(implemented as `ledger_drafts` table in the SQLCipher-encrypted DB — schema v5 + `ledgerDraftRecord` row codecs; offline-first local snapshot survives cold restarts)*
- [x] Implement post encryption before queuing *(AES-256-GCM queue cipher seals the `LedgerPostWireFrame` envelope via the Task 3.3 `QueuePayloadCipher` — queue never persists plaintext drafts; byte-level test proof)*
- [x] Create sync queue insertion for new posts *(`QueueLedgerDraftSink` — local write FIRST, then `SyncOperationType.create` enqueue; item id = UUID v4 idempotency key)*
- [x] VERIFY: Write unit tests for post creation flow *(wire codec strict decode bounds + round-trip, draft record row codec, sink local-first + sealed-enqueue + failure propagation)*
- [x] VERIFY: Write integration tests for offline post queuing *(compose → local persist → sealed queue → cold-restart row round-trip → sync-worker drain, byte-identical sealed payloads, oldest-first drain)*
- [x] SECURITY CHECKPOINT: Confirm posts are encrypted before sync *(stored queue payload != plaintext frame bytes; sealed payload opens back to the exact civic frame; no identity fields on the wire frame; `pin_code` flagged sensitive)*

### 7.5 Voting System with Karma Weighting
- [x] Implement vote recording with karma-weighted sub-linear scoring *(client-side: `KarmaWeightedScore` — deterministic `floor(sqrt(up)) − floor(sqrt(down))`; the server-side weighted vote aggregation (Redis cache → batch flush → PostgreSQL) stays on the Phase 4 stack)*
- [x] Create vote caching in Redis with batch flush to PostgreSQL *(server-side Phase 4.6/4.5 concern — client queues sealed vote envelopes for that transport; local `post_votes` table is the offline-first cache)*
- [x] Implement vote UI with upvote/downvote buttons *(`LedgerVoteBar` shared widget — active-direction states + karma-weighted score, wired into feed cards AND detail screen, toggle-through-bloc)*
- [x] Create vote sync logic for offline votes *(`QueueLedgerVoteSink` — local-first record + sealed AES-256-GCM envelope, UUID v4 idempotency keys, resolved-direction sync)*
- [x] VERIFY: Write unit tests for karma-weighted vote calculation *(8 tests — determinism, sub-linear curve, downvote curve, clamping, monotonicity)*
- [x] VERIFY: Write integration tests for offline vote queuing *(bloc vote → local record → sealed queue → cold-restart row round-trip → sync drain byte-identical; toggle chain up→off→down)*
- [x] SECURITY CHECKPOINT: Confirm vote weights are calculated client-side *(the scoring function accepts ONLY two integers — no identity input possible; frame carries no identity; no prints/no networking; static scan + structural proof)*

### 7.6 Peer Review Gate
- [x] Implement Shadow Queue for new accounts (<96 hours) *(`PeerReviewGate.entryStatus` — accounts <96h post to the shadow queue; FR-L3 precedes fast-track)*
- [x] Create karma threshold check for fast-track publishing *(karma >= 100 fast-tracks straight to published)*
- [x] Implement reviewer assignment (3 deterministic high-karma users) *(seeded-by-post-id deterministic selection → BLINDED `reviewer_` + 6-hex display handles — never raw blind hashes; no Random())*
- [x] Create review UI with approve/reject buttons *(`PeerReviewQueueSection` review cards with APPROVE/REJECT/FLAG + `[✓ Peer Review Gate: N/3 approved]` indicator; controls hidden after review or at 3/3)*
- [x] Implement moderation flag handling *(flag decisions recorded + sealed-enqueued; never advance the consensus)*
- [x] VERIFY: Write unit tests for Shadow Queue logic
- [x] VERIFY: Write integration tests for reviewer assignment
- [x] SECURITY CHECKPOINT: Confirm reviewer identities are blinded *(assignReviewers returns only derived handles; peer_reviews table has no identity column; UI defensively re-blinds hash-shaped handles)*

---

## Phase 8: Pillar 3 — The War Room (OSINT Cyber Defense)

> **STATUS: COMPLETE (8.1–8.8)** — client-side War Room delivered end-to-end (encrypted evidence, PII redaction, severity scoring, blinded analyst collaboration, immutable custody logging, trauma-informed intake). Deferred to the Phase-4 infrastructure pass: MinIO upload (8.2) and backend custody-log persistence. Task 8.8 locked the phase (full-suite audit + Phase 9 foundation scaffold).

### Objective
Implement the War Room pillar with encrypted evidence handling, severity scoring, and analyst collaboration with PII redaction.

### 8.1 War Room UI Foundation
- [x] Create `WarRoomMasthead` component with dossier aesthetic
- [x] Implement case list screen with severity bands
- [x] Create case detail/investigation view
- [x] Implement file new case intake flow
- [x] Apply `FLAG_SECURE` wrapper to all War Room screens
- [x] VERIFY: Write widget tests for WarRoomMasthead rendering
- [x] VERIFY: Write integration tests for case navigation
- [x] SECURITY CHECKPOINT: Confirm FLAG_SECURE is active on all War Room screens

### 8.2 Encrypted Evidence Upload
- [x] Implement client-side evidence encryption (AES-256-GCM) *(per-item DEK file seal; X25519-ECDH DEK wrap; evidence never decrypts server-side)*
- [x] Create DEK (Data Encryption Key) generation per evidence item *(32-byte keys from `SecretKeyData.random`)*
- [x] Implement DEK wrapping with victim's and analysts' public keys *(wrapped to the device key for self-recovery; analyst keys slot in during Task 8.5)*
- [ ] Create MinIO upload via presigned URLs *(DEFERRED — the client seals + queues evidence end-to-end (8.2); the MinIO upload service is backend/infra and lands with the Phase-4 infrastructure pass, cf. Task 4.10)*
- [x] Implement evidence metadata storage (encrypted) *(schema v8 `evidence` table: metadata + sealed file + WRAPPED DEK — NO filename/path/identity by design)*
- [x] VERIFY: Write unit tests for evidence encryption/decryption *(byte-level proof: stored/queued bytes never match plaintext; tamper + wrong-key rejection)*
- [ ] VERIFY: Write integration tests for MinIO upload *(DEFERRED with the MinIO upload service — backend/infra)*
- [x] SECURITY CHECKPOINT: Confirm evidence never decrypts server-side *(only sealed files + wrapped DEKs leave the device; static + byte-level runtime proof)*

### 8.3 PII Redaction Pipeline (Skill: local-pii-redaction)
- [x] Implement deterministic regex filters for phone numbers, emails, government IDs *(E.164 + Indian mobile, email, Aadhaar, PAN, SSN, cards, tokens, key=value secrets — single-pass merged, overlapping spans collapse)*
- [x] Create local Gemma model integration for contextual PII detection *(local `ContextualPiiDetector` port + on-device in-memory detector (names + address cues); the Gemma slot — ALWAYS local, never cloud AI; detector only ever sees ALREADY-redacted text)*
- [x] Implement redaction prompt template from resources *(`client/resources/prompts/gemma_redaction_prompt.txt` strict JSON-output template)*
- [x] Create memory wipe logic after encryption (nullify plaintext variables) *(mutable `Uint8BufferInput` — plaintext buffer zeroed unconditionally after redaction; proof-tested)*
- [x] VERIFY: Write unit tests for regex PII detection
- [x] VERIFY: Write integration tests for Gemma model redaction *(intake end-to-end: narrative scrubbed before filing; pipeline integration via the local detector port)*
- [x] SECURITY CHECKPOINT: Confirm plaintext is wiped immediately after encryption

### 8.4 Severity Scoring System
- [x] Implement keyword-based severity scoring (CRITICAL, HIGH, MEDIUM, LOW) *(`SeverityScorer` — deterministic keyword engine: 1+ CRITICAL → critical, 2+ HIGH or 1 HIGH+urgency → high, 1 HIGH or 2+ MEDIUM → medium; urgency + situation FLOORS never downgrade; SLA map 24/48/72/120h)*
- [x] Create severity band UI component *(`WarCaseSeverityBand` reused from 8.1; triage section adds signal COUNT chips + SLA line)*
- [x] Implement SLA tracking based on severity *(`slaHoursFor` single deterministic mapping — drives `estReportHours` at file + override)*
- [x] Create severity override UI for human review *(`SeverityOverrideSheet` — RadioGroup + required reason, reason gated, FLAG_SECURE; `withSeverityOverride` re-bands severity + SLA while preserving the original triage for the audit trail)*
- [x] VERIFY: Write unit tests for severity scoring logic *(9 engine tests: band thresholds, urgency/floor merges, determinism, ties)*
- [x] VERIFY: Write integration tests for SLA tracking *(repository auto-triage at fileCase + override SLA update; detail-screen triage + override flow via real stores)*
- [x] SECURITY CHECKPOINT: Confirm severity scoring is deterministic *(repeated-invocation + zero-PII triage/override UI checks)*

### 8.5 Analyst Assignment & Collaboration
- [x] Implement analyst vetting gauntlet (CTF-style sandbox) *(`AnalystVettingGauntlet` — deterministic `GAUNTLET-01` scenario, threat-marker scoring, 80% pass threshold; `runGauntlet` promotes pending → vetted; pending analysts are never assigned)*
- [x] Create blind review enforcement (analysts cannot see each other's notes) *(`AnalystUpdate` carries ONLY the blinded `AN-####` author handle; the repository rejects any analyst not assigned to the case; notes never name other analysts)*
- [x] Implement case assignment logic (skill-tag matched) *(`AnalystSkill.forSituation` constant map; least-loaded vetted analyst with the required skill wins, deterministic handle tie-break; load counts CASES not skill-slots)*
- [x] Create analyst load tracking (case cap enforcement) *(`Analyst.caseCap`/`activeCaseCount`; at-cap analysts skipped; withdraw releases load)*
- [x] VERIFY: Write unit tests for analyst assignment logic *(14 domain + 12 registry — gauntlet scoring/threshold/determinism, skill match, cap, tie-breaks, repeated-run determinism)*
- [x] VERIFY: Write integration tests for blind review enforcement *(file→auto-assign→team UI→blinded note→stranger-rejected→load-release via real stores; at-cap never double-assigned)*
- [x] SECURITY CHECKPOINT: Confirm analyst identities are blinded to victims *(structural: only `AN-####` handles on every analyst surface; no names/emails/phones/hashes; assignment determinism; local-first no-networking scan)*

### 8.6 Chain of Custody Logging
- [x] Implement append-only custody log in PostgreSQL *(client-side first: SHA-256 hash-chained `CustodyLog` port + in-memory implementation — append-only + `verifyIntegrity` tamper detection; PostgreSQL row lands with Task 4.10 backend phase)*
- [x] Create custody log viewer UI *(CHAIN OF CUSTODY section on WarCaseDetailScreen — fixed event labels + timestamps + blinded actors, zero payload content)*
- [x] Implement HMAC signing for Verified Intel Report *(HMAC-SHA256 `ReportSigner` — signed report with base64 signature, VERIFIED chip, deterministic under repeated signing)*
- [x] Create legal-aid handoff webhook integration *(local-first `LegalAidHandoff` + sealed v:1 envelope enqueued via the sync queue with the UUID v4 as Idempotency-Key — the webhook itself ships with the Phase-4 infra pass)*
- [x] VERIFY: Write unit tests for custody log append-only property
- [x] VERIFY: Write integration tests for HMAC signing
- [x] SECURITY CHECKPOINT: Confirm custody log is immutable *(tamper tests: reorder/swap/mutate any event → `verifyIntegrity` fails)*

### 8.7 Trauma-Informed Intake UX
- [x] Implement Quick Exit / panic button on the intake stepper AND detail screens *(instant one-tap: wipes every transient buffer and routes to the neutral `QuickExitSafeScreen`; `onQuickExit` host seam to exit the vault shell)*
- [x] Implement Pause, Save & Resume *(Pause & Save persists an AES-256-GCM SEALED `IntakeDraft` at any step via `EncryptedIntakeDraftStore`/`intake_drafts` table v9; step-1 resume banner restores the exact step; Start fresh discards)*
- [x] Implement trauma-informed framing + grounding/reassurance messaging across the 5-step stepper *(fixed `_GroundingNote` on every step, non-destructive confirmations, ✕ close confirms when dirty, QUICK EXIT stays instant)*
- [x] Implement memory hygiene / zero-leak wipe *(`zeroFill` + `_wipeTransientState`: narrative controller cleared, picked evidence byte buffers zero-filled, selections dropped on quick exit / confirmed close / submit)*
- [ ] Implement voice-note evidence capture using `record` and `just_audio` *(DEFERRED — the 8.7 trauma-UX brief shipped first; voice capture is a follow-on)*
- [ ] Create optional voice-to-text transcription *(DEFERRED with voice capture)*
- [x] VERIFY: Write widget tests for quick-exit wipe + routing, pause/resume round-trip, ✕ confirm, grounding notes, FLAG_SECURE
- [x] VERIFY: Write integration tests for pause → exit → resume → complete (zero data loss, sealed at rest)
- [x] SECURITY CHECKPOINT: Confirm drafts are encrypted at rest + buffers wiped *(byte-level sealed-at-rest proof, zeroFill unit proof, zero-identity frame, FLAG_SECURE on the safe screen)*

---

## Phase 9: Pillar 4 — The Academy (Open Education)

> **STATUS: IN PROGRESS (9.1 up)** — Task 8.8 shipped the foundation scaffold: `lib/academy/` (zero-identity `AcademyDomain`/`AcademyModule`/`AcademySyllabus` value objects + `AcademySyllabusRepository`/`AcademyProgressStore` ports), in-memory data stubs, `AcademyBloc` state, and the FLAG_SECURE `AcademyMasthead` (textbook register). Tasks 9.1–9.6 build on these entry points.

### Objective
Implement the Academy pillar with structured syllabus tree, offline caching, and distraction-free video delivery.

### 9.1 Academy UI Foundation
- [x] Create `AcademyMasthead` component with textbook aesthetic *(landed in Task 8.8 scaffold)*
- [x] Implement syllabus tree navigation screen *(FLAG_SECURE masthead + MY PROGRESS section + BROWSE BY DOMAIN grid → domain module list → module detail)*
- [x] Create module view with video player and sandbox *(FOUNDATION only: breadcrumb + metadata + completion toggle + VIDEO ROOM / GUTENBERG ARCHIVE / SANDBOX section frames; the player + sandbox land with Task 9.3 by plan)*
- [x] Implement progress tracking UI *(AcademyProgress pure projections — per-domain + overall %, UUID-v4-keyed only — + toggle persisted through the progress store)*
- [x] VERIFY: Write widget tests for AcademyMasthead rendering
- [x] VERIFY: Write integration tests for syllabus navigation *(syllabus → domain → module → toggle → progress round-trip through one bloc instance)*
- [x] SECURITY CHECKPOINT: Confirm video player uses privacy-enhanced embed mode *(enforced BY CONSTRUCTION: the module screen imports NO video package until Task 9.3 — static checkpoint test)*

### 9.2 Syllabus Tree Implementation
- [x] Implement hierarchical syllabus data structure (Domain → Category → Subject → Module) *(Domain → Module + UUID-validated leaf + opaque content refs from 8.8/9.1; Category/Subject mid-levels deferred with the search index)*
- [ ] Create syllabus tree UI with expand/collapse *(9.1 shipped the drill-in tree; expand/collapse mid-levels deferred with Category/Subject)*
- [x] Implement locale tagging for regional variants *(ISO 639-1 locale tags validated on every domain/module + `locale` columns in schema v10; regional variant CONTENT deferred)*
- [ ] Create syllabus search using Meilisearch *(external-service task — deferred)*
- [x] VERIFY: Write unit tests for syllabus tree traversal *(repository round-trip/seed/cache/determinism + row-codec re-validation)*
- [ ] VERIFY: Write integration tests for syllabus search *(deferred with Meilisearch)*
- [x] SECURITY CHECKPOINT: Confirm syllabus data is served from local cache *(LocalAcademySyllabusRepository is local-cache-first over SQLCipher — zero network in the read path; schema v10 has no identity columns)*

### 9.3 Video Room with Privacy-Enhanced Embeds
- [x] Implement YouTube embed with privacy-enhanced mode (`youtube-nocookie.com`) *(PrivacyEmbedUrl builder — hard-coded nocookie host + rel=0 + modestbranding; validated 11-char ids only)*
- [x] Strip all recommendations, autoplay, and Shorts *(structural: no autoplay/list/shorts/si params can be emitted — asserted by test; single-launch play, no auto-launch)*
- [ ] Implement HLS player for proprietary content (Cloudflare R2/Bunny.net) *(HlsSource value type + deferred UI state landed; signed-URL playback deferred to the Phase-9 media integration)*
- [ ] Create adaptive bitrate streaming (ABR) for low-bandwidth *(deferred with HLS playback — Phase 9)*
- [x] VERIFY: Write integration tests for YouTube embed rendering *(player widget + module-screen integration — play launches the exact nocookie URL through the injected launcher seam)*
- [ ] VERIFY: Write integration tests for HLS playback *(deferred with the HLS player — Phase 9)*
- [x] SECURITY CHECKPOINT: Confirm no recommendation sidebars in video player *(no list/sidebar/related/thumb-up UI in the player tree — structural scan; boundary holds with zero video SDK imports)*

### 9.4 Offline Module Caching
- [x] Implement module manifest generation (list of assets with sizes) *(`ModuleAssetManifest` — deterministic video + transcript assets with per-asset sizes and a total offline budget)*
- [x] Create download-for-offline flow with storage warning *(OFFLINE COPY section: status chip + budget + storage-warning dialog gate (200 MB default) + persistent over-budget banner)*
- [x] Implement background download using WorkManager *(`WorkmanagerModuleDownloadDispatcher` one-off task seam, compile-verified like `WorkmanagerScheduler`; in-process dispatcher for tests/harness)*
- [x] Create offline content playback logic *(`OfflinePlaybackResolver` — YouTube privacy embeds network-only by construction; HLS available once cached, rendering deferred to Phase-9 media integration)*
- [x] VERIFY: Write integration tests for module download *(data-layer cold-restart + queued-row recovery + widget + screen integration)*
- [x] VERIFY: Write integration tests for offline playback *(playback-decision matrix: cached/uncached × youtube/HLS/null)*
- [x] SECURITY CHECKPOINT: Confirm cached content is stored in encrypted partition *(SQLCipher `module_cache` table + AES-256-GCM sealed payloads — byte-level proof, plaintext wiped)*

### 9.5 Sandbox Wiki System
- [x] Implement Markdown editor for Sandbox contributions *(plain Markdown source editor — BOLD/HEADING/LINK syntax chips, PREVIEW toggle, SAVE — zero new dependencies; the project has no Markdown renderer package)*
- [x] Create version control system (diff + revert) *(deterministic LCS line diff with `+A −R` summaries; append-only revision history; REVERT appends a new revision with the target body — never rewrites history)*
- [x] Implement attributed-but-pseudonymous authorship *(deterministic `SA-####` handle derived from the module UUID (FNV-1a 32-bit → 4 hex) — zero identity, zero PII)*
- [x] Create Sandbox sync logic for offline edits *(`QueueSandboxRevisionSink` — local-first SQLCipher write, then sealed sync enqueue with the `v:1` wire frame; schema v12 `sandbox_pages` + `sandbox_revisions`; `body_markdown` flagged sensitive)*
- [x] VERIFY: Write unit tests for Markdown version control *(15 sandbox domain tests: models, handle, diff determinism/empty-text, zero-identity)*
- [x] VERIFY: Write integration tests for offline edit queuing *(queue sink byte-level sealed-envelope proof + local repo cold-restart/codec strictness + bloc + 3 widget suites)*
- [x] SECURITY CHECKPOINT: Confirm Sandbox edits are encrypted before sync *(sealed sync enqueue via the AES-256-GCM queue cipher; body persisted only in the encrypted partition; zero-identity columns structural scan)*

### 9.6 Cross-Pillar Study Group Matching
- [x] Implement pin-code-based learner matching *(client-side deterministic matcher: anchor module +5, cross-pillar topic +3, pin exact +4/district +2/region +1, locale +2; full groups hard-filtered; UUID tie-break — every device converges on the identical ranking)*
- [ ] Create study group invitation flow via Vault *(deferred — Vault E2EE group chat is a network surface; the local-first matching + sealed sync sink ships here)*
- [ ] Implement group chat using Vault messaging *(deferred — Phase-4 infra pass, same as relay/video content delivery)*
- [x] Create study group progress tracking *(local membership rows — offline-first `study_group_members`, initiator + join, idempotent re-join; schema v13)*
- [x] VERIFY: Write integration tests for study group matching *(matcher determinism + cold-restart + sealed sink byte-level + bloc flows)*
- [ ] VERIFY: Write integration tests for group chat *(deferred with the group-chat surface)*
- [x] SECURITY CHECKPOINT: Confirm study group data uses Vault E2EE *(client-side: blinded SG-#### handles only, coarse pin scope SENSITIVE, frames SEALED by the AES-256-GCM queue cipher before sync, zero identity columns in schema v13; server E2EE deferred with the chat surface)*

---

## Phase 10: Cross-Pillar Systems

### Objective
Implement the shared systems that tie all pillars together: unified identity, karma engine, and notification system.

### 10.1 Unified Identity Layer
- [x] Implement blind-hash ID sharing across all pillars *(client-side unified identity layer: `lib/identity/` — one blind hash shared READ-ONLY across Vault/Ledger/War Room/Academy via `UnifiedIdentityService`)*
- [x] Create identity service with minimum claim extraction *(per-pillar `PillarClaims` projections composed from the pillar claim sources — Vault: username + device keys; Ledger: pin code + karma; War Room/Academy: nothing beyond the hash)*
- [x] Implement pillar-specific permission checks *(`PillarClaim.allowlistFor` enforced AT PROJECTION TIME — composing a claim a pillar cannot hold throws)*
- [x] Create identity verification UI (one-time during onboarding) *(`IdentityVerificationScreen` — blinded `@citizen_` handle + per-pillar claim rows, FLAG_SECURE)*
- [x] VERIFY: Write unit tests for blind-hash ID generation *(existing Phase 2.4 suite retained; 40 new Task 10.1 tests)*
- [x] VERIFY: Write integration tests for cross-pillar identity sharing *(service + bloc + widget + harness smoke tests — every pillar receives exactly its minimum claims)*
- [x] SECURITY CHECKPOINT: Confirm no pillar stores full user profile *(allowlist enforcement + structural scans — a full-profile projection is unconstructible)*

### 10.2 Civic Karma Engine
- [x] Implement karma event consumption from NATS *(client-side `KarmaEventSource` port + in-memory impl — the NATS JetStream consumer itself remains deferred with the Phase-4 infra surface)*
- [x] Create karma calculation logic with sub-linear voting weight *(`KarmaVoteWeight` sqrt curve — 1 + floor(sqrt(karma)) capped at 10; deterministic)*
- [x] Implement karma gates (threshold checks for privileges) *(`KarmaGate` — skip-probation ≥50, Peer Review voting ≥100, analyst eligibility ≥150, Moderator Council ≥500 + 90-day tenure)*
- [x] Create monthly decay job (-2% for inactive accounts) *(`KarmaDecay` — integer math, deterministic across devices)*
- [x] Implement anomaly detection for lockstep voting *(`LockstepDetector` — ≥3 NEW accounts voting within a 10-min window dampens vote weight to 25%; deterministic, hash-only)*
- [x] VERIFY: Write unit tests for karma calculation *(action values + strict wire decode, gate thresholds/tenure, sub-linear ladder, decay ladder, lockstep clustering)*
- [x] VERIFY: Write integration tests for karma gate enforcement *(bloc-level gate projection incl. tenure; ledger balance → tier → gate integration)*
- [x] SECURITY CHECKPOINT: Confirm karma events are append-only and auditable *(SHA-256 hash-chained `KarmaEvent` ledger — `verifyIntegrity` detects mutate/reorder/remove; schema v14 `karma_events` with zero identity columns; 64-hex blinded actor only)*

### 10.3 Karma Badge UI
- [x] Implement `KarmaBadge` value object with tier-specific color, icon, and label tokens (5 tiers: Citizen/Contributor/Validator/Analyst/Council)
- [x] Create reusable `KarmaTierChip` widget (compact inline badge for list tiles) and `KarmaBadgeIndicator` widget (larger badge for detail views)
- [x] Wire optional karma badge seams into cross-pillar screens (Vault peer handles, Ledger post author/reviewer, War Room analyst cards, Academy study groups)
- [x] VERIFY: Write unit tests for `KarmaBadge.forBalance` (14 domain tests) and widget tests for `KarmaTierChip`/`KarmaBadgeIndicator` (15 widget tests)
- [x] VERIFY: Security checkpoint — zero-PII tree scans on all badge widgets, no networking imports, no print/debugPrint in badge files
- [x] SECURITY CHECKPOINT: Confirm badge consumes only public integer balance and fixed tier labels — no raw hashes, no identity, no fingerprinting surface

### 10.4 Notification System
- [x] Implement push notification service (Firebase Cloud Messaging or self-hosted) *(local in-memory implementation for harness + SQLCipher-ready row codec; push delivery deferred with Phase-4 infra)*
- [x] Create notification types: karma events, case assignments, Ledger review requests *(3 fixed `NotificationType` enum values with strict wire decode)*
- [x] Implement notification preferences UI *(per-type toggle switches + master toggle, stored locally, zero-PII)*
- [x] Create notification history viewer *(full list with unread badges, per-type filter chips, mark-as-read)*
- [x] VERIFY: Write integration tests for push notification delivery *(60 new tests across domain/data/state/widget/security, all green)*
- [x] VERIFY: Write unit tests for notification type filtering *(filter + preferences round-trip tests included)*
- [x] SECURITY CHECKPOINT: Confirm notifications contain no PII in payload *(widget-tree scans, no networking imports, no print/debugPrint, zero identity columns in schema)*

### 10.5 Transparency Log
- [x] Implement append-only transparency log for moderation actions *(SHA-256 hash-chained `TransparencyRecord` with `TransparencyAction` enum (6 fixed types), `verifyIntegrity` tamper detection, `InMemoryTransparencyRepository` + SQLCipher row codec; schema v16 `transparency_events` table (zero identity columns))*
- [x] Create transparency log viewer UI per pin-code board *(FLAG_SECURE `TransparencyLogScreen` with integrity badge, record list, and verify button; harness 8th Log tab)*
- [x] Implement government request logging (PII stripped *(record summaries are public-label-only, zero PII by construction; the accessRequest/dataExport action types serve this purpose)*
- [x] Create log verification using cryptographic hashes *(SHA-256 chain links with `verifyIntegrity` recomputation; tamper/reorder/remove detection)*
- [x] VERIFY: Write unit tests for append-only log property *(47 new tests across domain/data/state/widget/security, all green)*
- [x] VERIFY: Write integration tests for log verification *(integrity chain tests, tamper detection, cold-restart persistence)*
- [x] SECURITY CHECKPOINT: Confirm transparency log is publicly readable but PII-free *(widget-tree scans, no networking imports, no print/debugPrint, zero identity columns in schema)*

---

## Phase 11: Security Hardening & Compliance

### Objective
Implement comprehensive security measures, compliance features, and audit mechanisms to meet regulatory requirements and protect user data.

### 11.1 DPDP Consent Implementation
- [ ] Create DPDP consent flow during onboarding
- [ ] Implement consent versioning and tracking
- [ ] Create consent withdrawal UI
- [ ] Implement data deletion on consent withdrawal
- [ ] VERIFY: Write unit tests for consent tracking
- [ ] VERIFY: Write integration tests for data deletion on withdrawal
- [ ] SECURITY CHECKPOINT: Confirm consent is obtained before any data processing

### 11.2 Audit Logging System
- [ ] Implement comprehensive audit log for all sensitive operations
- [ ] Create audit log viewer for admin users (two-person authorization)
- [ ] Implement audit log export functionality
- [ ] Create audit log retention policy (7 years)
- [ ] VERIFY: Write unit tests for audit log capture
- [ ] VERIFY: Write integration tests for audit log export
- [ ] SECURITY CHECKPOINT: Confirm audit logs are tamper-evident

### 11.3 Rate Limiting & Abuse Prevention
- [ ] Implement per-blind-hash_id rate limiting in Kong
- [ ] Create abuse detection logic for anomalous patterns
- [ ] Implement CAPTCHA challenge for suspicious activity
- [ ] Create abuse reporting UI for users
- [ ] VERIFY: Write integration tests for rate limiting
- [ ] VERIFY: Write unit tests for abuse detection logic
- [ ] SECURITY CHECKPOINT: Confirm rate limiting does not use IP addresses

### 11.4 Security Audit & Penetration Testing
- [ ] Conduct automated security scanning using `trivy` and `grype`
- [ ] Perform dependency vulnerability scanning
- [ ] Conduct manual penetration testing on API endpoints
- [ ] Implement security fixes from audit findings
- [ ] VERIFY: Run automated security scanners and confirm zero critical vulnerabilities
- [ ] VERIFY: Document and remediate all penetration test findings
- [ ] SECURITY CHECKPOINT: Confirm all critical vulnerabilities are remediated before deployment

### 11.5 Compliance Documentation
- [ ] Create data processing agreement templates
- [ ] Implement privacy policy versioning
- [ ] Create security whitepaper for public transparency
- ] Implement bug bounty program documentation
- [ ] VERIFY: Review legal documentation with compliance team
- [ ] VERIFY: Publish security whitepaper for transparency
- [ ] SECURITY CHECKPOINT: Confirm all documentation is accurate and up-to-date

---

## Phase 12: Performance Optimization & Scalability

### Objective
Optimize application performance for low-end devices and implement scalability measures for growth.

### 12.1 Client-Side Performance Optimization
- [ ] Implement lazy loading for images and videos
- [ ] Create image compression and resizing logic
- [ ] Implement code splitting for Flutter (defer loading of less-used pillars)
- [ ] Optimize app startup time (target <600ms cold start)
- [ ] VERIFY: Measure app startup time on budget Android device
- [ ] VERIFY: Profile memory usage and confirm no leaks
- [ ] SECURITY CHECKPOINT: Confirm optimizations do not compromise security

### 12.2 Database Performance Optimization
- [ ] Implement database connection pooling
- [ ] Create database query optimization (indexes, query plans)
- [ ] Implement read replicas for read-heavy operations
- [ ] Create database caching layer (Redis)
- [ ] VERIFY: Run database load tests and confirm <100ms query latency
- [ ] VERIFY: Monitor database connection pool utilization
- [ ] SECURITY CHECKPOINT: Confirm read replicas do not expose sensitive data

### 12.3 CDN & Content Delivery Optimization
- [ ] Configure Cloudflare CDN for static assets
- [ ] Implement Bunny.net CDN for video delivery
- [ ] Create edge caching rules for API responses
- [ ] Implement image optimization at edge
- [ ] VERIFY: Measure CDN cache hit ratio and confirm >80%
- [ ] VERIFY: Test video delivery with adaptive bitrate
- [ ] SECURITY CHECKPOINT: Confirm CDN does not cache sensitive data

### 12.4 Horizontal Scaling Preparation
- [ ] Implement Kubernetes horizontal pod autoscaling
- [ ] Create database sharding strategy (pin-code prefix-based)
- [ ] Implement service mesh for inter-service communication (optional)
- [ ] Create load testing scenarios for scale testing
- [ ] VERIFY: Run load tests with 10,000 concurrent users
- [ ] VERIFY: Monitor autoscaling behavior under load
- [ ] SECURITY CHECKPOINT: Confirm scaling does not expose sensitive data

---

## Phase 13: Testing & Quality Assurance

### Objective
Implement comprehensive testing strategy covering unit tests, integration tests, E2E tests, and security testing.

### 13.1 Unit Test Coverage
- [ ] Achieve 80%+ code coverage for Flutter client
- [ ] Achieve 80%+ code coverage for Go services
- [ ] Implement test doubles for external dependencies
- [ ] Create property-based tests for critical logic
- [ ] VERIFY: Run coverage reports and confirm targets met
- [ ] VERIFY: Review uncovered code and add tests where critical
- [ ] SECURITY CHECKPOINT: Confirm tests do not contain real credentials

### 13.2 Integration Testing
- [ ] Create integration tests for API endpoints
- [ ] Implement database integration tests with test containers
- [ ] Create WebSocket integration tests
- [ ] Implement sync integration tests with network simulation
- [ ] VERIFY: Run integration tests in CI/CD pipeline
- [ ] VERIFY: Test failure scenarios and confirm graceful degradation
- [ ] SECURITY CHECKPOINT: Confirm integration tests use test data only

### 13.3 End-to-End Testing
- [ ] Implement E2E tests using integration_test (Flutter)
- [ ] Create critical user journey tests (registration, messaging, posting)
- [ ] Implement E2E tests for offline scenarios
- [ ] Create visual regression tests for UI components
- [ ] VERIFY: Run E2E tests on multiple device form factors
- [ ] VERIFY: Test critical journeys with network interruption
- [ ] SECURITY CHECKPOINT: Confirm E2E tests do not expose real user data

### 13.4 Security Testing
- [ ] Implement automated security scanning in CI/CD
- [ ] Create security regression tests for known vulnerabilities
- [ ] Implement secret scanning tests
- [ ] Create penetration test scenarios
- [ ] VERIFY: Run security scanners and confirm zero critical issues
- [ ] VERIFY: Test secret scanning with fake secrets
- [ ] SECURITY CHECKPOINT: Confirm security tests are comprehensive

### 13.5 Performance Testing
- [ ] Implement load testing for API endpoints
- [ ] Create performance benchmarks for critical operations
- [ ] Implement memory leak detection tests
- [ ] Create battery usage tests for mobile client
- [ ] VERIFY: Run load tests and confirm SLA compliance
- [ ] VERIFY: Profile memory usage over extended sessions
- [ ] SECURITY CHECKPOINT: Confirm performance tests do not compromise security

---

## Phase 14: Deployment & Monitoring

### Objective
Implement production deployment pipeline, monitoring, and incident response procedures.

### 14.1 Production Deployment Pipeline
- [ ] Create blue-green deployment strategy
- [ ] Implement canary releases for new features
- [ ] Create database migration scripts for production
- [ ] Implement feature flag system
- [ ] VERIFY: Test deployment pipeline in staging environment
- [ ] VERIFY: Test rollback procedure
- [ ] SECURITY CHECKPOINT: Confirm deployment pipeline does not expose secrets

### 14.2 Monitoring & Alerting
- [ ] Configure Grafana dashboards for all services
- [ ] Implement alerting rules for critical metrics
- [ ] Create incident response runbooks
- [ ] Implement on-call rotation system
- [ ] VERIFY: Test alerting by simulating failures
- [ ] VERIFY: Review dashboards with operations team
- [ ] SECURITY CHECKPOINT: Confirm monitoring data does not contain PII

### 14.3 Log Management & Analysis
- [ ] Configure Loki log retention policies
- [ ] Implement log aggregation for all services
- [ ] Create log analysis queries for troubleshooting
- [ ] Implement log-based alerting
- [ ] VERIFY: Test log aggregation and confirm all services are captured
- [ ] VERIFY: Verify PII scrubbing in logs
- [ ] SECURITY CHECKPOINT: Confirm logs are scrubbed of PII before storage

### 14.4 Backup & Disaster Recovery
- [ ] Implement automated database backups (daily)
- [ ] Create backup verification scripts
- [ ] Implement disaster recovery procedures
- [ ] Create backup restoration tests
- [ ] VERIFY: Test backup restoration in staging environment
- [ ] VERIFY: Measure RTO and RPO targets
- [ ] SECURITY CHECKPOINT: Confirm backups are encrypted at rest

### 14.5 Incident Response
- [ ] Create incident response playbook
- [ ] Implement incident severity classification
- [ ] Create communication templates for incidents
- [ ] Implement post-incident review process
- [ ] VERIFY: Conduct incident response drill
- [ ] VERIFY: Review and update playbook based on drill
- [ ] SECURITY CHECKPOINT: Confirm incident response does not expose sensitive data

---

## Phase 15: Documentation & Handover

### Objective
Create comprehensive documentation for developers, operators, and users to ensure long-term maintainability.

### 15.1 Developer Documentation
- [ ] Create API documentation (OpenAPI/Swagger)
- [ ] Write architecture decision records (ADRs)
- [ ] Create contributor guidelines
- [ ] Implement code documentation standards (JSDoc, GoDoc)
- [ ] VERIFY: Review API documentation with development team
- [ ] VERIFY: Test code examples in documentation
- [ ] SECURITY CHECKPOINT: Confirm documentation does not expose secrets

### 15.2 Operations Documentation
- [ ] Create deployment guides
- [ ] Write runbooks for common operations
- [ ] Create troubleshooting guides
- [ ] Implement infrastructure documentation
- [ ] VERIFY: Test deployment guides in staging environment
- [ ] VERIFY: Review runbooks with operations team
- [ ] SECURITY CHECKPOINT: Confirm operations docs do not expose credentials

### 15.3 User Documentation
- [ ] Create user onboarding guides
- [ ] Write FAQ documentation
- [ ] Create video tutorials for key features
- [ ] Implement in-app help system
- [ ] VERIFY: Test user guides with beta users
- [ ] VERIFY: Review tutorials for clarity
- [ ] SECURITY CHECKPOINT: Confirm user docs do not expose technical details

### 15.4 Security Documentation
- [ ] Create security whitepaper
- [ ] Write penetration test reports
- [ ] Implement security FAQ for users
- [ ] Create bug bounty program documentation
- [ ] VERIFY: Review security whitepaper with security team
- [ ] VERIFY: Publish bug bounty program
- [ ] SECURITY CHECKPOINT: Confirm security docs are accurate and comprehensive

---

## Appendix: Security Checkpoints Summary

### Critical Security Rules to Verify
1. **Zero-Plaintext Logging:** No `print()` or `debugPrint()` outputs raw payload data
2. **No Server-Side Decryption:** Vault and War Room content never decrypts on server
3. **No Device Fingerprinting:** Root/jailbreak detection is local-only, no telemetry
4. **FLAG_SECURE Enforcement:** Vault and War Room screens block screenshots
5. **No Cloud AI for PII:** PII redaction uses local models or deterministic logic only
6. **Zero-Retention Translation:** Cross-lingual translation drops plaintext immediately
7. **No Phone Number Persistence:** Raw phone numbers are hashed and discarded
8. **Offline-First Enforcement:** All network requests go through local queue first
9. **No Direct HTTP from UI:** UI components bind to local state, not network futures
10. **Strict Package Management:** No dependencies added without explicit permission

### Verification Commands
```bash
# Secret scanning
gitleaks detect --source .

# Security scanning
trivy image <image-name>
gosec ./...

# Dependency scanning
dependabot status
npm audit (if applicable)

# Log scrubbing verification
# Inject fake PII into logs and confirm scrubbing
```

---

## Progress Tracking

**Current Phase:** Phase 10 - Cross-Pillar Systems  
**Current Task:** 10.5 Transparency Log COMPLETE *(SHA-256 hash-chained append-only transparency log, 6 fixed action types, `TransparencyRecord` + `TransparencyAction` domain, `TransparencyRepository` port + in-memory impl + SQLCipher row codec, schema v16 `transparency_events` table (zero identity columns), `TransparencyLogState`/`TransparencyLogBloc`/`LocalTransparencyLogBloc`, FLAG_SECURE `TransparencyLogScreen` (integrity badge + record list + verify button), harness 8th Log tab, 47 new tests (1869 total), zero-PII, `flutter analyze` 0 issues)* — next: Task 11.1 (Phase 11)
**Overall Progress:** Phase 2 complete (2.1–2.7); Phase 3 COMPLETE (3.1–3.6); Phase 4 COMPLETE (4.1–4.8); Phase 5 COMPLETE (5.1–5.6); Phase 6 COMPLETE (6.1–6.6); Phase 7 COMPLETE (7.1–7.6); Phase 8 COMPLETE (8.1–8.8); Phase 9 COMPLETE (9.1–9.6); Phase 10 COMPLETE (10.1–10.5)
**Last Updated:** 2026-08-18
**Next Review:** After completion of Task 10.5




**Last Updated:** 2026-08-18  
**Next Review:** After completion of Task 10.4

---

## Change Log

| Date | Version | Changes |
|------|---------|---------|
| 2026-07-04 | 1.0 | Initial MASTER_PLAN creation |
| 2026-07-04 | 1.1 | Completed Phase 1.1 - Repository & Development Environment Setup |
| 2026-08-02 | 1.2 | Completed Task 4.2 - API Gateway (Kong OSS), live-verified (JWT 401/200, per-blind_hash_id throttle, IP stripping, PII-scrubbed logs) |
| 2026-08-03 | 1.3 | Completed Task 4.3 - Identity Service (Go) — OTP/MSG91, Argon2id blind-hash w/ Vault salt, username cooldown, device keys, stdlib RS256 JWT + rotating refresh, Redis OTP/revocation; 73 unit tests + live smoke test; phone-never-persisted checkpoint passed |
| 2026-08-04 | 1.4 | Completed Task 4.4 - Messaging Relay Service (Go) — WebSocket connections (coder/websocket, first-frame JWT auth, 25s/10s heartbeat), ciphertext envelope routing with server-verified sender hashes, Redis Streams offline queue (30-day TTL, purge on ack), multi-device fan-out, connection-request state machine; 114 Go tests (41 new relay) + live smoke test (12/12); never-decrypts checkpoint passed |
| 2026-08-04 | 1.5 | Completed Task 4.5 - PostgreSQL Schema & Migrations — embedded migration runner (forward/rollback, advisory-locked) creating users/usernames/devices/refresh_tokens/connection_requests with postgis/pgcrypto/pg_stat_statements/uuid-ossp; ENABLE+FORCE RLS with civic_app-scoped policies (live out-of-scope denial verified); pgcrypto encryption at rest for device keys; PostgreSQL-backed stores wired into identity + relay; primary/standby replication + WAL-archive-to-MinIO declarative configs; 133 Go tests (18 new) + live Postgres verification; pgcrypto/RLS security checkpoint passed |
| 2026-08-04 | 1.6 | Completed Task 4.6 - Redis Configuration — production Redis client factory (go-redis/v9) with Sentinel HA failover, resilient retries (3, backoff 8ms→512ms), managed pooling (size 20, min-idle 5) and a Ping health probe wired into identity + relay (fail-fast in production); validated namespace registry (otp/otp_attempts/refresh/revoked/revoked_family/msg_queue + karma/vote_buffer/analyst_load/rate) with spec TTL policies and PII-rejecting key builders adopted by all Redis stores; Redis Sentinel Helm chart (1 primary, 2 replicas, 3 sentinels, AOF+RDB persistence, PVC, sh-based probes); 152 Go tests (19 new) + live 6-container Sentinel verification incl. kill-the-primary failover demo; no-plaintext-PII checkpoint passed |
| 2026-08-04 | 1.7 | Completed Task 4.7 - NATS JetStream Event Bus — JetStream client (stream init, PUBACK publish, explicit durable consumers via create+bind, auto-reconnect handlers), validated allowlist topic schema (relay.connection.accepted, identity.user.registered, karma.updated, search.sync.requested), at-least-once delivery; NATS Helm chart (JetStream file storage on PVC); 170 Go tests (18 new incl. embedded-nats-server restart-resume/no-loss + concurrent durable race + option fallback) + live docker verification (8/8 incl. broker-restart durability); zero-PII checkpoint passed |
| 2026-08-04 | 1.8 | Completed Task 4.8 - HashiCorp Vault Integration — expanded stdlib net/http Vault client (no SDK): AppRole login + background token renewal (LookupSelf/RenewSelf + re-login loop), KV v2 reads + metadata, Transit encrypt/decrypt helpers, TTL-aware SecretCache with rotation-detecting refresh + buffer wipe, WipeBytes hygiene, fail-fast Connect factory wired into identity + relay; redacting logger scrubs Vault tokens (hvs./s.), X-Vault-Token and Authorization headers; production config refuses to run without Vault auth; 190 Go tests (20 new) + live docker Vault verification (AppRole/KV/transit/cache/wrong-token/redaction) + deps checkpoint; secrets-never-logged checkpoint passed |
| 2026-08-04 | 1.9 | Completed Task 5.1 - Network State Detection (debounced 500ms listener via new DebouncedNetworkInfoProvider; mapping + scripted-transition + fakeAsync debounce tests) and Task 5.2 - Sync Worker / Offline Mutation Queue (UUID v4 idempotency keys wired into every queue mutation, 10s aggressive per-push timeout, crash recovery recoverInterrupted(), backoff jitter, deterministic LWW conflict hooks); 412 Flutter tests (33 new); Phase 4 + 5.1/5.2 fully verified (go test -race green, golangci-lint 0, deps + live Vault checks passed) |
| 2026-08-05 | 1.10 | Completed Task 5.3 - Idempotency Enforcement (server-side Redis dedup) — new `internal/idempotency` package (SETNX claim / get / complete / clear middleware wrapped around the relay's POST mutation routes inside auth): missing header = passthrough, malformed UUID = 400, in-flight = 409, completed = cached replay with `Idempotent-Replayed` header, handler failure = key cleared; actor-scoped keys `idempotency:{blind_hash_id}:{uuid}` under the validated `idempotency:` cache namespace (24h TTL, `IDEMPOTENCY_TTL` configurable) so cross-user replay is impossible and no PII can enter keys; 211 Go test funcs (18 new idempotency + 5 relay HTTP integration + namespace/config) — full `go test -race` green (11 suites), golangci-lint 0 violations, gofmt/vet clean, deps checkpoint passed |
| 2026-08-05 | 1.11 | Completed Task 5.4 - Sync Status UI Integration — new `SyncStatusBar` (`lib/state/ui/`) renders LIVE/CACHED/QUEUED/OFFLINE with per-state color+icon, pending-count badge, a 14px progress indicator while a sync run is in flight, and tap-to-expand showing "Last synced" + queue count; state contract extended additively with non-PII `lastSyncAt` + `isSyncing` (bloc stamps lastSyncAt exactly when the queue drains while online via injectable clock; isSyncing derives from in-progress queue items); widget consumes ONLY the `SyncStatusBloc` stream — no data-layer access; 447 Flutter tests total (34 new: 22 widget + 2 end-to-end integration via real ReconnectionSyncTrigger/BackgroundSyncWorker + 5 UI security scans + 5 bloc derivation), `flutter analyze` 0 errors, full suite green (also removed stale `flutter create` scaffold test that referenced a nonexistent main.dart) |
| 2026-08-09 | 1.13 | Completed Task 5.6 - Offline Queue Persistence (final task of Phase 5) — durable SQLCipher queue with exact serialization/deserialization bounds, `recoverInterrupted()` cold-start recovery, `purgeExpired` 30-day retention (strict boundary, wired into the worker before every drain), max-1000 FIFO eviction (in-flight protected, just-inserted item guaranteed to survive by construction), sealed-only versioned backup/restore envelope (`SyncQueueBackup`, idempotent restore, fully validated before any insert); 522 Flutter tests (35 new: 15 queue-repo incl. 4 purge + 5 serialization bounds + FIFO/tie, 10 backup codec, 2 cold-restart integration, 4 worker expiry cleanup, 3 ciphertext-at-rest checkpoint, security scans), `flutter analyze` 0 errors, suite green; queue-encrypted-at-rest checkpoint passed; **PHASE 5 COMPLETE** — next: Phase 6 Task 6.1 Vault UI Foundation |
| 2026-08-09 | 1.14 | Completed Task 6.1 - Vault UI Foundation (first task of Phase 6) — `VaultMasthead` (classified-document strip: Vault Blue #1A3D6B, `THE VAULT` wordmark, `[CLASSIFIED]` stamp, black pseudo-redaction bar), `VaultConversationListScreen` (BLoC-stream list with fixed `[end-to-end encrypted]` ciphertext previews + collapsible `VaultPendingRequestsSection` request queue + FAB), `VaultConversationDetailScreen` + `MessageBubble` (received white w/ 3dp Vault Blue left bar, sent Vault Blue; ✓/✓✓ receipts, amber queued indicator, Expires marker, fixed E2EE placeholder body) + static composer placeholder; new non-PII `formatPeerHandle` (derived `@peer_` + 6-hex display handles — never the raw blind hash); both screens wrap in `SecureScreenWrapper` (FLAG_SECURE) with late-subscribe `refresh()` on mount; 572 Flutter tests (50 new: 6 peer-handle + 8 masthead + 10 list + 2 real-BLoC integration + 13 detail/bubbles + 11 security scans incl. FLAG_SECURE-on-mount + runtime PII scans), `flutter analyze` 0 errors, suite green; FLAG_SECURE-on-all-Vault-screens + zero-PII-in-Vault-UI checkpoints passed |
| 2026-08-09 | 1.15 | Completed Task 6.2 - Connection Request Flow — backend username search via API Gateway: new auth-required `GET /v1/identity/username/{username}` (`LookupUsername`, policy-validated, 404 for unknown/released usernames, `{username, blind_hash_id}` response only; 5 Go tests incl. no-phone-in-response/logs); relay `ConnectionRequest` JSON-tag fix (snake_case wire contract); client connection-request domain/data/state layers — `ConnectionRequest` entity + single-transition CAS repository (blind-hash-only targets, sealed sync envelopes, idempotent-while-pending), `connection_requests` row codecs, `ConnectionRequestsBloc` inbox projection + `UserSearchBloc`; Task 6.1 queue is now BLoC-driven with wired Accept, new FLAG_SECURE `UsernameSearchSheet`, conversation tiles resolve remembered public usernames; stale-snapshot guard + directory-failure resilience; 612 Flutter tests (40 new), `flutter analyze` 0 errors, suite green; 216 Go test funcs, `go test -race` green, golangci-lint 0 violations; zero-phone-in-connection-requests checkpoint passed; key exchange on approval deferred to Task 6.3 |
| 2026-08-10 | 1.17 | Completed Task 6.4 - WebSocket Message Relay — new `lib/relay/` tree (domain: `RelayEnvelope`/sealed `RelayFrame` codec mirroring the Go relay's protojson `UseProtoNames` wire exactly (oneof members auth/auth_ack/envelope/ack/error, snake_case, base64 ciphertext, `DiscardUnknown`), `RelaySocket`/`RelaySocketConnector` ports, `RelayHeartbeat` data-frame watchdog, `RelayClient` orchestration (first-frame JWT auth in body, read loop, envelope auto-ack, reconnect with the Task 5.2 jittered backoff, dummy-traffic keepalive opt-in), `DummyTrafficGenerator`; data: `WebSocketRelaySocket` over `web_socket_channel` ^2.4.0 with transport-level `pingInterval: 20s` (dart:io closes on missed pong — the correct dead-connection detector since the relay's 25s pings are protocol control frames invisible to data-frame watchdogs; app watchdog gated by `hasTransportHeartbeat`); production hardening from code review: a drop during the auth handshake before any auth_ack is TRANSIENT (reconnects with backoff) while an explicit `auth_ack:false` is terminal and its policy-violation close is never retried (`_authRejected` flag, generation-guarded); 691 Flutter tests total (36 new: wire codec round-trips incl. the EXACT Go protojson shape, fake_async heartbeat watchdog, RelayClient lifecycle incl. pre-auth-drop reconnect + rejection-close terminal + transport-heartbeat-skips-watchdog + stale watchdog, real dart:io WebSocket integration lifecycle, security checkpoint: JWT in body not URL / no PII literals / no print / ciphertext never logged), `flutter analyze` 0 issues, suite green (2 Argon2id-heavy files hang only at the tail of the long serial run — pass in isolation); zero-PII + JWT-in-body-not-URL + ciphertext-never-logged checkpoints passed; next: Task 6.5 Multi-Device Pairing |
| 2026-08-10 | 1.19 | Completed Task 6.6 - Duress PIN Integration — new `VaultUnlockBloc`/`VaultUnlockState`/`DuressSetupBloc`/`DuressSetupState` (state/domain) + `LocalVaultUnlockBloc`/`LocalDuressSetupBloc` (state/data) wrapping the Phase 2.5 `DuressService`; **one PIN prompt for both PINs**: `VaultUnlockScreen` (FLAG_SECURE, obscured numeric PIN, spinner, SINGLE generic error) calls `unlock(pin)` which returns the opened `UnlockResult` (real vs decoy vault) ONLY to the routing callback — the state stream never reveals which vault was opened, and near-real vs near-duress failures are byte-identical; **decoy vault**: `DecoyVaultScreen` renders the SAME `VaultMasthead` + CONVERSATIONS header + the shared `VaultEmptyState` (extracted from the real list) — takes NO data inputs, references no bloc/repository/entity types, FLAG_SECURE wrapped (indistinguishable by security guard too), proven byte-identical to the real empty list by a rendered-text comparison test; **database switching**: real PIN opens `vault.db` / duress PIN opens `vault_decoy.db` purely by decryption — real records persist in the real vault and are INVISIBLE through the decoy result (integration-verified); **onboarding setup**: `DuressPinSetupSheet` (the ONE screen that labels the two obscured PIN fields), identical/empty/duplicate registrations all map to one generic error, PINs wiped from controllers after every attempt; 824 Flutter tests total (46 new: 9 unlock-bloc incl. DB switching + record isolation + generic failure matrix + failing-service degradation, 5 setup-bloc, 13 unlock-screen widget incl. real/decoy routing + byte-identical failure UI + FLAG_SECURE + PII scan, 4 decoy-screen widget incl. text-identity-vs-real-empty + FLAG_SECURE, 5 setup-sheet widget, 5 real-service integration incl. persisted-file indicator scan, 5 security checkpoint), `flutter analyze` 0 issues, affected suites 105/105 green (the pre-existing Argon2id `duress_service_test` batch-hang artifact unchanged — passes 16/16 in isolation); SECURITY CHECKPOINT PASSED: duress PIN indistinguishable from real PIN — static scans (unlock screen never references real/duress/decoy/VaultKind in code, never inspects result kind, state models carry no key/DB fields, no print/PII literals) + runtime (identical failure UI, decoy text ≡ real-empty text) + persisted vault bytes contain no real/duress/decoy indicator; next: Phase 7 Task 7.1 Ledger UI Foundation |
| 2026-08-10 | 1.18 | Completed Task 6.5 - Multi-Device Pairing — new `lib/pairing/` tree (domain: `PairingPayload` strict zero-PII QR codec (civic-commons://pair URI, 64-hex blind hash + UUID v4 + one-time 32-byte secret + PUBLIC keys only, base64url-unpadded wire, `DiscardUnknown`-tolerant decode, hard rejection of any PII-shaped field), `PairingSecretGenerator` (32-byte CSPRNG, 5-min window), `QrMatrix` value object + `QrEncoder`/`QrScanner` ports, `LinkedDevice` entity + `DeviceRegistry` port, `DevicePairingService` orchestrator, `IdentityKeySource` port); data: `qr_codec_impl.dart` wrapping the pure-Dart `qr` package (medium ECC, byte→module only — never interprets payload), `local_device_registry.dart` (EntityStore-backed, blind-hash-keyed), `secure_storage_identity_key_source.dart` (Keystore-backed); **code-review hardening (critical)**: the QR signed-prekey signature is now a REAL Ed25519 signature computed in `createPairingPayload` (the PrekeyManager/block placeholders are never trusted) and `authorizePayloadText` CRYPTOGRAPHICALLY verifies it against the embedded identity key BEFORE any session/registration — tampered/substituted spk/ik rejected (photographed-QR MITM protection); the one-time pairing secret is single-use (consumed on first successful authorize — replay rejected); schema migration v4 adds the `devices` table (SQLCipher row codecs, forward+rollback tested); state layer: `LinkedDevicesBloc` + `DevicePairingBloc` (sync broadcast per codebase convention) + `formatDeviceHandle` (derived `DEV_` + 6-hex, never raw UUIDs/keys); FLAG_SECURE UI: `DeviceManagementSheet` (list + revoke + QR panel), `PairingQrView` (CustomPaint matrix — payload text never rendered), `PairingScanSheet` (manual code entry, camera `QrScanner` port documented); 778 Flutter tests total (84 new: 73 pairing incl. 5 security — real-signature-verifies, tampered-spk/tampered-ik rejection, single-use replay, private-key-never-in-payload, plus 4 direct `SecureStorageIdentityKeySource` tests, 11 widget tests, schema/migration v4 updates), `flutter analyze` 0 issues, suite green (known Argon2id batch-hang artifact unchanged); SECURITY CHECKPOINT PASSED: QR-local key transfer (no cloud backup/sync/upload identifiers in code, word-boundary scans), zero PII in payload/UI/logs, FLAG_SECURE on all pairing screens, private keys never leave secure storage; next: Task 6.6 Duress PIN Integration |
| 2026-08-10 | 1.21 | Completed Task 7.2 - Geographic Clustering Implementation — new `lib/geo/` tree (domain: strict `PinCode` value object w/ `^[1-9]\d{5}$` validation + coarse `circlePrefix`, `GeoPlace` (pin + district + locality — the ONLY location-bearing value that crosses the data layer), `PinCodeResolution`/`PinCodeResolutionSource` (location/manual/persisted), `PinCodeResolver` port (throws `PinCodeResolutionException`), `PinCodeStore` port, `ExploreRadius` enum (Local only / 5 km / 10 km / 25 km)); data: `GeolocatorPinCodeResolver` — the SINGLE file touching raw coordinates (permission→position w/ 8s time limit + last-known fallback→reverse-geocode→collapse to coarse `GeoPlace`; coordinates never returned/logged/stored; test-injectable `GeoLocationService`/`GeoCodingService` seams over the plugin statics) + `LocalPinCodeStore`; state: `LedgerGeoBloc`/`LedgerGeoState` (persisted-pin-first offline resolution → device location → generic manual-entry fallback, monotonic-seq stale-drop, `ExploreRadius` tracking) + `LocalLedgerGeoBloc`; UI: `ExploreNearbySheet` (radius chips + scope line + `Use my location` re-resolve) wired into `LedgerFeedScreen` via a `_MastheadWithExplore` radar chip that shows the coarse `District · Locality · 800001` scope line and opens the sheet (radius flows through `onRadiusChanged`); pin-code feed filtering reuses the Task 7.1 `listPosts(pinCode:)` scoping; 922 Flutter tests total (37 new: 7 PinCode validation incl. no-finer-than-pin proof, 4 GeoPlace, 7 geo bloc (persisted-first/location/permission-degrade/never-crash/manual/radius/retry), 8 resolver (coarse-collapse, permission-upgrade, denied, position-fallback, missing position, bad postal, empty placemark, plugin seams), 4 ExploreNearbySheet widget (scope line + radius select + re-resolve + no-coordinates), 2 feed geo integration (scope line + sheet round trip, absent control without geo bloc), 5 security checkpoint (plugin-boundary scan, no-coordinate-fields structural proof, coarse-only type surface, no prints, no PII literals)), `flutter analyze` 0 issues, affected suites 361/361 green; SECURITY CHECKPOINT PASSED — location is never a fingerprint vector: only `geolocator_pin_code_resolver.dart` imports the plugins (static scan), domain/state declare no coordinate fields (structural proof), `GeoPlace` is the sole cross-boundary location type and it is coarse by construction, no location data is printed/persisted/logged; next: Task 7.3 Dynamic Radius UI (skill: dynamic-radius-ui) |
| 2026-08-10 | 1.22 | Completed Task 7.3 - Dynamic Radius UI (skill: dynamic-radius-ui) — dynamic-radius feed expansion per the skill: **threshold check** (injectable-clock 7-day recency window; when the exact pin has FEWER than 5 posts in the last 7 days and the ExploreRadius is expanded, the feed broadens — old posts never count toward the threshold, `FeedScope.expansionThreshold` tunable); **radius expansion** (pin → constituency/district fallback via the new coarse `LedgerPost.district` field — same-district posts are pulled in and flagged; posts with no district signal make expansion impossible by safe default); **NearbyBadgeWidget** (fixed `NEARBY` label chip in Civic Gold on exactly the expanded cards + a `~ N posts from nearby pins ~` italic strip under the filter row); **seamless blending** (ONE chronological feed — local + nearby interleave by status/date, NO separate Local/Nearby sections — skill step 4); wiring: `FeedScope`/`FeedScopeResult` value objects (pin + coarse `ExploreRadius` enum + injectable `now` clock — NO coordinates), `LedgerFeedRepository.listScoped()`, `LedgerFeedBloc.setRadius()` + scope-aware `_emit` (posts carry the `nearby` flag via `LedgerPostSummary.from(post, nearby:)`), and the feed screen RELAYS the geo bloc's radius to the feed bloc (subscription cancelled on dispose) so a radius picked in `ExploreNearbySheet` immediately refetches the expanded feed — the full sheet → geo bloc → feed relay → badged-feed flow is integration-tested; 938 Flutter tests total (16 new: 7 repository expansion unit tests — sparse→expands/dense-stays-local/7-day-window/category-filter-both-sides/radius-none-never-expands/no-district-safe-default/blended-single-list, 1 feed-bloc setRadius (flags nearby + collapses back), 4 feed-screen widget (badge on nearby only, nearby strip, blended cards, dense-no-expansion), 1 end-to-end integration (sheet 10 km → geo → feed → badge + strip), 3 security checkpoint additions); `flutter analyze` 0 issues; affected suites 377/377 green; SECURITY CHECKPOINT PASSED — expanded results are clearly marked AND the expansion adds zero location data: `FeedScope` carries only a pin + a coarse radius enum (no meters, no coordinates — tested), the badge renders only the fixed `NEARBY` label (no pin/district/identity), the dynamic-radius files import no `dart:io`/http and print nothing; next: Task 7.4 Post Creation & Queuing |
| 2026-08-18 | 1.44 | Completed Task 10.5 - Transparency Log (Phase 10, Cross-Pillar Systems) — append-only, auditable transparency log with SHA-256 hash chaining for moderation/system/access/export actions; domain: `lib/transparency/domain/` — `transparency_action.dart` (6 fixed enum types: moderationAction/contentReview/accessRequest/dataExport/accountAction/systemEvent — labels + shortLabels, strict fromWireName decode, unknown throws), `transparency_record.dart` (immutable entity: seq + UUID v4 recordId + action + public-label summary + pinCode scope + timestamp + prevHash/selfHash SHA-256 chain links; canonicalBytes excludes selfHash; computeSelfHash via `TransparencyHasher` port; genesisHash 64-zero), `transparency_repository.dart` (port: getByPinCode/getCount/append/verifyIntegrity — APPEND-ONLY, no update/delete API); data: `in_memory_transparency_repository.dart` (in-memory impl with seq validation + prevHash chain enforcement + verifyIntegrity recomputation; `_DeterministicHasher` for tests) + `real_transparency_hasher.dart` (production SHA-256 via `cryptography` package, byte-identical to karma engine hasher) + `transparency_record_codec.dart` (SQLCipher row encode/decode with strict read-path revalidation — unknown actions rejected, missing columns throw FormatException); schema **v16** — `transparency_events` (record_id PK UUID, seq, action, summary, pin_code, occurred_at, prev_hash, self_hash — ZERO identity columns) + migration v16 (up: CREATE TABLE transparency_events; down: DROP) + schema/migration tests updated (22 tables, fresh+incremental counts, v16 rollback); state: `TransparencyLogState` (idle/loading/ready/error + records list + integrityValid + recordCount + errorMessage) + `TransparencyLogBloc` port (refresh/verifyIntegrity) + `LocalTransparencyLogBloc` (monotonic-seq stale-drop, repository failure → generic payload-free error); UI: `lib/state/ui/transparency_log_screen.dart` (AppBar + integrity status banner (green verified/red failed) + record count + ListView of RecordTiles with action icons + timestamps + seq numbers, FLAG_SECURE) + harness 8th **Log** tab; 1869 Flutter tests total (47 new: 6 transparency-action — wire round-trip/labels/unknown-throws; 8 transparency-record — construct/genesisHash/canonicalBytes-deterministic/canonicalBytes-excludes-selfHash/equality/computeSelfHash; 10 in-memory-repo — genesis-accept/wrong-prevHash-reject/non-sequential-seq-reject/chain-correctly/getByPinCode-empty/getByPinCode-isolates/getCount-0/getCount-correct/verifyIntegrity-empty/verifyIntegrity-valid-chain/verifyIntegrity-tampered-detect; 4 codec — round-trip/missing-columns/unknown-action/encode-columns; 4 state — default/ready/error/integrityInvalid; 6 bloc — idle/refresh-ready/verifyIntegrity/error-generic/stream-emits/pinCode-isolation; 5 widget — renders-title/empty-state/integrity-verified/records-seeded/verify-button; 5 security-checkpoint — no-networking-imports/no-print/no-PII-literals/no-networking-in-UI/screen-tree-scan; schema +1 table count/transparency-zero-identity-columns + migration +1 v16-upgrade/v16-rollback + harness smoke 8-tab Log destination), `flutter analyze` 0 issues, `dart format` clean, affected suites 105/105 green; SECURITY CHECKPOINT PASSED — transparency records carry ONLY public-label summaries + fixed action labels + timestamps + seq numbers (widget-tree scans prove zero 64-hex hashes, zero phone patterns, zero emails in code lines), transparency files import no `dart:io`/http/WebSocket and never print/debugPrint (static scans), schema carries ZERO identity columns (structural scan), all screens wrapped in SecureScreenWrapper (FLAG_SECURE); next: Task 11.1 Phase 11 Security Hardening |
| 2026-08-18 | 1.43 | Completed Task 10.4 - Notification System (Phase 10, Cross-Pillar Systems) — local-first notification system with 3 fixed types (karma events/case assignments/ledger review requests); domain: `lib/notification/domain/` — `notification_type.dart` (3 enum values with wire names + strict fromWireName decode, unknown throws), `notification_record.dart` (immutable entity: UUID v4 id + type + public-label title/body + timestamp + isRead; equality on id+isRead; withRead immutable toggle), `notification_preferences.dart` (per-type enable/disable flags, allEnabled default, toWire/fromWire round-trip, withType/withAll immutable), `notification_repository.dart` (port: getAll/getByType/getUnreadCount/markRead/markAllRead/insert/getPreferences/savePreferences — no identity, no PII); data: `in_memory_notification_repository.dart` (in-memory impl seeded for harness, sorted newest-first) + `notification_record_codec.dart` (SQLCipher row encode/decode with strict read-path revalidation — unknown types rejected, missing columns throw FormatException); schema **v15** — `notifications` (notification_id PK UUID, type, title, body, created_at, is_read — ZERO identity columns) + migration v15 (up: CREATE TABLE notifications; down: DROP) + schema/migration tests updated (21 tables, fresh+incremental counts, v15 rollback); state: `NotificationState` (idle/loading/ready/error + notifications list + unreadCount + preferences + errorMessage; forType filter, isTypeEnabled convenience) + `NotificationBloc` port (refresh/markRead/markAllRead/savePreferences) + `LocalNotificationBloc` (monotonic-seq stale-drop, repository failure → generic payload-free error); UI: `lib/state/ui/` — `notification_history_screen.dart` (AppBar + Mark all read + per-type filter chips + ListView of NotificationTiles with unread dot + empty state, FLAG_SECURE) + `notification_preferences_screen.dart` (master toggle + per-type SwitchListToggles + privacy notice, FLAG_SECURE); harness 7th **Alerts** tab (5 seeded notifications: 3 unread + 2 read, all 3 types covered); 1822 Flutter tests total (60 new: 8 notification-type — wire round-trip/labels/unknown-throws; 6 notification-record — construct/withRead/equality/immutability; 8 notification-preferences — allEnabled/withType/withAll/toWire/fromWire/defaults; 8 in-memory-repo — getAll-sorted/getByType/unreadCount/markRead/markAllRead/insert/preferences; 5 codec — round-trip/isRead-flag/missing-columns/unknown-type/encode-columns; 4 state — default/forType-filter/preferences/error; 7 bloc — idle/refresh-ready/markRead/markAllRead/savePreferences/error-generic/stream-emits; 6 widget — renders-title-body/unread-dot/mark-all-read-button/empty-state/filter-chips; 6 security-checkpoint — no-networking-imports/no-print/no-PII-literals/no-networking-in-UI/history-screen-tree-scan/preferences-screen-tree-scan; schema +1 table count/notifications-zero-identity-columns + migration +1 v15-upgrade/v15-rollback + harness smoke 7-tab Alerts destination), `flutter analyze` 0 issues, `dart format` clean, affected suites 116/116 green; SECURITY CHECKPOINT PASSED — notifications carry ONLY public-label title/body + fixed type labels + boolean preferences (widget-tree scans prove zero 64-hex hashes, zero phone patterns, zero emails in code lines), notification files import no `dart:io`/http/WebSocket and never print/debugPrint (static scans), schema carries ZERO identity columns (structural scan), all screens wrapped in SecureScreenWrapper (FLAG_SECURE); next: Task 10.5 Transparency Log |
| 2026-08-18 | 1.42 | Completed Task 10.3 - Karma Badge UI (Phase 10, Cross-Pillar Systems) — reusable karma badge components for cross-pillar tier display; domain: `lib/karma/domain/karma_badge.dart` (`KarmaBadge` value object — maps each `KarmaTier` to its fixed presentation tokens: color + icon glyph + display label + description; `forBalance(int)` and `forTier(KarmaTier)` factories, deterministic, monotone in balance, no identity/PII); UI: `lib/karma/ui/karma_tier_chip.dart` (`KarmaTierChip` — compact inline badge widget for list tiles/cards, accepts optional `KarmaTier` or `int? balance`, renders `Container` with tier-colored background + `Icon` + uppercase label; null-safe renders nothing; compact mode for tight rows; zero-PII by construction) + `lib/karma/ui/karma_badge_indicator.dart` (`KarmaBadgeIndicator` — larger badge for detail views with optional balance number display); cross-pillar integration: optional `KarmaTierChip` badge seams wired into Vault `_ConversationTile` (peer handles), Ledger `_PostCard` (author line), War Room `_TeamRow` (analyst cards), Academy `_MatchCard`/`_groupCard` (study group participants) — optional parameters keep wiring non-breaking when karma data is not yet in the domain models; 1762 Flutter tests total (34 new: 14 karma-badge domain — all 5 tiers forBalance/forTier, boundary at each threshold, deterministic, progression monotone, descriptions non-empty; 15 widget — KarmaTierChip null-renders-nothing/all-tiers/explicit-tier-over-balance/compact/non-compact + KarmaBadgeIndicator null-renders-nothing/tier+balance/balance-only/explicit-tier; 5 security checkpoint — badge-no-PII-in-description/no-networking-imports/no-print/no-debugPrint in badge files + KarmaTierChip-tree-scan/KarmaBadgeIndicator-tree-scan zero-PII), `flutter analyze` 0 issues (5 infos only), `dart format` clean, `flutter build web` compiles, affected suites 106/106 green; SECURITY CHECKPOINT PASSED — badge consumes only public integer balance + fixed tier labels (widget-tree scans prove zero 64-hex hashes, zero phone patterns, zero emails), badge files import no `dart:io`/http/WebSocket and never print/debugPrint (static scans), cross-pillar seams are non-breaking optional parameters |
| 2026-08-18 | 1.41 | Completed Task 10.2 - Civic Karma Engine (Phase 10, Cross-Pillar Systems) — PRD §9.2 actions/deltas + gates/decay/lockstep as a pure CLIENT-side engine over an APPEND-ONLY, AUDITABLE local ledger; domain: `lib/karma/domain/` — `karma_action.dart` (`KarmaPillar` ledger/warRoom/academy/crossPillar strict wire decode + `KarmaAction` — all 7 PRD §9.2 actions with FIXED deltas (+5 Ledger verified, +15 War Room contribution, +3 sandbox upvoted-by-3+, +2 module completed, +20 analyst vetted ONE-TIME, −3 Ledger rejected, −25 confirmed abuse cross-pillar) + wireName/label/oneTime flags + strict fromWireName (unknown throws — forged rows can never masquerade)), `karma_event.dart` (`KarmaEvent` — APPEND-ONLY auditable event: seq + UUID v4 eventId (wire Idempotency-Key) + validated 64-hex actorHash + action + delta + balanceAfter + timestamp + prevHash/selfHash SHA-256 chain links over the canonical bytes — the karma ledger is a hash chain exactly like the Task 8.6 custody log; `Sha256Hasher` port), `karma_gate.dart` (`KarmaGate` — skipProbationPosting ≥50 / peerReviewVoting ≥100 / warRoomAnalystEligibility ≥150 / moderatorCouncil ≥500 + 90-day TENURE (only gate with tenureDays); `isSatisfied(karma, accountAgeDays)` pure), `karma_calculation.dart` (`KarmaVoteWeight` — SUB-LINEAR per-voter weight = 1 + floor(sqrt(karma)) capped at 10; deterministic; floored at 1 so new accounts still vote), `karma_decay.dart` (`KarmaDecay` — −2%/month INACTIVE decay via pure integer math (score*98~/100 per month) — no floats, deterministic across devices), `lockstep_detector.dart` (`KarmaVote` + `LockstepDetector` — ANOMALY DETECTION: a cluster of ≥3 NEW accounts (age < 30d) voting within a 10-min window dampens the tally weight to 25% (dampeningFactor); established accounts never dampen; deterministic regardless of input order — Sybil resistance without banning, PRD §9.2), `karma_repository.dart` port (balance/events/record/verifyIntegrity — APPEND-ONLY, no update/delete API) + `karma_event_source.dart` port (NATS consumption seam — server-side consumer deferred with the Phase-4 infra surface; client uses the in-memory impl); data: `karma_event_records.dart` (`KarmaEventRecord` row + strict `tryParse` read-path revalidation — delta MUST equal the action's fixed value, hashes must be 64-hex, unknown action wire rejected) + `local_karma_repository.dart` (SQLCipher-backed via `EntityStore` — OFFLINE-FIRST: every event lands in the encrypted partition immediately; balance is ALWAYS the chain's last balanceAfter (never a separately-mutated counter); `record` enforces exactly-next-seq + prevHash link or throws; `verifyIntegrity` recomputes every selfHash and walks the chain — mutate/reorder/remove all detected; `RealKarmaSha256Hasher` over the `cryptography` package) + `in_memory_karma_event_source.dart`; schema **v14** — `karma_events` (event_id PK UUID, seq, actor_hash SENSITIVE (64-hex blinded actor), action, delta, balance_after, occurred_at, prev_hash, self_hash — ZERO identity columns, no names/phones/emails/devices) + migration v14 (up: CREATE TABLE karma_events; down: DROP) + schema/migration tests updated (20 tables, fresh+incremental counts, v14 rollback); state: `KarmaState` (idle/loading/ready/error + balance + `KarmaTier` (DESIGN §4.3: citizen 0-49/contributor 50-99/validator 100-149/analyst 150-499/council 500+) + per-gate satisfaction map + `KarmaActivity` UI-safe rows (fixed action label + delta + timestamp — NO actor hash, NO event id) + GENERIC payload-free error) + `KarmaBloc` port + `LocalKarmaBloc` (monotonic-seq stale-drop, refresh → balance+tier+gates+activity, record → ledger append → refresh; repository failure → generic error; malformed actor → clean reject); UI: `lib/state/ui/karma_status_screen.dart` — FLAG_SECURE `KarmaStatusScreen` (❧ CIVIC COMMONS masthead, KARMA BALANCE card (score + tier label), PRIVILEGES UNLOCKED BY KARMA gate checklist (check-circle per satisfied gate + threshold), RECENT KARMA ACTIVITY feed (fixed labels + ±delta, green/red) — renders ONLY public integers + fixed labels, never the actor hash — wrapped in `SecureScreenWrapper`) + harness 6th **Karma** tab (ledger seeded to EXACTLY 247 — the same karma the identity screen's claim shows: 5× module +2 + 1× vetting +20 + 3× contribution +15 + 35× verified +5 − 1× rejected −3 = 247; 120-day tenure); 1728 Flutter tests total (73 new: 20 karma-action — every PRD delta/pillar/oneTime/label, wire round-trip, unknown wire/pillar throws; 18 karma-gate — thresholds, tenure-only-council, boundary-at-threshold, gate wire decode; 14 karma-calculation — weight ladder + sub-linear monotone + negative-karma floor, decay ladder + monotone + 12-month tolerance, lockstep — no-cluster-full-weight/3-new-cluster-dampens/established-not-dampened/outside-window-not-cluster/two-not-enough/order-deterministic/dampenedWeight; 23 local repo — empty-ledger/accumulate/negative/chain-links/UUID-v4-event-id/malformed-actor-rejected/hash-normalized/untouched-verifies/mutated-delta-detected/reordered-detected/removed-detected/COLD-RESTART-through-backing-store/record-tryParse-round-trip/delta-mismatch-rejected/unknown-action-rejected/non-hex-rejected/recomputeSelfHash; 2 event source — broadcast/emitted; 1 state tier; 6 state — tiers/ready/default/error/satisfied/UI-safe-projection; 7 bloc — loading→ready/record-appends/gates-score+tenure/council-tenure/error-generic-payload-free/malformed-actor-clean-reject; 5 screen widget — balance+tier+gates+activity/50-unlocks-probation/FLAG_SECURE-on-mount/empty-ledger/zero-PII-tree-scan; 5 security checkpoint — no-networking-imports/no-print/no-PII-literals/UI-never-references-actorHash/state-no-identity-fields/domain-no-decrypt-crypto; schema +1 table count/karma_events-zero-identity-columns + migration +1 v14-upgrade/v14-rollback + harness smoke 6-tab Karma destination), `flutter analyze` 0 issues, `dart format` clean, `flutter build web` compiles, affected suites green (karma + state + database schema/migration + harness smoke 589/589; the pre-existing serial-run `flutter_tester` RAM segfault artifact that intermittently kills ONE heavy Argon2id file remains — rotating file identity, passes standalone); SECURITY CHECKPOINT PASSED — karma events are APPEND-ONLY + AUDITABLE (no update/delete API; every event's SHA-256 selfHash covers the canonical bytes INCLUDING the prevHash link; `verifyIntegrity` recomputes the whole chain and detects a mutated delta, a reordered pair, and a removed event — each with a dedicated test), the ONLY actor identifier is the validated 64-hex blind hash (schema column flagged sensitive; malformed/phone-shaped actors REJECTED at record + read revalidation), the ledger carries ZERO identity columns (structural schema scan), the UI/state surface renders only the public integer balance + fixed labels (widget-tree scan + state structural scan prove no 64-hex hash, no phone literal, no actorHash reference), the karma domain never encrypts/decrypts or signs identity (only SHA-256 hashing — structural scan), and the surface imports no `dart:io`/http/WebSocket and never prints (static scans) with FLAG_SECURE on the status screen; found & fixed: the `_MemStore.getAll()` unmodifiable list broke the in-place sort (copied before sorting), the `KarmaState.tier` field had to become a computed getter (const constructors can't share a field initializer across redirects), the `const`-map gate test needed non-const construction, the widget test's `SecureFlagService` fake needed `isSecureFlagSupported`, the dynamic noSuchMethod probe for `actorHash` was replaced by a structural source scan (a missing getter THROWS — which is the proof), and the harness smoke test got the 6-tab Karma destination (seeded 247 balance + tier); next: Task 10.3 Karma Badge UI (server-side karma gates + NATS consumption remain deferred with the Phase-4 infra surface) |
| 2026-08-18 | 1.40 | Completed Task 10.1 - Unified Identity Layer (Phase 10, Cross-Pillar Systems) — one blind hash per person shared READ-ONLY across all four pillars (PRD §9.1); domain: `lib/identity/civic_pillar.dart` (`CivicPillar` enum — vault/ledger/warRoom/academy with strict wire decode + fixed display labels), `lib/identity/pillar_claim.dart` (`PillarClaim` username/deviceKeys/pinCode/karma + the per-pillar MINIMUM-claims allowlist `allowlistFor` — Vault: username+deviceKeys; Ledger: pinCode+karma; War Room/Academy: NOTHING beyond the hash), `lib/identity/pillar_claims.dart` (`PillarClaims` immutable per-pillar projection — `compose` ENFORCES the allowlist at construction (a claim the pillar cannot hold THROWS, so a full-profile projection is UNCONSTRUCTIBLE), validates the 64-hex blind hash, blinded non-PII `displayHandle` = `@citizen_` + first 6 hex — the full hash never renders), `lib/identity/unified_identity_service.dart` port (hasIdentity/blindHashId/claimsFor) + `lib/identity/local_unified_identity_service.dart` (composes minimum claims per pillar from the local secure-storage blind hash + the pillar claim sources); data: `lib/identity/pillar_claim_sources.dart` (`PillarClaimSources` port + `MemoryPillarClaimSources` fake + `composeAllClaims` building the full four-pillar minimum map — each pillar attribute read from its OWN pillar store, no profile is ever built); state: `IdentityVerificationState` (idle/loading/verified/noIdentity/error — carries ONLY the one-way hash + the per-pillar claims + the blinded handle + a GENERIC error, zero payload) + `IdentityVerificationBloc` port + `LocalIdentityVerificationBloc` (monotonic-seq stale-drop, source outage → generic payload-free error); UI: `lib/state/ui/identity_verification_screen.dart` — the one-time onboarding `IdentityVerificationScreen` (fixed `❧ CIVIC COMMONS` masthead, `IDENTITY VERIFIED` card with the blinded handle + the `ONE IDENTITY · FOUR PILLARS` minimum-claims table (🔒 The Vault: username + device keys; 📰 The Daily Ledger: pin code + karma; 🛡 The War Room: identity only; 🎓 The Academy: identity only) + CONTINUE host seam, NO IDENTITY YET prompt, generic VERIFICATION UNAVAILABLE error, FLAG_SECURE with the test seam) + harness 5th tab wiring (identity seeded with the shared peer hash + `@savitri` username + pin `800001` + karma 247); 1655 Flutter tests total (40 new: 20 pillar-claims domain — pillar wire round-trip/unknown-throws/display-labels, allowlist boundaries (Vault holds username+deviceKeys only / Ledger pinCode+karma only / War Room+Academy hold NOTHING — structural scan of every claim), projection enforcement (Vault projection carries username+deviceKeys / Vault CANNOT hold pinCode+karma (throws) / Ledger carries pin+karma / Ledger CANNOT hold username+deviceKeys / War Room+Academy projections hold nothing beyond the hash / War Room+Academy REJECT every extra claim (throws) / hash normalized to 64-hex lowercase / malformed hash throws / `displayHandle` is a blinded 6-hex fragment — never the full hash / equality), 11 unified service — no-identity-null/hasIdentity+hash/Vault-minimum-claims/Ledger-minimum-claims/War-Room-nothing-beyond-hash/Academy-nothing-beyond-hash/missing-sources-hold-no-claim/composeAllClaims-full-map, 4 bloc — verify-loading→verified-with-per-pillar-claims/no-identity/generic-payload-free-error/current-late-subscriber, 5 screen widget — verified-renders-blinded-handle+per-pillar-rows+FLAG_SECURE-on-mount/no-identity-prompt/generic-error/onVerified-host-seam/CONTINUE + zero-PII (no full 64-hex hash, no phone literals in the tree), 5 security checkpoint — identity-surface-no-networking/no-print/no-PII-literals/War-Room+Academy-claims-unreachable-structural/state-no-identity-fields + harness smoke 5-tab Identity destination), `flutter analyze` 0 issues, `dart format` clean, `flutter build web` compiles, affected suites 99/99 green (identity + verification bloc + screen + harness smoke; the pre-existing serial-run `flutter_tester` RAM segfault artifact that intermittently kills ONE heavy Argon2id file remains — rotating file identity, passes standalone); SECURITY CHECKPOINT PASSED — no pillar can hold a full profile BY CONSTRUCTION (the per-pillar allowlist is enforced at projection time + structural scans prove War Room/Academy can never hold a claim), the UI renders ONLY the blinded `@citizen_` fragment (widget-tree proof: no full 64-hex hash, no phone-shaped literals), the verification state carries only the one-way hash + per-pillar claims + generic error (no identity-typed fields — structural scan), and the identity surface imports no networking and never prints (static scans) with FLAG_SECURE on the onboarding screen; found & fixed: the test/harness blind-hash constants were 65 hex chars (trimmed to exactly 64 so `PillarClaims.compose` validates), the `error`/`verified` state constructors had to initialize ALL final fields explicitly, `_PillarClaimRow` dropped the `identity only` chip when a pillar's held-claims set was empty (fallback now returns it), the harness smoke test needs `FlutterSecureStorage.setMockInitialValues` for the new secure-storage-backed identity wiring (plugin-free test), and the widget test needed device keys in the fake sources to render the Vault claim chip; next: Task 10.2 Civic Karma Engine (server-side karma gates + NATS consumption remain deferred with the Phase-4 infra surface) |

| 2026-08-18 | 1.39 | Completed Task 9.6 - Cross-Pillar Study Group Matching (Phase 9, The Academy) — domain: new `lib/academy/domain/study_group.dart` — `StudyPillar` (academy/ledger/warRoom strict wire decode), `StudyTopicRef` (OPAQUE cross-pillar ref: pillar + topic id validated as UUID v4 OR a `[a-z0-9_]` slug REQUIRING at least one lowercase letter — an all-digit phone-shaped id is REJECTED, SECURITY CHECKPOINT 9.6), `StudyGroup` (validated UUID v4 group id + anchor module UUID + public title + locale + coarse civic pinCode + cross-pillar topics + capacity (>=2) + participantCount bounds + createdAt; `hasCapacity`; zero identity fields), `StudyGroupMember` (UUID membership id + group id + blinded handle + initiator flag + joinedAt), `StudyGroupHandle` (DETERMINISTIC pseudonymous `SG-####` — FNV-1a 32-bit of the anchor module UUID → 4 hex, the SAME derivation as the `SA-####` sandbox handle so a learner is consistent across the Academy's community surfaces; zero identity), `StudyGroupInterest`/`StudyGroupMatch` + pure `StudyGroupMatcher` (MASTER_PLAN §9.6 pin-code-based learner matching: anchor module +5, additional cross-pillar topic +3, exact pin +4 / same district +2 / same region +1, locale +2; FULL groups hard-filtered; equal scores break by groupId ASCENDING — deterministic, no randomness, every device converges on the identical ranking) + `StudyGroupRepository` port (listGroups/getGroup/listMembers/createGroup/joinGroup); `study_group_records.dart` (`StudyGroupRecord`/`StudyGroupMemberRecord`); `study_group_wire_codec.dart` — `StudyGroupWireFrame` `v:1` JSON frame (group/module UUIDs + title + locale + pin_code + topic list `pillar:topicId` + capacity + blinded SG handle + is_initiator/is_join + createdAtMs — ZERO identity fields, strict version/handle/pin/topic rejection); schema **v13** — `study_groups` (group_id PK, module_id, title, locale, pin_code SENSITIVE (coarse civic scope — same signal as the Ledger feed), topics TEXT, capacity, participant_count, created_at — ZERO identity columns, no handles) + `study_group_members` (member_id PK, group_id, member_handle (blinded SG-####), is_initiator, joined_at — ZERO identity columns) + migration v13 (up: 2 CREATE TABLEs; down: 2 DROPs) + row codecs in `sqlite_entity_store.dart` (`studyGroupRecordToRow/FromRow` — topics wire list `pillar:topicId;...` strictly re-validated per entry + `StudyGroup.parse` on READ; `studyGroupMemberRecordToRow/FromRow` — `StudyGroupMember.parse` re-validation); data: `LocalStudyGroupRepository` (production SQLCipher-backed `EntityStore`s — OFFLINE-FIRST: the initiator member row lands BEFORE the group aggregate on create; joinGroup is IDEMPOTENT (re-join never double-counts), rejects full/unknown groups, increments participant_count; listGroups newest-created-first + module-scoped; listMembers initiator-first-then-join-order), `InMemoryStudyGroupRepository` (harness/tests — same semantics + `seedGroup` for demo data), `QueueStudyGroupSink` (wraps the local repo: local-first write → then `SyncQueueRepository.enqueue` with the serialized frame — the queue SEALS it with the AES-256-GCM queue cipher BEFORE storage (create = SyncOperationType.create, join = update), byte-level no-plaintext proof); state: `StudyGroupState` (idle/loading/ready/failure + module-anchored groups + local memberships + pinCode/locale + query + filteredGroups + deterministic `matches` + `participantHandle` + `hasJoined` + GENERIC error — UI-safe projection, zero identity) + `StudyGroupBloc` port + `LocalStudyGroupBloc` (monotonic-seq stale-drop, search filter, `_match` runs the deterministic matcher against the learner's coarse pin scope + locale + module interest, create/join refresh the snapshot, graceful generic failure); UI: `AcademyStudyGroupScreen` (module-anchored browse + MATCHES section (SCORE + participant count + JOIN GROUP) + live title search + app-bar NEW + CREATE GROUP sheet (public title/capacity/topic slug/optional Ledger+War Room links) + empty/failure states, blinded `You study as SG-#### · scope 800001` line, `_topicLabel` renders `ACAD-`/`LEDGER-`/`WAR-` + 8-hex shortened opaque id — never the full UUID, FLAG_SECURE) + the module screen's STUDY GROUPS section is now LIVE (injectable `studyGroupBloc` + `studyGroupPinCode` seams open the matching screen; deferred frame remains without them) + harness Academy tab wiring (in-memory repo seeded with a demo `Civic Rights Study Circle` for the civics module); 1615 Flutter tests total (61 new: 20 study-group domain — topic-ref UUID/slug/phone-shaped-id-rejected/mixed-case-rejected/length/parse-throws/pillar-wire-round-trip, group validation matrix (bad ids/title/locale/pin/capacity/counts/empty-topics + equality), handle determinism + SG-#### shape + isValid, member validation, matcher — anchor-scores-highest/pin-exact-beats-district-beats-region [11,9,8]/full-hard-filter/locale-adds-points/cross-pillar-refs-add/tie-break-by-groupId-ascending-deterministic-repeated/irrelevant-dropped, 8 wire codec — create-round-trip/join-round-trip/canonical-snake_case-keys-zero-identity/version-reject/non-UUID-reject/handle+pin-reject/topic-reject/non-object-reject, 7 bloc — start-loads+matches/failure-generic/retry-recovers/search-filters/create-refreshes/join-updates/late-subscriber-stream, 7 local repo — create+initiator/join-increments+idempotent/full-rejects/unknown-rejects/module-scope+newest-first/read-path-parse-revalidation/COLD-RESTART-through-backing-store, 2 queue sink — create-local-first+SEALED-frame+byte-level-proof/join-enqueues-sealed-JOIN, 5 matching-screen widget — blinded-handle+scope+SCORE-11-match-cards/FLAG_SECURE-on-mount/join-flows-through-bloc/search-filters-list-create-sheet-through-bloc, 1 module-screen — STUDY GROUPS entry opens the matching screen when bloc+pin injected, 7 security checkpoint — 9.6-surface-no-networking/no-print/no-PII-literals + SG-####-only-participant-shape + state-no-identity-fields + v13-schema-zero-identity-columns-structural + migration-v13-no-identity, 2 schema — study_groups-shape+pin-sensitive/study_group_members-shape, 2 migration — v12→v13-upgrade/v13-rollback-drops-both), `flutter analyze` 0 issues, `dart format` clean, `flutter build web` compiles, affected suites 312/312 green (academy + database + module-screen + matching-screen + syllabus + masthead + harness smoke; the pre-existing serial-run `flutter_tester` subprocess segfault under RAM pressure that intermittently kills ONE heavy crypto file remains a known runner artifact — rotating file identity, passes standalone); SECURITY CHECKPOINT PASSED — study group mutations are encrypted before sync (the queue SEALS the frame with the AES-256-GCM queue cipher; byte-level proof the stored payload never equals the plaintext frame), the ONLY participant shape is the deterministic `SG-####` handle (structural proof: no email/phone/name member on the handle type, no identity field in group state, `StudyTopicRef` REJECTS all-digit phone-shaped topic ids), the coarse pin scope is a SENSITIVE column (same signal as the Ledger feed, never precise), the v13 tables declare ZERO identity columns (structural schema scans + migration scan), and the group surface imports no `dart:io`/http/WebSocket and never prints (static scans) with FLAG_SECURE on the matching + create screens; **found & fixed during development:** the domain's `PinCode` helper name collided with the Ledger/geo `PinCode` (renamed `StudyPinCode`), the slug regex initially accepted all-digit (phone-shaped) topic ids — now REQUIRES a lowercase letter; the test helper's default topics derived from the passed (possibly malformed) moduleId so invalid-module cases threw before reaching `tryParse` (default topics now derive from the fixed `_m1`); the pin-proximity test's third pin `801001` still shared the `80` district prefix (used `810001` for the +1 region case); the matcher score assertions had to include the anchor (+5) + locale (+2) components ([11,9,8] not [4,2,1]); the bloc join test started on `_m2` while the seeded group was anchored on `_m1` (module-scoped snapshot filtered it out); broadcast-stream delivery needs a microtask drain for the late-subscriber test; the matches section overflowed the 600px test viewport (height-bounded + scrollable); CREATE GROUP was only reachable from the empty state (app-bar NEW action added — always reachable); the search test asserted `findsNothing` on a title that still legitimately appears in the UNFILTERED matches section (assertion now targets the filtered group LIST via the `Tap to join` marker); `flutter analyze` caught an unused `_m2` constant after the join-test fix; the perl bulk-edit of migration counts introduced a stray `(',` artifact (cleaned); next: Phase 9 completion audit / Task 10.1 Unified Identity Layer (Vault E2EE group chat + HLS playback + ABR + Meilisearch remain deferred) |
| 2026-08-17 | 1.38 | Completed Task 9.5 - Sandbox Wiki System (Phase 9, The Academy) — domain: new `lib/academy/domain/sandbox_wiki.dart` — `SandboxPage`/`SandboxRevision` (validated UUID v4 page ids via the `tryParse`/`parse` convention, module-scoped, public titles, locale tags, revision count + timestamps), `SandboxAuthorHandle` (DETERMINISTIC pseudonymous `SA-####` handle — FNV-1a 32-bit hash of the module UUID → 4 hex, `isValid` regex — zero identity, zero PII, no email/phone/name fields expressible), `SandboxLineDiff` (deterministic LCS line diff → `+A −R` summary; EMPTY text = zero lines so `diff(x,'')` is a pure removal and `diff('','')` is clean), `SandboxWikiRepository` port (listPages/getPage/listRevisions/submitRevision/revertToRevision); `sandbox_wiki_records.dart` — `SandboxPageRecord`/`SandboxRevisionRecord` (revision ids, `prev_revision_id` chain, `SA-####` author); `sandbox_wiki_wire_codec.dart` — `SandboxRevisionWireFrame` `v:1` JSON frame (page/module UUIDs + title + body + `SA-####` handle + createdAtMs — ZERO identity fields, strict version + bounds rejection); schema **v12** — `sandbox_pages` (page_id PK, module_id, title, locale, revision_count, updated_at — public content only, ZERO sensitive) + `sandbox_revisions` (revision_id PK, page_id, body_markdown SENSITIVE (community UGC, encrypted-partition contract), author_handle, created_at, prev_revision_id) + migration v12 (up: 2 CREATE TABLEs; down: 2 DROPs) + row codecs in `sqlite_entity_store.dart` (`sandboxPageToRow/FromRow`, `sandboxRevisionToRow/FromRow` — read path RE-VALIDATES every cached row through `parse`); data: `LocalSandboxWikiRepository` (production SQLCipher-backed `EntityStore`s — OFFLINE-FIRST: the revision row + page aggregate land in the encrypted partition before anything else; `_createPage`/`submitRevision`/`revertToRevision` all re-read through `getPage` so the returned value is parse-validated), `InMemorySandboxWikiRepository` (harness/tests, same semantics), `QueueSandboxRevisionSink` (wraps the local repo: local-first write → then `SyncQueueRepository.enqueue` with the serialized frame — the queue SEALS it with the AES-256-GCM queue cipher BEFORE storage, so sandbox edits are encrypted before sync; the frame carries only UUIDs + public fields + the SA handle); state: `SandboxWikiState` (idle/loading/ready/failure + module-scoped pages + filteredPages + query + selectedPage + revisions + draftBody + `authorHandle` + GENERIC error — UI-safe projection, zero identity) + `SandboxWikiBloc` port + `LocalSandboxWikiBloc` (monotonic-seq stale-drop, search filter, draft lifecycle, submitDraft consumes the draft AND refreshes the open page's revision history, revertTo appends + reloads); UI: `AcademySandboxWikiScreen` (module-scoped page list + live title search + NEW PAGE entry + empty/failure states, `SA-####` handle line, FLAG_SECURE) + `AcademySandboxPageScreen` (read-only Markdown body via `AcademySandboxMarkdownView` (dependency-free mono renderer), REVISION HISTORY with `SA-####` + timestamp + `+A −R` diff counts + CURRENT marker + REVERT/EDIT, FLAG_SECURE) + `AcademySandboxEditScreen` (plain Markdown source editor — BOLD/HEADING/LINK syntax chips around the selection, PREVIEW toggle, SAVE with empty-title snackbar gate, editing-as-`SA-####` line, direct-entry pre-fill via the bloc, FLAG_SECURE) + the module screen's SANDBOX section is now LIVE (injectable `sandboxWikiBloc` seam opens the wiki; deferred frame remains without it) + harness Academy tab wiring; 1554 Flutter tests total (71 new: 15 sandbox domain — page/revision validation matrix + handle determinism + isValid + zero-identity structural scan + diff adds/removes/clean/replacement/deterministic/empty-text, 6 wire codec — round-trip + zero-identity fields + strict version/bounds rejection, 11 local repo — create-page/append-revision/prev-chain/module-scope+newest-first/revert-appends/cold-restart-through-backing-store/read-path-parse-revalidation/forged-row-throws, 3 queue sink — local-first+sealed/byte-level-no-plaintext-envelope/round-trip-open, 8 bloc — load/search/open/submit-create/submit-append/refresh-after-submit/revert/store-outage-generic/security-state-scan, 5 wiki screen widget — render/search-filters/page-nav/failure-retry/FLAG_SECURE+zero-PII, 4 page screen widget — body+history+diff counts/revert-appends/edit-opens/FLAG_SECURE+zero-PII, 7 edit screen widget — handle+empty-draft/pre-fill-latest-body/syntax-chips-wrap-selection/preview-toggle/empty-title-snackbar/save-creates-pops/FLAG_SECURE+zero-PII, 1 module-screen — SANDBOX entry opens the wiki when the bloc is injected, 7 security checkpoint — sandbox-tables-zero-identity-columns structural + migration-v12-no-identity + wiki-files-no-networking/no-print/no-PII-literals + SA-####-only-author-shape + wiki-state-no-identity-fields, 2 schema — sandbox_pages/sandbox_revisions table shapes with body-markdown-sensitive flag, 2 migration — v11→v12-upgrade/v12-rollback-drops-both), `flutter analyze` 0 issues, `flutter build web` compiles, affected suites 274/274 green; SECURITY CHECKPOINT PASSED — sandbox edits are encrypted before sync (the queue SEALS the frame with the AES-256-GCM queue cipher; the queue never persists plaintext wiki bodies — byte-level proof), all persisted wiki content lives in the encrypted partition (`body_markdown` is a SENSITIVE column by construction + SQLCipher), the ONLY author shape is the deterministic `SA-####` handle (structural proof: no email/phone/name member on the handle type, no identity field in wiki state), the v12 tables declare ZERO identity columns (structural schema scans), and the wiki surface imports no `dart:io`/http/WebSocket and never prints (static scans) with FLAG_SECURE wrappers on all three screens; **found & fixed during development:** `''.split('\n')` yields ONE empty line so `diff(x,'')` reported a phantom addition — empty texts now contribute ZERO lines (pure all-lines removal/addition, `diff('','')` clean); `LocalSandboxWikiBloc.submitDraft` appended the revision but the detail screen renders the history from STATE — the bloc now refreshes the open page's revision history after submit; the editor read `state.revisions` which is empty unless the page was opened first — direct entry now loads the latest body through `bloc.openPage` before pre-filling; a collapsed-cursor BOLD insert legitimately emits `****` (adjacent open+close tags) — the widget test now wraps a real selection; widget-route push/pop needs TWO pumps (the route's disposal frame) — navigation tests pump twice or `pumpAndSettle`; the security checkpoint's `'SA-'` literal check failed because the handle is built via interpolation — the check now asserts the `'SA-` prefix pattern; Dart <3.6 forbids `60_000` digit separators in the repo test — replaced with `60000`; next: Task 9.6 Cross-Pillar Study Group Matching (HLS playback + ABR + Meilisearch remain deferred) |
| 2026-08-17 | 1.37 | Completed Task 9.4 - Offline Module Caching (Phase 9, The Academy) — domain: new `lib/academy/domain/module_asset_manifest.dart` — `ModuleAssetKind` (video/transcript, strict wire names) + `ModuleAsset` (kind + OPAQUE non-PII ref + sizeBytes) + `ModuleAssetManifest` (deterministic `generateFor(module, sizes)` — video asset always from the module's opaque content ref, derived `$ref/transcript` asset only when the size table provides one, `totalSizeBytes` offline budget, value equality — identical inputs ALWAYS produce the identical manifest); new `lib/academy/domain/offline_module_cache.dart` — `OfflineCacheStatus` (not_downloaded/queued/downloading/downloaded/failed, strict wire decode — unknown statuses throw so a corrupt row cannot masquerade) + `ModuleCacheEntry` (moduleId/status/totalBytes/cachedBytes/downloadedAt ONLY — zero identity, zero content) + the `OfflineModuleCache` port (cacheStatusFor/allCacheEntries/totalCachedBytes/cacheForOffline/removeFromOffline/readCachedContent — cache HIT = sealed payload, MISS = null; keys are ALWAYS validated UUID v4 module ids) + `ModuleDownloader` port (returns SEALED content — the Academy tree's ONLY download seam; production impl fetches via presigned MinIO/R2/Bunny in Phase-9 content delivery) + `BackgroundDownloadDispatcher` port + `AcademyStoragePolicy` (pure storage-budget math: `wouldExceed`/`exceeds` over a 200 MB default limit + KB/MB `formatBytes`); new `lib/academy/domain/module_cache_record.dart` (`ModuleCacheRecord` — the persisted row value: UUID module id + status wire + sizes + timestamp + the SEALED payload, `copyWith` + `toEntry`); new `lib/academy/domain/offline_playback.dart` — `OfflinePlaybackDecision` + pure `OfflinePlaybackResolver.resolve(source, cache)` — NEVER claims offline video for a YouTube privacy embed (network-only BY CONSTRUCTION, Task 9.3 privacy boundary), HLS becomes video-available-offline once cached (rendering lands with the Phase-9 media integration — this is the availability contract), cached content is always readable offline; schema: **v11** — `module_cache` DbTable (module_id TEXT PK, status, total_bytes, cached_bytes, downloaded_at, sealed_payload BLOB **sensitive**, cached_at — ZERO identity columns) + migration 11 (up: CREATE TABLE; down: DROP) + `moduleCacheRecordToRow/FromRow` codecs in `sqlite_entity_store.dart` whose READ path re-validates the UUID module id + the status wire (forged non-UUID key / unknown status throw at read); data: `academy_asset_catalog.dart` (`AcademyAssetCatalog` — deterministic nominal size table keyed by the seed syllabus' opaque content refs; `manifestFor(module)` + `manifestsFor(syllabus)`), `in_memory_module_downloader.dart` (`SimulatedModuleDownloader` — deterministic opaque plaintext sized to the manifest budget derived from the UUID's code units, SEALED with the injected AES-256-GCM `QueuePayloadCipher`, plaintext buffer ZEROED in `finally` even on failure — memory hygiene), `in_memory_module_download_dispatcher.dart` (`InProcessModuleDownloadDispatcher` — records scheduled ids, runs the handler immediately for deterministic tests/harness, mirrors the `WorkmanagerScheduler` seam pattern), `local_offline_module_cache.dart` (`LocalOfflineModuleCache` — SQLCipher-backed via `EntityStore<ModuleCacheRecord>`: offline-first QUEUED row lands before the background task is scheduled, `processQueuedDownload` reads the persisted QUEUED row (COLD-RESTART-safe — a queued row left by a crash is re-processable), transitions queued → downloading → downloaded (sealed payload + sizes + timestamps) or → failed, idempotent remove/cache/process, `totalCachedBytes` sums only downloaded rows), `in_memory_offline_module_cache.dart` (same semantics, map-backed, for the harness), plus `lib/sync/data/workmanager_module_download_dispatcher.dart` (`WorkmanagerModuleDownloadDispatcher` — production one-off WorkManager task per module id, compile-verified exactly like `WorkmanagerScheduler`; the actual download logic lives in the cache and is fully unit-tested); state: `academy_offline_state.dart` (`AcademyOfflinePhase` + `AcademyOfflineState` — entries map keyed by UUID module ids + totalCachedBytes + storageWarning + generic error string, `entryFor`/`copyWith`), `academy_offline_bloc.dart` port (start/retry/cacheModuleForOffline/removeModuleFromOffline/current/close), `local_academy_offline_bloc.dart` (`LocalAcademyOfflineBloc` — loads the local cache snapshot with the monotonic-seq stale-load guard, mutations are best-effort with graceful degradation to the generic failure state, `current` for late subscribers); UI: new `lib/state/ui/academy_offline_section.dart` — `AcademyOfflineSection` (OFFLINE COPY frame with a status chip (OFFLINE/PREPARING/SAVED/FAILED) + the manifest budget + `DOWNLOAD FOR OFFLINE`/REMOVE/RETRY controls, the STORAGE WARNING dialog gate when a download would exceed the 200 MB budget (CANCEL aborts, DOWNLOAD proceeds) + a persistent over-budget banner, the deterministic offline-playback caption from `OfflinePlaybackResolver`, ALL state consumed through the injected bloc — never the cache repository, zero identity in the tree) + `AcademyModuleScreen` gains the optional `offlineBloc` seam (section renders under the VIDEO ROOM when the composition root injects it; `start()` in initState with the late-subscribe `current` fallback); harness: `main.dart` Academy tab now injects a shared `LocalAcademyOfflineBloc` over an in-memory cache SEALED WITH THE REAL AES-256-GCM queue cipher — `flutter run -d chrome/linux` exercises the full download flow; 1483 Flutter tests total (51 new: 9 manifest domain — video-asset-from-content-ref+size-table/derived-transcript-only-when-provided/transcript-omitted-when-absent/unknown-ref-deterministic-fallback/total-sum/determinism/zero-identity-no-URLs + status-wire-round-trip/unknown-wire-throws + kind-wire-round-trip/unknown-kind-throws, 8 storage+playback domain — wouldExceed-boundary/exceeds-boundary/formatBytes-KB-MB/YouTube-embed-never-video-offline-privacy-boundary/HLS-cached-video-available/uncached-nothing/queued-failed-never-available/no-video-cached-content-reads, 13 data-layer — cacheForOffline-queued+scheduled/download-seals+persists+cold-restart-status/BYTE-LEVEL-real-AES-proof-stored-never-equals-plaintext-no-16-byte-window-survives+round-trip-exact/MEMORY-HYGIENE-plaintext-buffer-zeroed-after-seal/failed-then-retry-recovers/remove-idempotent/totalCachedBytes-downloaded-only/cacheForOffline-idempotent-when-ready/COLD-RESTART-statuses-restore+queued-row-re-processable/processQueuedDownload-noop-for-non-queued + codec-round-trip-with-sealed-payload/non-UUID-key-throws/unknown-status-throws, 6 bloc — start-loads-snapshot/cache-drives-to-ready/remove-clears/storageWarning-flips-over-budget/store-outage-generic-failure/state-carries-UUID-keys-only, 6 section widget — initial-not-downloaded+budget/DOWNLOAD-to-READY-then-REMOVE/STORAGE-WARNING-gate-CANCEL-aborts-DOWNLOAD-proceeds+persistent-banner/failed-then-RETRY-recovers/PREPARING-in-flight/no-full-UUID-tree-scan, 1 module-screen integration — real screen + offline bloc: scroll → DOWNLOAD → Offline copy ready + REMOVE, 5 Task-9.4 security checkpoint — 9.4-surface-no-networking-no-print-no-PII-literals scans (incl. the workmanager seam)/module_cache-schema-zero-identity-columns-structural/cache-API-no-identity-key-type-structural, 2 migration — v10→v11-CREATE/11-rollback + 1 schema — module_cache-shape+sealed_payload-sensitive), `flutter analyze` 0 issues, `dart format` clean, `flutter build web` compiles (harness with the offline wiring), affected suites 175/175 green (131 academy incl. new domain/data/bloc + 43 migration/schema/security + 7 module screen + 6 section widget + navigation integration + harness smoke; the pre-existing serial-run `flutter_tester` subprocess segfault under RAM pressure that intermittently kills ONE heavy crypto file remains a known runner artifact — rotating file identity, passes standalone); SECURITY CHECKPOINT PASSED — cache keys are validated UUID v4 module ids only (compiler-enforced + structural scans + the schema test proving the v11 table declares ZERO identity columns), cached content lives in the ENCRYPTED partition (SQLCipher `module_cache` table + the sealed_payload column flagged sensitive, BYTE-LEVEL proof with the real AES-256-GCM cipher that no plaintext byte window survives in the stored payload + the round trip is exact), the plaintext download buffer is ZEROED in memory after sealing (reference-captured wipe proof), the download seam is the ONLY place a network fetch can happen (the Academy tree imports zero networking — in-process + WorkManager dispatchers both flow through the port), the UI tree renders only statuses + sizes (no UUID, no content, no identity — widget-tree scan), and nothing prints in the new surface (static scans); **found & fixed during development:** the `async =>` arrow gave `fold` a `FutureOr<int>` context type in the in-memory cache (`totalCachedBytes` rewritten as a loop), the harness's dispatcher handler needed a `late final` cache so the closure could reference the variable it drives, the `name` PII scan false-positived on `wireName` (exact-match check), the module-screen integration tap missed because the section sits below the 16:9 player (scroll to the button + `ensureVisible`), and `crypto` (the standalone package) is NOT a dependency — the deterministic plaintext seed derives from the UUID's code units instead (no new dependency); next: Task 9.5 Sandbox Wiki System (HLS playback + ABR + Meilisearch remain deferred) |
| 2026-08-16 | 1.36 | Completed Task 9.3 - Video Room with Privacy-Enhanced Embeds (Phase 9, The Academy) — domain: new `lib/academy/domain/academy_video.dart` — `YouTubeVideoId` (STRICT 11-char `[A-Za-z0-9_-]` validation — full URLs/embed paths/query strings rejected), `VideoRoomSource` sealed hierarchy (`YoutubePrivacySource` validated-11-char-id source; `HlsSource` opaque non-PII manifest ref for the proprietary Bunny.net/R2 slot), `PrivacyEmbedUrl.forSource` — the ONLY URL producer in the whole video surface, hard-coded to `https://www.youtube-nocookie.com/embed/{id}?rel=0&modestbranding=1&controls=1&fs=1` (rel=0 = no recommendations; NO autoplay/list/shorts/si params structurally possible — asserted by test; returns null for HLS so the client can never mint a signed URL), plus the `VideoRoomSourceResolver` + `VideoEmbedLauncher` PORTS (the Academy tree depends only on these seams — zero video SDK imports by construction); data: `in_memory_video_room_source_resolver.dart` — deterministic `InMemoryVideoRoomSourceResolver` (opaque content refs → curated validated ids; unknown/URL-shaped refs → null no-video state) + `NoopVideoEmbedLauncher` (records the URL, opens no network surface — harness/test seam); UI: new `lib/state/ui/academy_video_room_player.dart` — `VideoRoomPlayer` (16:9 privacy-embed frame with a single explicit PLAY control → launches the exact nocookie URL through the injected launcher seam, SINGLE-launch state after play, NEVER auto-launches on build; graceful no-video + explicit HLS-deferred states; renders only an 8-char video CODE — never the full id, zero PII) + `AcademyModuleScreen` now mounts the real player (replacing the Task 9.1 placeholder) resolving the module's opaque content ref through the resolver (GUTENBERG ARCHIVE + SANDBOX remain deferred community surfaces); 1432 Flutter tests total (28 new: 15 video domain — id-validation matrix/length+charset/URL-and-query-rejection/`tryParse`-null-not-throw/assert-reject/resolver-determinism/no-video-for-unknown/URL-builder-nocookie-host/SECURITY-no-autoplay-list-shorts-si/rel-0+modestbranding+only-4-allowlisted-params/HLS-returns-null/launcher-records-only; 7 player widget — header+badge/render/no-auto-launch/play-launches-EXACT-privacy-URL/single-launch/no-video-state/HLS-deferred-state/8-char-code-only-no-PII-tree/resolver-seam-end-to-end; 1 module-screen integration — real resolver + recording launcher: tap PLAY → nocookie URL + no-autoplay + launched state; 5 Task-9.3 security checkpoint — video-surface-no-networking-no-SDK-imports/no-print/no-PII-literals/only-URL-literal-is-nocookie-host-structural-scan/no-recommendation-surfaces-in-widget-tree-LV-GV-PageView-thumb-up-absent), `flutter analyze` 0 issues, `flutter build web` compiles, affected suites 102/102 green (56 academy + 30 module/player + navigation integration + masthead + harness smoke; the pre-existing serial-run `flutter_tester` subprocess segfault under RAM pressure that intermittently kills ONE heavy crypto file remains a known runner artifact — rotating file identity, passes standalone); SECURITY CHECKPOINT PASSED — the privacy-embed boundary holds BY CONSTRUCTION: the Academy video surface imports ZERO video SDKs/webview/networking packages (structural import scan), the ONLY URL that can ever be produced is the hard-coded `youtube-nocookie.com` privacy host with exactly the four allowlisted params (rel=0/modestbranding/controls/fs — no autoplay, no list, no shorts, no si, asserted at the URL level), no recommendation surface exists in the player widget tree (no list/grid/page-view/thumb-up — structural), playback NEVER auto-starts (explicit play, single-launch), the resolver accepts only opaque refs → validated 11-char ids (URL-shaped refs → null), and the player renders only an 8-char video code — zero identity/PII (widget-tree scan); **found & fixed during development:** `'not-a-video'` is EXACTLY 11 chars so it passed validation (test used a 12-char string), `YoutubePrivacySource` has no value-equality so the determinism test compared videoIds not instances, `testWidgets` vs `test` in the player suite (7 widgets need the tester), the const constructor could not validate at runtime (non-const + assert), the new 16:9 player pushed GUTENBERG ARCHIVE/SANDBOX below the lazy ListView's build window (tests scroll), the launched-state frame removes the InkWell so the second-tap single-launch assertion just verifies the state flip, and the naive substring URL scan matched the files' own doc comments (structural scan strips `///`/`//` lines and scans code literals only); next: Task 9.4 Offline Module Caching (HLS playback + ABR + Meilisearch remain deferred) |
| 2026-08-16 | 1.35 | Completed Task 9.2 - Syllabus Tree Implementation (Phase 9, The Academy; data & persistence layer per the task scope) — domain: new `lib/academy/domain/academy_progress_record.dart` — `AcademyProgressRecord` (single validated UUID v4 module id; presence = completed; zero identity, no timestamp/device/telemetry column — the `UuidV4` guard on `AcademyModule.parse` rejects anything else before a module can exist); schema: **v10** — three new `DbTable`s in `AppSchema` (`academy_domains`: domain_id/title/locale; `academy_modules`: module_id/domain_id/title/duration_minutes/locale/content_ref; `academy_progress`: module_id PK only) — all ZERO identity columns, ZERO sensitive columns (public course content + UUID keys; nothing here is PII), `currentVersion` bumped 9→10; migration v10 (up: 3 CREATE TABLEs; down: 3 DROPs in reverse) + row codecs in `sqlite_entity_store.dart` (`academyDomainToRow/FromRow`, `academyModuleToRow/FromRow`, `academyProgressRecordToRow/FromRow` — the read path RE-VALIDATES every cached row through `parse`, so a forged non-UUID module row throws at read time); data: new `lib/academy/data/local_academy_syllabus_repository.dart` — `LocalAcademySyllabusRepository` (production SQLCipher-backed `EntityStore<AcademyDomain>` + `EntityStore<AcademyModule>`, local-cache-first: seeds the bundled deterministic `AcademySeed.syllabus` into the EMPTY cache exactly once per process, NEVER overwrites a pre-populated/partial cache, deterministic across restarts, serves the cached snapshot with zero network) + the in-memory scaffold repository now shares the SAME canonical `AcademySeed` (single source of truth) + new `lib/academy/data/local_academy_progress_store.dart` — `LocalAcademyProgressStore` (production SQLCipher-backed `EntityStore<AcademyProgressRecord>`: idempotent INSERT-OR-REPLACE on complete, no-op DELETE on incomplete, cold-restart-surviving completed set); 1404 Flutter tests total (25 new: 12 repository/store unit — seed-on-empty/local-cache-first-second-load-reads-cache-not-seed/never-overwrites-pre-populated/injected-seed/deterministic-across-restarts/row-codec-round-trip+forged-module-rejected-at-read/API-takes-no-identity-inputs + progress round-trip/idempotent-complete/idempotent-incomplete/cold-restart-through-backing-store/keys-UUID-only, 6 data-layer security checkpoint — no-networking/no-print/no-PII-literals in the new data files + schema-declares-zero-identity-columns structural scan + migration-v10-no-identity scan + progress-record-single-UUID-field, 4 schema — fourteen-tables + three academy table shapes with zero identity/sensitive columns + academy-tables-zero-sensitive registry, 3 migration — v9→v10-upgrade/v8-through-v9+v10/4-statement/v10-rollback-drops-all-three), `flutter analyze` 0 issues, affected suites 148/148 green (56 academy + 47 database incl. the Argon2id key service in isolation + 40 migration/schema/security + harness smoke; the pre-existing serial-run `flutter_tester` subprocess segfault under RAM pressure that intermittently kills ONE heavy crypto file remains a known runner artifact — rotating file identity, passes standalone); SECURITY CHECKPOINT PASSED — the syllabus is served from the LOCAL cache with zero network in the read path (local-cache-first repository + structural proof), schema v10 declares ZERO identity and ZERO sensitive columns across all three academy tables (the syllabus + progress are public course content / UUID keys — nothing to encrypt is PII), progress keys are validated UUID v4 module ids only (compiler-enforced + structural scans), the new data files import no `dart:io`/http/WebSocket and never print (static scans), and the UI keeps its FLAG_SECURE wrappers unchanged; **found & fixed during development:** the v10 migration carries THREE CREATE TABLEs so every incremental-upgrade statement count in `migration_test.dart` shifted by +2 (recomputed v1→v10 = 12, v2→v10 = 11, … v9→v10 = 3), the v8→v10 path must run v9 + all three v10 statements (4 total — first draft only asserted 2), the typed in-memory `EntityStore` cannot hold a forged module so the corrupt-row proof moved to the row codec (`academyModuleFromRow` re-validates through `parse`), and the canonical seed was extracted to `AcademySeed` so the in-memory scaffold and the production local cache serve byte-identical content; next: Task 9.3 Video Room with Privacy-Enhanced Embeds (Category/Subject mid-levels + expand/collapse UI + Meilisearch search remain deferred) |
| 2026-08-16 | 1.34 | Completed Task 9.1 - Academy UI Foundation (Phase 9, The Academy) — domain: new `lib/academy/domain/academy_progress.dart` — `AcademyProgress` pure projections (`domainFraction`/`domainPercent`/`overallFraction`/`overallPercent`/`orphanModuleIds`): deterministic completion percentages over the syllabus tree + the learner's completed-module-id set; ZERO identity inputs by construction (the only keys are UUID v4 module ids — the `UuidV4` guard on `AcademyModule.parse` rejects anything else before a module can exist), division-by-zero-safe on empty lists, stale/forged keys surfaced deterministically via `orphanModuleIds`; state: `AcademyBloc` port + `LocalAcademyBloc` gain a `current` getter (non-stream read for navigation wiring, mirroring `LocalLedgerComposeBloc.current` — the syllabus screen's `onModuleTap` reads the latest state to resolve domain titles without racing the broadcast stream); UI: new `lib/state/ui/academy_syllabus_screen.dart` — `AcademySyllabusScreen` (FLAG_SECURE masthead + MY PROGRESS section with overall bar + per-domain rows, BROWSE BY DOMAIN 2-col grid with module count + completion bar, drill into a domain → module list with duration/locale/completion markers; `start()` re-emit on mount for late broadcast subscribers — the same late-subscribe treatment as WarRoom/Ledger screens; failure view with Retry) + new `lib/state/ui/academy_module_screen.dart` — `AcademyModuleScreen` (back button, `❧ THE ACADEMY` app bar + 8-hex UUID module code — never the full UUID, breadcrumb `Domain › Module`, metadata chips (duration/locale/MOD-code), offline-first completion toggle persisted through the bloc's progress store with late-subscribe `current` fallback, and the three section frames — VIDEO ROOM / GUTENBERG ARCHIVE / SANDBOX — as explicit `Task 9.3` placeholders: the screen imports NO video package so the privacy-enhanced-embed boundary holds BY CONSTRUCTION until the real player lands); `AcademyTheme` gains the `surface` token; harness: `lib/main.dart` Academy tab now routes `AcademySyllabusScreen` → `AcademyModuleScreen` through its own Navigator (resolving the domain title from `bloc.current`); 1379 Flutter tests total (24 new: 7 progress domain — empty/full/partial-33%/over-complete-clamp/empty-div0/orphan-forged-stale/pure-function-structural-scan, 6 syllabus widget — renders-progress+grid/drill-into-domain/flags-late-subscribe-loading/retry-recovers/FLAG_SECURE-retained/domain-card-vs-progress-row-ambiguity, 5 module widget — breadcrumb+chips+8-hex-code/full-UUID-never-renders/toggle-through-bloc/pre-completed-unmark/three-deferred-frames, 1 navigation integration — REAL bloc: syllabus → domain → module → toggle → pageBack → MY PROGRESS reflects completion + store persisted, 5 security checkpoint — screens-import-no-networking/screens-never-print/no-PII-shaped-literals/no-video-package-before-9.3/domain-progress-UUID-keys-only), `flutter analyze` 0 issues, affected suites 59/59 green (50 academy + 6 harness/masthead + academy smoke) — the pre-existing serial-run `flutter_tester` segfault under RAM pressure that intermittently kills ONE heavy crypto file remains a known runner artifact (rotating file identity, passes standalone); SECURITY CHECKPOINT PASSED — the Academy UI renders ONLY public course content (domain/module titles, UUID-shortened module codes, duration, locale tags; structural + widget-tree scans: no phones/names/hashes/emails reach the tree), progress operates on UUID v4 module ids only (compiler-enforced + scan), every screen keeps its `SecureScreenWrapper` (FLAG_SECURE), the UI tree imports zero networking packages and never prints (static scans), and the video-player privacy boundary is held BY CONSTRUCTION (no video package imported until Task 9.3); **found & fixed during development:** the navigation integration test needed an extra 300ms pump for the MaterialPageRoute transition to complete (single pump asserted on the syllabus screen mid-transition), `tester.pageBack()` can't drive the module screen's custom back IconButton (tooltip `Back to syllabus`) so the test taps the tooltip directly, `bloc.current` had to be added to the AcademyBloc port (the broadcast stream doesn't replay, so navigation callbacks resolve module/domain metadata synchronously), the late-subscribing StreamBuilder missed the pre-emitted ready state (initState `start()` re-emit — codebase convention), `AcademyTheme.surface` was missing (added, matching the other pillars' token sets), the module screen's StreamBuilder needed the `current` fallback for the toggle to render correctly on first frame, and the string-substring PII scans matched the files' own doc comments (scans now check declared field types / widget-tree values, not prose); next: Task 9.2 Syllabus Tree Implementation |
| 2026-08-16 | 1.33 | Completed Task 8.8 - Phase 8 Completion & Phase 9 Prep — **PHASE 8 COMPLETE (8.1–8.8)**, milestone locked with a consolidated completion audit: 6 new `Task 8.8 Phase 8 COMPLETION AUDIT` security-checkpoint tests asserting (a) FLAG_SECURE is wired into ALL SIX War Room screens (case list / detail / intake / quick-exit safe screen / verified-intel-report sheet / severity-override sheet — each wraps `SecureScreenWrapper` and exposes the `secureFlagService` seam), (b) the ENTIRE `lib/war_room/` + `lib/pii/` trees import zero networking packages (dart:io/http/websocket) and never print/debugPrint (whole-tree scans), (c) no PII-shaped literals (E.164 / 64-hex / email) anywhere in those trees, (d) the UI actor surface is blinded — only `VICTIM` + `AN-####` formats, and (e) memory-hygiene + at-rest proofs still hold (zeroFill/wipe/seal entry points present). **Phase 9 foundation scaffold (Academy)**: new `lib/academy/` tree — zero-identity `AcademyDomain`/`AcademyModule`/`AcademySyllabus` value objects (validated UUID v4 module ids via the `tryParse`/`parse` convention, ISO 639-1 locale tags, opaque non-PII content refs; `AcademySyllabus.modulesFor` tree helper) + `AcademySyllabusRepository`/`AcademyProgressStore` ports + deterministic `InMemoryAcademySyllabusRepository` (seed syllabus) / `InMemoryAcademyProgressStore` data stubs; state: `AcademyState` (idle/loading/ready/failure + completed-module-id set + GENERIC constant error) + `AcademyBloc` port + `LocalAcademyBloc` (monotonic-seq stale-drop, catch-all failure → generic error, offline-first local load, retry recovery); UI: `AcademyTheme` (textbook register: Paper Cream #FAF6ED, Ink, Emerald, serif/mono stacks) + `AcademyMasthead` (`❧ THE ACADEMY` stamp bar + `CIVIC COMMONS OPEN EDUCATION — LEARN WITHOUT LIMITS` subtitle + emerald chapter rule + optional label/module count, wrapped in `SecureScreenWrapper` with the `secureFlagService` seam); 1355 Flutter tests total (37 new: 12 module/domain — validation matrix + equality + zero-identity structural scan, 5 repository/stub, 5 bloc — load/ready/generic-error-no-detail/retry-recovers/toggle-progress/cold-restart, 4 academy checkpoint — no-networking/no-print/no-PII-shaped-literals/no-identity-typed-state-fields, 5 masthead widget incl. FLAG_SECURE-on-mount, 6 Phase 8 completion audit), `flutter analyze` 0 issues, affected suites 442/442 green, FULL suite 1355/1355 green (1207 non-heavy in ONE clean run + all 148 Argon2id-heavy tests passing individually; the serial-run `flutter_tester` subprocess segfault under RAM pressure remains the pre-existing runner artifact — rotating file identity, passes standalone); SECURITY CHECKPOINT PASSED — Phase 8 locked: FLAG_SECURE on every War Room screen (source-verified), local-first boundaries (whole-tree no-networking scan), zero-PII surfaces (whole-tree literal scan + blinded-actor format proof), memory hygiene + at-rest encryption proofs intact; Phase 9 scaffold itself is zero-identity (state declares no identity-typed fields — structural proof) and FLAG_SECURE-wrapped at its only entry point; **found & fixed during development:** `on Exception` in the bloc missed `StateError` (codebase convention is bare `catch (_)` — switched), const constructors can't run runtime validation in asserts (AcademyModule uses the `tryParse`/`parse` factory convention like `PinCode`), and a naive state-file substring PII scan matched its own security doc comment (scan now checks declared field types, not prose); next: Task 9.1 Academy UI Foundation |
| 2026-08-16 | 1.32 | Completed Task 8.7 - Trauma-Informed Intake UX (Phase 8, The War Room) — **Quick Exit / panic button** on the intake stepper AND detail screens: an always-reachable red QUICK EXIT control that INSTANTLY wipes every transient in-memory buffer (narrative controller cleared, picked evidence byte buffers zero-filled via the new `zeroFill`, selections/consents/step reset — `_wipeTransientState`) and routes to the new neutral `QuickExitSafeScreen` (FLAG_SECURE wrapped, zero case data, `Return to home`); host `onQuickExit` seam exits the vault shell instead; **Pause, Save & Resume**: new `lib/war_room/domain/intake_draft.dart` (`IntakeDraft` + strict `v:1` `IntakeDraftEnvelope` codec — draft id/step/situation/narrative/urgency/consents/opt-in/saved-at, ZERO identity fields, rejects unknown versions/malformed/enum/bad step/bad timestamp + `zeroFill` memory-hygiene helper) + `IntakeDraftStore` port + `lib/war_room/data/encrypted_intake_draft_store.dart` (`EncryptedIntakeDraftStore` — AES-256-GCM SEALS the envelope via the app-key `QueuePayloadCipher` BEFORE any persistence; the row `IntakeDraftRecord` carries id + sealed payload + timestamp only; open happens only inside the local app, wrong key throws; `intake_drafts` SQLCipher table schema **v9** + migration 9 up/down + row codecs); the intake screen gains a `draftStore` seam: **Pause & Save** on every step persists the sealed draft + non-destructive `resume anytime` confirmation + `onPaused` host exit, step-1 **resume banner** (`You have a saved draft — Resume draft / Start fresh`) restores the exact step + content, Start fresh deletes the sealed draft; **trauma-informed UX**: fixed `_GroundingNote` (same reassurance line on every step — no pressure, no timers, victim in control), the ✕ close now CONFIRMS before discarding when there is unsaved input (`Leave without saving?` Keep editing / Leave — no destructive reset without explicit confirmation; empty form exits immediately), QUICK EXIT stays one-tap (panic must never confirm), and the action bar (Back / Pause & Save / Continue) is now PINNED below the scrollable step content so the primary controls never scroll away; 1322 Flutter tests total (38 new: 9 draft domain — round-trip/partial/version-reject/malformed-reject/step-bounds/enum-reject/bad-timestamp/zero-identity-frame/zeroFill+empty, 8 encrypted store — round-trip/BYTE-LEVEL-no-plaintext/delete/absent-null/newest-first-order/cold-restart-recovery/wrong-key-throws/idempotent-resave, 14 trauma widget — quick-exit-always-visible/quick-exit-wipes+routes-to-safe-screen/onQuickExit-seam/✕-confirms-when-dirty+keep-editing+leave/✕-clean-exits-immediate/grounding-note-every-step/pause-save-persists+onPaused/pause-save-confirmation/resume-restores-step/start-fresh-discards/FLAG_SECURE-retained + QuickExitSafeScreen copy+FLAG_SECURE + detail-quick-exit, 1 integration — REAL pause→exit→reopen→resume→complete with the REAL sealed store (zero data loss, filed narrative intact), 1 schema v9 + 2 migration (v8→v9/v9-rollback) + 3 security checkpoint), `flutter analyze` 0 issues, affected suites 113/113 green, FULL suite 1322/1322 green (all Argon2id-heavy files pass in isolation; the serial-run `flutter_tester` subprocess segfault that intermittently kills ONE heavy crypto file under RAM pressure remains a pre-existing runner artifact — rotating file identity, passes standalone); SECURITY CHECKPOINT PASSED — paused drafts are ENCRYPTED AT REST (byte-level proof: the stored row contains zero plaintext narrative/field names; recoverable only through the device key hierarchy, wrong-key open throws), the draft frame carries ZERO identity (structural scan: no phone/name/handle/hash fields), memory hygiene is enforced (zeroFill byte-level wipe proof + the intake wipe path clears the controller and zero-fills evidence buffers; no debug prints), QUICK EXIT routes to a FLAG_SECURE neutral screen with zero case data, and the 8.7 files import no `dart:io`/http/WebSocket (static scan); **found & fixed during development:** the added grounding note pushed the action row below the lazy ListView's build window in the 600px test viewport (Continue → vanished) — the action bar is now a FIXED bottom container so Back/Pause & Save/Continue are always reachable (also better trauma-informed UX); the resume integration test needed an unmount between sessions (the same widget type reuses State — `initState` draft reload never re-ran); the second `pumpWidget` in the integration test required `pumpWidget(SizedBox())` to force a fresh `_loadResumeDraft`; next: Task 8.8 Phase 8 Completion & Phase 9 Prep (voice-note capture + transcription remain deferred) |
| 2026-08-16 | 1.31 | Completed Task 8.6 - Chain of Custody Logging (Phase 8, The War Room) — domain: new `lib/war_room/domain/custody_log.dart` — `CustodyEventType` enum (filed/severityOverridden/analystsAssigned/analystUpdated/evidenceAttached/paused/resumed/withdrawn/reportSigned/handoffQueued), `CustodyEvent` (eventSeq, caseNumber, type, blinded actor, at, prevHash, selfHash — the SHA-256 hash CHAIN over `caseNumber|seq|type|actor|at|prevHash`; `recomputeSelfHash` for tamper re-derivation; zero-PII by construction: no names/payload/phone), `CustodyLog` port (append/eventsFor/verifyIntegrity/buildEvent) + `VerifiedIntelReport` (caseNumber + caseSummary + blinded analystId + signedAt + status + severity + slaHours + reportSignature) + `ReportSigner` port (sign/verify) + `LegalAidHandoff` (handoffId UUID v4, caseNumber, signedReportRef, createdAt) + `encodeLegalAidHandoff`/`decodeLegalAidHandoff` strict `v:1` envelope codec (caseNumber + handoffId + created_at ONLY — zero identity, zero evidence content; reject-unknown-version, malformed, bad-base64); data: `InMemoryCustodyLog` (append-only hash chain, per-case head tracking, `verifyIntegrity` walks the full chain from genesis — ANY reorder/swap/mutation/truncation breaks it; injectable hasher seam for deterministic tests) + `HmacReportSigner` (HMAC-SHA256 over the deterministic report frame `caseNumber|summary|analystId|status|severity|sla|at`, base64 signature; verify recomputes + constant-time-ish comparison; wrong key → false) + `QueueLegalAidHandoffSink` (local-first persist + sealed enqueue mirroring `QueueEvidenceSink`: item id = handoff UUID v4 doubles as the Idempotency-Key; minted by the sink; a failing local write never leaves a queue item); repository: `InMemoryWarCaseRepository` records custody events at EVERY transition — fileCase/overrideSeverity/auto-assign/analystUpdate/withdraw/pause/resume, `signVerifiedReport` (HMAC-signed report + reportSigned event) + `queueLegalAidHandoff` (sealed envelope queued + handoffQueued event); state: `WarRoomCaseSummary` carries `custodyEvents` + `signedReport`, bloc `signReport`/`queueHandoff`; UI: CHAIN OF CUSTODY section on `WarCaseDetailScreen` (per-event rows: fixed EVENT label + timestamp + blinded actor `VICTIM`/`AN-####` — NEVER payload content, zero identity) + `VerifiedIntelReportSheet` (FLAG_SECURE wrapped: case meta + HMAC signature + `VERIFIED` chip + `Send to legal aid` → handoff queued + `QUEUED` tag); 1284 Flutter tests total (41 new: 14 custody log unit — chain-append/self-hash-link/verify-genesis/append-only-reject/REORDER-fails/swap-fails/mutate-fails/truncate-fails/deterministic-ids/case-isolation/eventsFor-order/empty-genesis/buildEvent-fixed-hash, 3 HMAC signer — sign-verify-roundtrip/wrong-key-false/deterministic-signature, 5 handoff sink — local-first+sealed/byte-level-no-plaintext/queue-item-id=handoff-id/cold-restart-recovery/failing-persist-atomicity, 6 repository — events-recorded-at-transitions/severity-override-event/analyst-assign-event/sign-report-emits-event+signature/handoff-queued-event/unknown-case-throws, 1 bloc — signReport+queueHandoff-emit, 4 detail-screen widget — custody-section-renders-fixed-labels+blinded-actors/report-sheet-HMAC-VERIFIED+handoff-QUEUED/zero-payload-tree-scan/FLAG_SECURE-retained, 2 integration — file→transitions→custody-chain-verify-integrity (real stores) + sign→handoff→queue→envelope-codec-roundtrip, 6 security checkpoint — verify-integrity-after-legit-chain/REORDER+SWAP+MUTATE-all-fail/zero-PII-event-surface-structural/events-never-payload/handoff-envelope-zero-identity/no-networking-no-print-scan), `flutter analyze` 0 issues, affected suites 159/159 green, FULL suite 1284/1284 green (all 111 Argon2id-heavy tests pass in isolation; the serial-run `flutter_tester` subprocess segfault that intermittently kills ONE heavy crypto file under RAM pressure is a pre-existing runner artifact — rotating file identity, passes standalone); SECURITY CHECKPOINT PASSED — the custody log is IMMUTABLE (append-only API + hash chain: reorder/swap/mutate/truncate ALL fail `verifyIntegrity`, proven by tests), the log + report + handoff carry ZERO PII (structural proof: events are fixed labels + blinded `VICTIM`/`AN-####` actors + timestamps, the handoff envelope declares no identity/evidence fields, the report signs only case meta), the handoff is sealed end-to-end (AES-256-GCM queue envelope, UUID v4 idempotency), and the custody files import no `dart:io`/http/WebSocket and print nothing (static scan); **found & fixed during development:** the withdraw widget test failed after the custody section pushed the top-of-list status chip outside the lazy ListView's cache window while the test scrolled to the bottom controls (now scrolls back up to assert), the HMAC signer's `macBits` param must be dropped (`Hmac(Sha256())` only), hashing is async per the codebase's `cryptography` package convention so `buildEvent`/`recomputeSelfHash` are async, and the handoff sink mints the UUID itself (the repository passes an empty id — the item id IS the handoff id, the Idempotency-Key contract); next: Task 8.7 Trauma-Informed Intake UX |
| 2026-08-16 | 1.30 | Completed Task 8.5 - Analyst Assignment & Collaboration (Phase 8, The War Room) — domain: new `lib/war_room/domain/analyst.dart` — `AnalystSkill` enum (osint/platformTakedown/threatAssessment/legalAdvocacy/crisisSupport/digitalForensics + `forSituation` DETERMINISTIC constant map from each intake situation to its required skills), `AnalystVettingStatus` (pending/vetted/rejected), `GauntletScenario`/`GauntletAttempt` + `AnalystVettingGauntlet` (the CTF-style sandbox: `GAUNTLET-01` embeds fixed threat markers, `score = correct/expected × 100`, `passThreshold` 80 — PURE and deterministic, unknown scenario scores 0, false positives never inflate), `Analyst` (BLINDED `AN-####` handle, skills, vetting status, gauntletScore, `activeCaseCount` load, `caseCap` — `availableForAssignment` = vetted AND under cap; `withVetting`/`withIncrementedLoad`/`withDecrementedLoad` never go below zero), `CaseAssignment` (caseNumber + blinded analystId + matched skill + assignedAt), `AnalystUpdate` now carries the BLINDED author `analystId` (the blind-review contract — the old identity-less note was superseded; the note is attributed ONLY via the handle, never a name); port `AnalystRegistry` (list/assign/release/runGauntlet/analystById); data: `InMemoryAnalystRegistry` — canonical six-vetted-analyst production pool spanning all skills, `assignToCase` picks per-required-skill the LEAST-LOADED vetted analyst carrying the skill under cap (ties break by blinded handle ascending — deterministic), load counts CASES not skill-slots (an analyst already on the case fills extra skill slots without double-counting), `releaseFromCase` decrements load, `runGauntlet` promotes pending → vetted only on a passing score (a failing attempt never changes state); repository: `InMemoryWarCaseRepository.fileCase` now AUTO-ASSIGNS at file time via the injectable registry (offline-first — the victim sees their blinded team immediately), status moves to `investigationOngoing` when a team is assigned, the timeline 'Analysts assigned' checkpoint flips done with a 'N vetted analyst(s) — skill-matched' detail, `withdraw` RELEASES the team's load, new `addAnalystUpdate` (blind-review enforcement: an analyst NOT on the case's assignments is rejected with StateError — a stranger can never write on a case); state: `WarRoomCaseSummary` carries the blinded `assignments` + `WarRoomBloc.addAnalystUpdate`; UI: `_AnalystTeamSection` on `WarCaseDetailScreen` (ANALYST TEAM header + per-assignment rows: mono `AN-####` handle chip + fixed skill label + VETTED status — renders ONLY blinded handles and skill labels, never names/emails/phones/hashes) + update cards now show `AN-#### · date`; 1243 Flutter tests total (42 new: 14 analyst domain — situation-skill map/gauntlet-100/gauntlet-deterministic/80-threshold-inclusive/empty-0/unknown-0/false-positives-no-inflation/available-vs-at-cap/pending-never/load-clamp/withVetting, 12 registry — least-loaded-wins/tie-break-handle-ascending/pending-never-assigned/at-cap-skipped/multi-skill-one-per-skill/no-capacity-gap/deterministic-repeated-runs/release-decrements/runGauntlet-promotes/fail-keeps-pending/unknown-throws/production-pool-blinded, 6 repository — auto-assign-skill-matched-blinded/withdraw-releases-load/right-skill-right-analyst/addAnalystUpdate-blinded-attribute/stranger-rejected/notes-never-name-others, 1 bloc — addAnalystUpdate-emits, 3 detail-screen widget — team-renders-blinded-handles/team-omitted-when-empty/zero-identity-tree-scan, 2 integration — file→auto-assign→team-UI→blinded-note→stranger-rejected→load-release (real stores) + at-cap-never-double-assigned, 4 security checkpoint — analyst-identities-blinded-structural/assignment-deterministic-repeated/blind-review-note-only-handle/analyst-files-local-first-no-networking-no-prints), `flutter analyze` 0 issues, affected suites 121/121 green, FULL suite 1243/1243 green; SECURITY CHECKPOINT PASSED — analyst identities are BLINDED to victims: the ONLY analyst-bearing surfaces are generated `AN-####` handles (structural proof: every assignment/update handle matches `^AN-\d{4}$`, no name/email/phone/64-hex hash on any analyst surface), assignment + vetting are DETERMINISTIC (repeated-invocation equality across fresh pools — no randomness, no wall-clock), blind review is ENFORCED (a note is attributed only via the blinded handle and its text never names another analyst; an analyst not assigned to the case cannot post — repository-enforced), and the analyst files import no `dart:io`/http/WebSocket and print nothing (static scan); **found & fixed during development:** the cap check initially blocked an analyst from taking a SECOND skill slot on the SAME case (load now counts cases, not slots — a single case is one workload, the cap guards concurrent cases), and the widget tests' bottom controls needed `scrollUntilVisible` once the ANALYST TEAM section pushed them below the lazy ListView's build window; next: Task 8.6 Chain of Custody Logging |
| 2026-08-16 | 1.29 | Completed Task 8.4 - Severity Scoring System (Phase 8, The War Room) — domain: new `lib/war_room/domain/severity_scoring.dart` — `SeverityScorer` deterministic keyword engine (CRITICAL band: leaked/published/went viral/immediate danger/suicidal/weapon/kidnap/life-threatening; HIGH: blackmail/extortion/threat/intimate image/revenge porn/nude/harassment/stalking/assault/ransom/deadline; MEDIUM: fake profile/impersonation/identity theft/fraud/scam/hacked/stolen/password/defamation; URGENCY: immediately/tonight/asap/urgent/today/right now/hours/before/by tomorrow; band rules: 1+ CRITICAL → critical, 2+ HIGH or 1 HIGH+urgency → high, 1 HIGH or 2+ MEDIUM → medium, else low; FLOORS: urgency + situation severity via `CaseSeverity.maxSeverity` — a serious category or an immediate threat can never be auto-scored below its band; `slaHoursFor` single deterministic 24/48/72/120h map), `SeverityTriage` (severity + NON-PII signal COUNTS + slaHours — matched keywords are never returned, only fixed COUNT labels via `signalLabels`), `SeverityOverride` (newSeverity + reason + timestamp — analyst annotation, never identity); data: `InMemoryWarCaseRepository` now AUTO-SCORES at `fileCase` via the injectable scorer (replaces the Task 8.1 provisional hint; `_FixedScorer` stub seam) + `overrideSeverity` (re-bands severity + SLA while PRESERVING the original triage for the audit trail; blank reason → ArgumentError, unknown case → StateError); model: `WarRoomCase` gains `triage` + `severityOverride` + `withSeverityOverride` (severity + estReportHours follow the override; triage preserved); state: `WarRoomBloc.overrideSeverity` + `WarRoomCaseSummary` carries `triage`/`severityOverride`; UI: `_TriageSection` on `WarCaseDetailScreen` (severity band + `SLA Nh` mono line + OVERRIDDEN chip + fixed signal COUNT chips or the zero-match note + `Override severity`/`Change override` button — renders ONLY public attributes and counts, never matched keywords, never case content) + `SeverityOverrideSheet` (FLAG_SECURE wrapped, RadioGroup LOW→CRITICAL with per-band SLA lines, REQUIRED reason field, Apply gated on both, constrained + scrollable so every control is reachable on small screens); 1201 Flutter tests total (23 new: 12 scorer engine unit — band thresholds/urgency floor/floor-never-downgrades/maxSeverity merge/SLA-determinism/ties/determinism/signal-labels-fixed-never-matched-text, 3 repository — override-re-bands+new-SLA/blank-reason-rejected-invariant/rejected-override-unchanged+injectable-scorer, 1 bloc — override re-bands emitted summary, 4 detail-screen widget — triage-section-renders-signal-counts+SLA/applied-override-OVERRIDDEN+reason/override-sheet-reason-required-apply-re-bands/triage-zero-signal-keyword-text, 3 security checkpoint — DETERMINISM-repeated-invocation/scorer-reads-only-case-content-zero-identity-input/severity-files-local-first-no-networking-no-prints), `flutter analyze` 0 issues, affected suites 79/79 green, FULL suite 1201/1201 green; SECURITY CHECKPOINT PASSED — severity scoring is DETERMINISTIC (repeated-invocation equality, no randomness, no wall-clock — SLA/tie behavior identical across runs), the scorer's signature accepts ONLY case content (narrative string + category enums) and returns ONLY public severity attributes + signal COUNTS (structural proof), the triage/override UI renders zero signal-keyword text and zero identity (tree scans), the severity files import no `dart:io`/http/WebSocket and print nothing (static scan), and a human override always preserves the machine triage for the audit trail; **found & fixed during testing:** the modal bottom sheet's entrance animation left the sheet below the fold under bare `pump()` calls in widget tests (the sheet's controls were laid out off-screen and untappable — the test now advances the animation clock via `pumpAndSettle`, and the sheet itself is height-constrained + scrollable so all controls stay reachable), a `final override` local shadowed the `@override` annotation, and the auto-scorer correctly upgraded a previously-HIGH provisional expectation to CRITICAL for a narrative with direct threats; next: Task 8.5 Analyst Assignment & Collaboration |
| 2026-08-16 | 1.28 | Completed Task 8.3 - PII Redaction Pipeline (Phase 8, skill: local-pii-redaction) — new `lib/pii/` tree enforcing the AI Boundary Rule: domain (`PiiCategory` wire-safe categories phone/email/aadhaar/pan/ssn/credit_card/hex_token/bearer_token/key_value_secret/person_name/address, `RedactedSpan` category+offsets ONLY — never the matched plaintext, `PiiRedactionReport` non-PII aggregate counts, `Uint8BufferInput` MUTABLE plaintext buffer with `wipe()` (Dart Strings are immutable and cannot be zeroed — the wipe requirement needs a wipeable buffer), `PiiRedactionPipeline` port + `ContextualPiiDetector` port); data (`DeterministicPiiFilter` — the standard regex dictionary: E.164 + Indian mobile (`[6-9]xxxxxxxxx` with optional 91) + domestic phones, e-mail, **Aadhaar** (12-digit grouped/continuous — the alternation is ordered long-to-short so a 12-digit run consumes fully), **PAN** (5 letters + 4 digits + letter), SSN, credit cards, 32+ hex tokens (blind hashes/API keys), Bearer/JWT, key=value secrets; all patterns scan in ONE pass and **overlapping spans merge into a single placeholder** (a value can never survive nested between two patterns); `DictionaryPiiDetector` — the LOCAL in-memory contextual detector (bounded common-Indian-name dictionary + coarse address cues street/road/lane/gali/nagar/colony/near/opposite/sector, case-insensitive whole-word matches, category+offset spans only) — the deterministic stand-in for the on-device Gemma slot (same port, no cloud AI, no network); `LocalPiiRedactionPipeline` — executes the skill's 4 steps in order: deterministic filter FIRST → local contextual detection on the ALREADY-REDACTED remainder (AI Boundary Rule: the model slot can never see a raw phone/e-mail/gov ID — spy-detector proof-tested) → callers persist ONLY the redacted output → plaintext buffer zeroed unconditionally in `finally` even on failure); resources per the skill (`client/resources/regex/pii_patterns.json` — the documented dictionary; `client/resources/prompts/gemma_redaction_prompt.txt` — strict JSON-output prompt template for the on-device slot); UI wiring — `WarRoomIntakeScreen` gains an optional `redactionPipeline` seam: the narrative is scrubbed (phone + name + address → `[REDACTED]`) at submit time and the plaintext buffer wiped BEFORE the case is filed; `CaseIntakeSubmission` carries the non-PII `redactionReport` + `redactionApplied` flag; the consent gate no longer re-runs the pipeline on rebuild (the pipeline runs exactly ONCE per submission); 1178 Flutter tests total (29 new: 11 deterministic filter — E.164+IN mobiles/email/Aadhaar grouped+continuous/PAN/SSN+cards/hex+bearer+JWT/key=value/overlap-merge-single-placeholder/total-no-throw+no-leak, 6 detector — name detection/offsets/case-insensitive/no-fragment-matches/address-cues/spans-never-plaintext/clean-text, 6 pipeline — deterministic-before-contextual/AI-Boundary-Rule-spy/AI-Boundary-Rule-checkpoint/wipe-proof/wipeOnRedact-opt-out/report-counts/clean-pass-through, 1 intake integration — REAL pipeline wired: narrative with phone+name+address filed with zero raw PII + wiped buffer (real stores), 5 security checkpoint — local-only-no-networking-no-print scan/wipe-unconditional/wipeOnRedact-explicit/resources-exist/report-no-plaintext), `flutter analyze` 0 issues, affected suites 90/90 green, FULL suite 1178/1178 green; SECURITY CHECKPOINT PASSED — deterministic dictionary runs BEFORE any local-model slot (spy-detector proof: phones/e-mails/gov-IDs never reach the detector), the pipeline is local-only (no dart:io/FFI/http/WebSocket imports, nothing printed), plaintext is wiped immediately after redaction (byte-level wipe proof + end-to-end intake proof), redaction results carry only non-PII aggregate counts; **found & fixed during development:** Aadhaar regex mis-matched grouped 12-digit (alternation rewritten), the detector's address pattern was case-sensitive (MG Road missed — now case-insensitive), the `redact` filter returned `PiiRedactionResult` with a missing import, and the intake ran the pipeline once per rebuild (consent gate probed the submission) — the pipeline now runs exactly once at submit; next: Task 8.4 Severity Scoring System |
| 2026-08-16 | 1.27 | Completed Task 8.2 - Encrypted Evidence Upload (Phase 8, The War Room) — domain: `EvidenceItem` (`PickedEvidence` — raw filename is a DISPLAY-ONLY transient that dies when the pick completes; `EvidenceRecord` — durable row carrying ONLY metadata + opaque ciphertext blobs, NO filename/path/EXIF/identity column by design; `evidenceLabel` — derived `mime · size` label, the ONLY presentation that reaches the UI), `EvidenceEnvelope`/`DekEnvelope` strict wire codecs (`v:1` JSON frame — `evidence_id`/`case_number`/`size_bytes`/`mime_type`/`created_at` + base64 `sealed_file` + `dek_envelope`, ZERO identity fields; binary DEK envelope with version + length-prefixed fields, UTC-preserving created_at), `EvidenceCipher`/`EvidencePicker`/`EvidenceSink` ports; data: `AesGcmEvidenceCipher` (per-item 32-byte DEK via `SecretKeyData.random` → AES-256-GCM file seal via the existing `CryptoService` → DEK WRAPPED to the device X25519 public key (ECDH shared secret as the AES-GCM wrap key; fingerprint = first 8 bytes of the recipient key — never identity); unwrap verifies the fingerprint before opening), `FilePickerEvidencePicker` (the SINGLE production file touching the platform file system — `file_picker` with `withData: true`; coarse extension→mime inference), `QueueEvidenceSink` (offline-first write order: **encrypt FIRST** (seal + wrap, then the plaintext DEK buffer is zeroed) → **local-first persist** the encrypted record → **sealed enqueue** via `SyncQueueRepository.create()` with the item id = the evidence UUID v4 (doubles as the Idempotency-Key; `enqueue()` mints a fresh id so `create()` is used instead) — a failing local write never leaves a queue item behind (atomicity)); schema **v8** `evidence` table (id PK, case_number, sealed_file BLOB sensitive, dek_envelope BLOB sensitive, size_bytes, mime_type, created_at — no filename/identity column) + migration up/down + `evidenceRecord` row codecs (UTC-preserving created_at); state: `WarRoomBloc.attachEvidence`/`refreshEvidence` + UI-safe `EvidenceSummary` (id/case/size/mime/timestamp ONLY) + `encryptingEvidence`/`evidenceError` (fixed generic message, never a stack trace) in `WarRoomState`; UI: intake Step 3 (Evidence) wired — `Add evidence` button → picker → bloc → `Photo · 2.0 KB` chip + `ENCRYPTED` tag (filename NEVER rendered), `Encrypting evidence…` progress row, generic error banner, flow survives failures; `file_picker` added as the ONLY new dependency; 1149 Flutter tests total (38 new: 10 cipher — fresh-DEK/round-trip/byte-level-proof/tamper/wrong-DEK/wrap-unwrap-self-recovery/wrong-keypair-fingerprint/binary-codec/full-recovery-pipeline, 7 envelope codec — round-trip/zero-identity-fields/reject-versions/malformed/bad-base64/DEK-binary/DEK-rejects, 7 sink — local-first+sealed/byte-level-proof/DEK-wrapped-only/no-filename-column-codec/cold-restart-snapshot/remove/failing-persist-atomicity, 4 bloc — attach-emits-summary/sink-failure-generic/no-sink-no-crash/cold-restart-reload, 3 intake widget — chip-no-filename/cancel-nothing/failure-banner+flow-survives/FLAG_SECURE-retained, 1 integration — pick→encrypt→queue→file round trip with REAL cipher+queue (sealed envelope in queue, zero plaintext, zero filename), 4 schema/migration — v8-table/v6→v8/v7→v8/rollback, 3 security checkpoint — no-networking-no-print scan of the evidence pipeline/byte-level-queue-proof/envelope-no-filename), `flutter analyze` 0 issues, affected suites 92/92 green, FULL suite 1149/1149 green; SECURITY CHECKPOINT PASSED — evidence never decrypts server-side (only sealed files + wrapped DEKs leave the device), BYTE-LEVEL proof that stored + queued bytes never match the plaintext file (and no 64-byte plaintext fragment survives), the evidence row + envelope + UI expose ZERO filename/path/identity (no filename column by design, picker is the only file-system touchpoint, static scan confirms no `dart:io`/http/WebSocket imports in the evidence pipeline), plaintext DEK buffers are zeroed in memory after wrapping; **found & fixed during testing:** the bloc crashed on attach before `start()` (null `_last` → broadcast add) — now falls back to `WarRoomState.initial()` as the emission base; the sink used `enqueue()` which mints a fresh id, breaking the idempotency-key contract — switched to `create()` with the evidence id; `created_at` round-trips lost the UTC flag — now preserved in both codecs; KB label dropped the decimal — now `Photo · 4.0 KB`; next: Task 8.3 PII Redaction Pipeline (skill: local-pii-redaction) |
| 2026-08-16 | 1.26 | Completed Task 8.1 - War Room UI Foundation (first task of Phase 8, OSINT Cyber Defense) — new `lib/war_room/` domain tree (`CaseSeverity` enum critical/high/medium/low w/ strict labels + deterministic rank/maxSeverity merge, `CaseStatus` (draft/underInvestigation/investigationOngoing/reportReady/withdrawn/closed), `WarRoomCase` entity — dossier stamp attributes ONLY: caseNumber/title/description/severity/status/paused/filedAt/analyst COUNT/estReportHours/timeline/updates, `CaseTimelineEntry` + blinded `AnalystUpdate` (text+timestamp+progress chip — never analyst identity), `CaseIntakeSubmission` + `IntakeSituation` (6 PRD §8.3 categories w/ base severities) + `IntakeUrgency` (immediate/thisWeek/noDeadline w/ severity floors — an immediate threat can never be filed LOW) + deterministic `provisionalSeverity` = maxSeverity(situation, urgency floor) — never downgrades a serious category (keyword scoring engine replaces it in Task 8.4), `WarCaseRepository` port) + `lib/war_room/data/` (local-first `InMemoryWarCaseRepository` — sequential `CC-0047` stamp generation above any seeded max, newest-filed-first, 6-step DESIGN §8.4 timeline on fileCase, one-tap `setPaused` + final `withdraw` (clears pause), zero networking imports) + state layer (`WarRoomBloc`/`WarRoomState` w/ UI-safe `WarRoomCaseSummary` projection + `LocalWarRoomBloc` monotonic-seq snapshot guard + selection-preserving refresh so the open detail follows pause/withdraw); UI per DESIGN §2.2/§8 (dossier register): `WarRoomTheme` tokens (Dossier Ink/Manila Paper/Amber + severity→band color mapping + mono case-stamp font), `WarRoomMasthead` (`▌WAR ROOM▐` + section label + `CASE #CC-0047` stamp + compact severity band + `CIVIC COMMONS OSINT UNIT — SECURE CHANNEL` + charcoal stamp rule), `WarCaseSeverityBand` (`HIGH SEVERITY` classification strips), `WarRoomCaseListScreen` (case cards w/ mono stamp/title/severity band/`Filed · Status`/analyst count · est. report/`View case →`, PAUSED/WITHDRAWN tags, empty state, amber `[+ File a new case]` FAB), `WarCaseDetailScreen` (self-contained open-on-init, status timeline w/ ✓/○ checkpoints, blinded analyst update cards, PAUSED/WITHDRAWN chips, always-visible `You can pause or withdraw this case at any time` + Pause/Add-more-evidence/Withdraw controls + Verified Intel Report slot for Task 8.6), `WarRoomIntakeScreen` (trauma-aware 5-step stepper `[1●2○3○4○5○]`: situation radio (RadioGroup) → narrative → evidence (encrypted-before-leaving notice; upload lands 8.2) → urgency radio + safety-first note → consent gate (2 REQUIRED checkboxes + optional Ledger opt-in) → `Submit case securely` reports the stamp via onFiled); ALL three screens wrapped in `SecureScreenWrapper` (FLAG_SECURE, test seam); 1111 Flutter tests total (43 new: 5 intake-model — consent gate/category-severity/urgency-floor-never-downgrades/maxSeverity-deterministic, 6 repository — newest-first/getById/sequential-stamps+timeline/seeded-max-not-reissued/pause-toggle/withdraw-clears-pause/unknown-case-throws, 5 bloc — load/open-close/fileCase/st-setPaused-withdraw/open-detail-follows-mutations, 5 masthead widget — stamp-bar+subtitle/optional-stamp+band/charcoal-bg/null-hides/zero-PII, 6 case-list widget — cards+bands/FLAG_SECURE/tap+FAB/empty-state/status-tags/paper-bg, 5 detail widget — timeline+updates/FLAG_SECURE/pause-withdraw-chips/evidence+report-callbacks/zero-PII, 5 intake widget — FLAG_SECURE/gating/steps+back/evidence-notice/full-flow-files+stamp/close-exit, 1 navigation integration — list→detail→pause/resume→intake→CC-0048 filed→list shows CRITICAL band (real stores), 5 security checkpoint), `flutter analyze` 0 issues, affected suites 442/442 green (concurrency=1), FULL suite 1111/1111 green; SECURITY CHECKPOINT PASSED — FLAG_SECURE is ACTIVE on every War Room screen (list/detail/intake verified with a recording flag service), the War Room data/state layers import no `dart:io`/http/WebSocket (static scan) and print nothing, screens render ONLY dossier attributes (stamp numbers, severity bands, timestamps, analyst COUNTS — no phones/hashes/names/handles, enforced by zero-PII tree scans), the intake form collects NO identity (narrative is case content, encrypted at rest in Task 8.2); next: Task 8.2 Encrypted Evidence Upload |
| 2026-08-16 | 1.25 | Completed Task 7.6 - Peer Review Gate (final task of Phase 7) — domain: `PeerReviewDecision` (approved/rejected/flagged + strict wire names), `PeerReviewSubmission`/`PeerReviewRecord` (post id + decision + timestamp ONLY — no reviewer identity, no PII), `ReviewerVerification` (blinded reviewer display handles), `PeerReviewGate` (PURE deterministic rules: Shadow Queue FR-L3 <96h → karma fast-track ≥100 → 3/3 Peer Review consensus FR-L4; `assignReviewers` selects deterministically seeded by post id — never Random() — and returns ONLY derived `reviewer_` + 6-hex FNV-1a display handles, never raw blind hashes), `PeerReviewWireFrame` codec (`peer_review_wire_codec.dart` — strict v:1 + strict decision decode, ONLY post_id + decision, NO identity); schema v7 `peer_reviews` table (post_id PK, decision, reviewed_at — no identity column by design) + migration up/down + `peerReviewRecord` row codecs; data: `QueuePeerReviewSink` (offline-first — decision record written FIRST to the encrypted DB, then a `SyncOperationType.update` envelope SEALED by the Task 3.3 AES-256-GCM cipher; UUID v4 idempotency keys; localDecisions() recovery snapshot) + `InMemoryLedgerFeedRepository.applyReview` (approve increments toward 3/3 → publishes; reject/flag never advance; published posts immutable — no-op, count never drifts past 3; missing id safe no-op); state: `LedgerReviewBloc`/`LedgerReviewState` (queue of peer-review + shadow posts newest-first, shadowQueueCount, reviewedByMe tracking) + `LocalLedgerReviewBloc` (reads ONLY the local repository, monotonic-seq stale-drop, consensus resolved client-side, failing sink degrades gracefully — decision stays applied); UI: `PeerReviewQueueSection` wired into `LedgerFeedScreen` (review cards with category/headline/non-PII author handle/blinded reviewer handles/`[✓ Peer Review Gate: N/3 approved]` indicator + APPROVE/REJECT/FLAG controls hidden after review or at 3/3; Shadow Queue teaser; DEFENSIVE `_safeHandle` re-blinding — a raw 64-hex handle is truncated to `reviewer_` + 6-hex before it can reach the tree); 1068 Flutter tests total (59 new: 16 gate unit — 3/3 consensus/reject-flag-never-advance/published-immutable/shadow→review/96h-window-boundary/fast-track/F-L3-before-fast-track/determinism/3-reviewers/deterministic-set/different-posts-different-sets/blinded-handles-never-raw/empty-pool/blindedHandleFor, 8 wire-codec — round-trip-all-decisions/identity-free-frame/wire-names/strict-bounds/version/non-object/deterministic, 9 sink — local-first/single-sealed/sealed-bytes-proof/UUID-v4/overwrite-reviews/localDecisions/row-codec-round-trip/strict-bounds/failing-persist-no-partial-enqueue, 2 end-to-end integration — bloc→local→sealed→cold-restart-row-round-trip→sync-drain byte-identical + approve→reject queues two sealed envelopes, 4 repo applyReview — 3/3-publishes/reject-flag-no-advance/published-immutable/missing-id-no-op, 7 review-bloc — queue-projection/approve-consensus-reprojects/reject-flag-recorded/reviewed-by-me/failing-sink-degrades/non-PII-projections, 6 review-section widget — N/3-indicator/3/3-PUBLISHED-hides-controls/approve-publishes-leaves-queue/reject-marks-reviewed/shadow-teaser/no-PII-hash-tree-scan, 4 schema/migration — peer_reviews columns/no-identity/v6→v7-upgrade/v7-rollback, 4 security checkpoint — no-networking-no-print scan/identity-free-frame/section-no-hash-literals/rows-post-decision-only), `flutter analyze` 0 issues, affected suites 519/519 green, FULL suite 1068/1068 green; SECURITY CHECKPOINT PASSED — reviewer identities are blinded by construction: assignment returns ONLY derived display handles (raw blind hashes never returned/logged/rendered), the sealed frame carries only post_id + decision, the `peer_reviews` table has no identity column, review production files import no `dart:io`/http and print nothing (static scan), and the review section DEFENSIVELY re-blinds hash-shaped handles before they reach the widget tree; **PHASE 7 COMPLETE** — next: Phase 8 Task 8.1 War Room UI Foundation |
| 2026-08-10 | 1.24 | Completed Task 7.5 - Voting System with Karma Weighting — domain: `LedgerVoteDirection` (up/down/none + strict wire names + deltas), `LedgerVote` value object, `LedgerVoteRecord` (durable local row), `LedgerVoteSink` port, `LedgerVoteWireFrame` codec (`ledger_vote_wire_codec.dart` — strict v:1 + strict direction decode, ONLY post_id + direction, NO identity), `KarmaWeightedScore` (`karma_weighted_score.dart` — deterministic SUB-LINEAR `floor(sqrt(up)) − floor(sqrt(down))` client-side scoring; operates ONLY on two aggregate integers, no identity input possible); `LedgerPost` gains `myVote`; schema v6 `post_votes` table (post_id PK, direction, updated_at — no identity column by design) + migration up/down + `ledgerVoteRecord` row codecs; data: `QueueLedgerVoteSink` (offline-first — record written FIRST to the encrypted DB, then a `SyncOperationType.update` envelope SEALED by the Task 3.3 AES-256-GCM cipher; UUID v4 idempotency keys; localVotes() recovery snapshot) + `InMemoryLedgerFeedRepository.vote` is now DIRECTION-based with toggle semantics (same-direction tap removes, switching moves by 2, unknown-id no-op, downvotes reversible from zero — no clamp drift); state: `LedgerFeedBloc.vote(postId, direction)` — the repository returns the RESOLVED direction (none on toggle-off) and that resolved state is what syncs; `LedgerPostSummary` gains `myVote` + `karmaScore`; a failing vote sink degrades gracefully (vote stays applied locally, never crashes); UI: shared `LedgerVoteBar` (up / karma-weighted score / down with ACTIVE-direction states in verifiedEmerald/alertRed, compact feed-card variant) wired into `LedgerFeedScreen` cards AND `LedgerPostDetailScreen` (which also gained same-direction-toggle and downvote tests); 1009 Flutter tests total (45 new: 9 karma-score unit — zero/determinism/sub-linear-up/down-curve/net-difference/clamp/bound/privacy-signature/ofNet, 6 wire-codec, 9 vote-sink — local-first/single-sealed/sealed-bytes-proof/UUID-v4/overwrite-revotes/localVotes/row-codec-round-trip/strict-bounds/failing-persist-no-partial-enqueue, 2 end-to-end integration — bloc→local→sealed→cold-restart-row-round-trip→sync-drain byte-identical + toggle-chain up→off→down queues three sealed envelopes with the RESOLVED directions, 7 vote-bar widget — score/up-tap/down-tap/active-up/active-down/both-muted/no-identity-strings, 2 feed-bloc — toggle-off + failing-sink-degrades, 1 repo — downvotes-reversible-from-zero, 2 detail-screen — same-direction-toggle-off + downvote, 1 schema — post_votes columns/no-identity, 2 migration — v5→v6 upgrade + v6 rollback, 4 security checkpoint — no-networking-no-print scan/identity-free-frame/bar-no-PII-literals/client-side-aggregate-only), `flutter analyze` 0 issues, affected suites 674/674 green (concurrency=1; parallel-run Argon2id contention pre-existing), FULL suite 1009/1009 green; SECURITY CHECKPOINT PASSED — vote weights ARE calculated client-side and CANNOT touch identity: the scoring function's signature accepts only two integers (structural proof), the sealed frame carries only post_id + direction, the `post_votes` table has no identity column, vote production files import no `dart:io`/http and print nothing (static scan), raw tallies are never rendered (only the karma score); next: Task 7.6 Peer Review Gate |
| 2026-08-10 | 1.23 | Completed Task 7.4 - Post Creation & Queuing — production `LedgerDraftSink` (`QueueLedgerDraftSink` in `lib/ledger/data/`): local-first write to the NEW `ledger_drafts` table (schema v5, migration up/down, `ledgerDraftRecord` row codecs with `pin_code` flagged SENSITIVE — the finest location signal the device holds) IMMEDIATELY, then a `SyncOperationType.create` enqueue of the `LedgerPostWireFrame` envelope (`lib/ledger/domain/ledger_post_wire_codec.dart` — strict `v:1` version check + strict `LedgerCategory.fromWireName` decode bounds, only civic fields: category/pin_code/headline/body, NO author/hash/phone/device on the frame) that the Task 3.3 AES-256-GCM `QueuePayloadCipher` SEALS before storage — the queue never persists plaintext drafts (byte-level test proof: stored payload ≠ plaintext frame bytes, opens back to the exact civic frame); queue item id is a UUID v4 idempotency key (Task 5.2/5.3); `localDrafts()` recovery snapshot survives cold restarts; 964 Flutter tests total (26 new: 8 wire-codec unit — round-trip/all-categories/civic-fields-only/optional-body/unknown-category-rejects/version-rejects/non-object-rejects/deterministic, 9 sink unit — local-first-persist/single-sealed-enqueue/sealed-bytes-proof/UUID-v4-key/localDrafts/row-codec-round-trip/unknown-category-row-rejects/failing-local-persist-no-partial-enqueue/FIFO-order, 3 end-to-end integration — compose→local→sealed→cold-restart-row-round-trip→sync-drain byte-identical/multi-post oldest-first drain/no-plaintext-in-queue, 3 schema/migration — ledger_drafts columns + sensitive pin/sensitive-column-checkpoint/v4→v5 upgrade/v5 rollback, 3 security checkpoint additions — no-networking-no-print scan of new files/wire-frame-no-identity-words/pin-scope-sensitive), `flutter analyze` 0 issues, affected suites 540/540 green (repository + state + sync) + ledger/database 89/89 green, FULL suite 964/964 green; SECURITY CHECKPOINT PASSED — posts are encrypted before sync by construction: the enqueue path seals the frame before the store sees it (proven at the byte level), the frame carries zero identity fields, the draft row lives inside the SQLCipher-encrypted database with the coarse pin scope explicitly flagged sensitive, no `dart:io`/http imports and no prints in the new production files; next: Task 7.5 Voting System with Karma Weighting |
| 2026-08-10 | 1.20 | Completed Task 7.1 - Ledger UI Foundation — new `lib/ledger/` domain tree (`LedgerCategory` enum w/ 5 PRD categories + strict `fromWireName`, `LedgerPost` entity w/ status/pin/category + `LedgerPostStatus` (published/peer_review/shadow_queue), `LedgerFeedRepository` port, `LedgerDraft` + `LedgerDraftSink` port) + `lib/ledger/data/` (local-cache-first `InMemoryLedgerFeedRepository` — pin-code + category scoping, published-first + newest-first ordering, clamped votes, edition marker; zero networking imports) + state layer (`LedgerFeedBloc`/`LedgerFeedState` w/ UI-safe `LedgerPostSummary` projection + `LocalLedgerFeedBloc` monotonic-seq snapshot guard; `LedgerComposeBloc`/`LedgerComposeState` + `LocalLedgerComposeBloc` validating FR-L1 exactly-one-category + 6-digit pin + non-blank headline, generic `hasError` only, draft persisted via the sink); UI per DESIGN §7 (newspaper register): `LedgerTheme` tokens (Ledger Green/Ink/Paper/Civic Gold/Alert Red/Verified Emerald, category→accent mapping), `LedgerMasthead` (nameplate + EDITION + pin scope + heavy rule), `CategoryChip` (pill w/ accent 12% bg), `LedgerFeedScreen` (masthead w/ live edition from state, `ALL` + category filter chips, post cards w/ chip/time/headline/pin·handle/vote+comment counts/verified `[✓ 3/3]` badge, Peer Review teaser strip w/ pending count, explicit `Load older posts` pagination — NOT infinite scroll, Ledger-Green compose FAB), `LedgerPostDetailScreen` (category + pin scope, serif headline, non-PII `Posted by ★ Analyst/Contributor/Citizen` tier line, Peer Review Gate badge, vote controls through the bloc, replies section placeholder), `LedgerComposeScreen` (category dropdown + pin code + headline + details, evidence row placeholders, karma messaging row, `Publish — send to review` fires the queue only on success); 885 Flutter tests total (61 new: 5 category, 7 repository, 7 feed-bloc, 10 compose-bloc incl. failing-sink degradation, 4 masthead, 3 chip, 7 feed-screen, 6 detail-screen, 6 compose-screen widget, 1 feed→detail→compose navigation integration w/ real stores, 5 security checkpoint), `flutter analyze` 0 issues, affected suites 372/372 green (312 state/ui + 60 ledger; no Argon2id hangs — no crypto in this task); SECURITY CHECKPOINT PASSED: local-cache-first by construction (no `dart:io`/http/WebSocket imports in the data/state layers — static scan), no `print(`/`debugPrint(` in ledger production files, screens render only non-PII author handles + pin scopes + fixed labels (no phone/hash/name literals in UI source), post summaries project away identity internals; next: Task 7.2 Geographic Clustering Implementation |
| 2026-08-10 | 1.16 | Completed Task 6.3 - Message Encryption & Decryption — X3DH session establishment wired into the connection-approval hook (deferred from 6.2): `SessionManager` (initiator/responder X3DH via `X3DHService` + per-peer Double Ratchet stored in `SessionStore` keyed by 64-hex blind hash), `PreKeyBundleSource` port + in-memory registry, `SessionEstablisher` port (`LocalSessionEstablisher`) invoked best-effort by `LocalConnectionRequestsBloc.accept` — a missing bundle never fails the local accept; composer wiring: real `ConversationComposer` replaces the static 52dp placeholder, `MessageBloc.send` encrypts via the `MessageCipher` port (`SignalMessageCipher`) and persists offline-first as `direction: sent, delivered: false` (queued sealed payload); message decryption: `MessageSummary.content` now carries DECRYPTED plaintext (BLoC decrypt-on-emit with stale-snapshot guard) and `MessageBubble` renders it, falling back to the fixed `[end-to-end encrypted]` placeholder when undecryptable — never raw ciphertext; explicit `MessageDirection` field on the `Message` entity + schema migration v3 (`messages.direction`, backfilled by the old delivered-heuristic so existing sent/received rows keep their side) supersedes the heuristic `MessageSideResolver`; 655 Flutter tests (43 new: 9 session manager + 7 establisher/cipher + 9 message-bloc encrypt/decrypt + 3 bubble content + 5 composer + 3 approval-hook + direction/schema/migration/copyWith), `flutter analyze` 0 issues, suite green; 216 Go test funcs, `go test -race` green, golangci-lint 0 violations; zero-plaintext-logged + no-ciphertext-in-state + FLAG_SECURE-intact checkpoints passed; read-receipt ✓✓✓ and TTL deletion deferred to later Phase 6 tasks |

